# Ralph Progress Log — ralph/cctop-pane
# Feature: cctop pane (tasks/prd-cctop-pane.md v1.1)
# Started: 2026-09-12
# Format: [US-XXX] PASS|FAIL | summary
#

## Codebase Patterns
- Native slash command is `/cctop-pane` (`COMMAND` in plugin/hooks/pane.tsx): the engine refuses `$.command.register({ name: "cctop" })` because the plugin's skill already owns `/cctop` (`/cctop:cctop`). Stories that say `/cctop <args>` for the native command mean `/cctop-pane <args>`; `/cctop` alone is the skill.
- `claude -p --debug` writes nothing to stderr; the debug log is `~/.claude/debug/latest` (symlink to the newest `<session>.txt`). Grep it for `hooks module cctop loaded`, `refused`, `hook failed`.
- TS toolchain: `typescript` is the only devDependency and tsconfig has `types: []`, so `node:test` / `node:assert/strict` are declared by hand in `tests/pane/node.d.ts` (extend it when a test needs another assert method). `npm test` compiles with tsconfig.test.json (commonjs + esModuleInterop, outDir `.test-build`) and runs `node --test '.test-build/tests/pane/**/*.test.js'` (a bare directory arg is not a glob in Node 22).
- In a `.catch` handler the failure is `next.error` (`{ kind, message?, budget }`), not a `failure` field; there are no `$.ui.*` element globals, always `const { Text } = $.ui.resolve(e)`.
- `claude plugin validate --strict ./plugin` prints the hooks and `$` calls it found in the module; use it to confirm the engine sees the events you meant.
- Render tests: import `tests/pane/harness.ts` first (it installs the JSX globals `h`/`Fragment` the compiled module calls), build the event with `paneRender(id, columns)`, dispatch `ui.render` through `fakeOn($)`, and flatten with `renderToText(tree, columns)` (`tests/pane/render.ts`: row/column Boxes, width/flexGrow/padding, Text wrap/truncate, `[hotkey label]` Buttons). Fixtures load with `fixture('summary')` from `tests/pane/fixture.ts`; `scripts/pane-fixtures.sh` regenerates the query ones.
- `claude plugin validate --strict` refuses `$` handed to any function that is not a top-level declaration of the module (`$ is passed to "apply", which is not a function declared at the top of this file`): keep module state in module-scope `let`s and write `$`-taking helpers as top-level `function`s; `register` resets the state.
- The validator follows `$` only into top-level functions of the same file, never across an import: a helper module that needs the engine takes a facade type (a `Pick` of the `$` nouns it uses) built by a top-level function in pane.tsx (`pollerEngine($)`); `$.env.get` needs its literal name in pane.tsx too. The fake `$` is structurally assignable to such a facade. Also: `fakeEngine` copies `opts.process` at construction, so a test that changes answers later writes to `$.process.script`.
- In a `.tsx` module never name a parameter or local `h` (or `Fragment`): JSX compiles to calls of the global `h`, and a shadowing binding typechecks fine but fails at run time with `h is not a function`. Views take `Pick<ElementTable<'terminal'>, 'Box' | 'Text'>` built from `$.ui.resolve(e)` in pane.tsx; tests hand them `fakeElements(new Map())` and walk the tree for `key`/`wrap`/`color` props.

[US-001] PASS | TS toolchain (package.json, tsconfig.json, tsconfig.test.json, tests/pane/node.d.ts shim), hooks skeleton (plugin/hooks/hooks.json, pane.tsx: session.start registers the command, command.run opens the pane, ui.render draws `cctop`), docs/verification/pane.md, CI `pane` job; 1 placeholder test; typecheck/test/validate green, headless load check exit 0 with no refused line. Gotcha: the command had to be named `cctop-pane` because the engine reserves `/cctop` for the plugin's skill (PRD open question 1); criterion (a) named it `cctop`, resolved as the PRD's own contingency prescribes.
[US-002] PASS | headless harness: tests/pane/harness.ts (fakeEngine with fake element table, counted invalidates, captured logs, manual clock tick, in-memory store/fs, scripted process.run and session; fakeOn with matcher/glob chain, working next, .catch replay; paneRender), tests/pane/render.ts (renderToText: flex row/column, width/%/flexGrow/flexShrink, padding/margin/border, wrap + truncate-start/middle/end, Buttons), tests/pane/fixture.ts + fixtures/{summary,tools,files,agents,advice,events,usage}.json, scripts/pane-fixtures.sh; 24 tests (skeleton at 50/80 cols, render, harness, fixtures). Gotcha: the d.ts `$.env` is `{ get, set }` not a plain object, so the fake keeps the object at `$.env.values`; and a fixed-width Box in an overflowing row is shrunk only after content-sized siblings (Yoga would shrink both).
[US-003] PASS | plugin/hooks/model.ts (Model, initialModel, pure reduce over session.start/turn.start/turn.complete/tool.start/tool.end/session.compact/usage, percentile, formatCountdown, usageRows with docs/metrics.md keys); pane.tsx hooks for turn.start/turn.complete/session.compact and a tool.call wrapper timing next(e) (isError, text.length -> ceil/4), $.session.usage() only from a $.clock.every(1000) timer while busy plus once after session.start/turn.complete (after next), every registration .catch -> $.ui.log; interim render of turn/running tool/usage rows; tests/pane/reducer.test.ts (10 tests: scripted sequence, purity, concurrent tools, percentile, usage rows from fixtures/usage.json, timer cadence, pass-through, throw path, render at 50/80); 34 tests total; typecheck, validate and the headless load check green (all 7 events listed, no refused line). Gotcha: the validator only accepts `$` passed to top-level functions, so the model and helpers moved out of `register`.
[US-004] PASS | plugin/hooks/poller.ts (createPoller(engine, getModel, setModel) with start/stop/tick/reschedule/running, one in-flight promise over cctop query summary|tools|files|agents|advice|events --session <id> at timeoutMs 5000, 2 s busy / 10 s idle, verbs parsed once from `cctop query --help` (parseQueryVerbs), failures keep the previous JSON, stale after 30 s without a full success, writeMarker stub to ~/.cctop/pane/<id>.json); model.ts gains query/queryAt/verbs/sessionId, the binary/session.id/verbs/query/tick/stale actions, isSupported/unsupportedVerbs, UNSUPPORTED and the precedence comment (Context/Limits from usage else query.summary); pane.tsx detects the binary with `cctop --version` (3 s) after session.start's next(e), starts the poller on open when present, reschedules on turn.start/turn.complete, draws the install hint, a stale line and `<verb>: unsupported by this cctop version`; tests/pane/poller.test.ts (13 tests: help parsing, tick order, cadence, no overlap on a hung summary, keep-previous + stale + recovery, unsupported verb, missing binary, marker, hooks: missing/non-zero version, present cadence, unsupported line, late detection); 47 tests total; typecheck, validate and headless load check green. Gotcha: createPoller cannot take `$` (the validator refuses `$` across imports), so it takes a facade built in pane.tsx; see notes.
[US-005] PASS | plugin/hooks/views/overview.tsx (renderOverview(model, el, columns, placement, now); header/contextBlock/tokensBlock/limitsBlock/turnBlock/advisorLine are pure row builders keyed by docs/metrics.md ids; two columns from 60 body columns, values right-aligned in fixed-width Boxes, every Text wrap=truncate, palette colours only, ≈ on approx sources, `needs the cctop binary` once each for velocity, the token breakdown and the Advisor when missing; inline placement = header + Context + Limits), views/format.ts (Measured/missing accessors over the query JSON, TUI-style duration and usd formats); model.ts gains modelName ($.session.model after session.start) and turn.toolMs (api vs tool time = elapsed − tool time, marked ≈); pane.tsx draws the Overview then the stale/unsupported/install lines; tests/pane/overview.test.ts (15 tests: fixtures at 50/60/80, no row over width, two-column only ≥ 60, ≈ exactly where the fixture says approx, row keys ⊂ metrics.md ids, Text wrap + palette check incl. a source grep of views/, dim badges, high-severity Advisor last row, missing binary, no source, inline); skeleton/reducer render assertions updated; 62 tests total; typecheck, validate and the headless load check green. Gotcha: `cctop query advice` items carry no `severity` (src/advisor/mod.rs), so the Advisor-first row only fires once a binary adds one; summary has no api/tool split either, so tool time is engine-native from the tool.call timings and API time is derived.
