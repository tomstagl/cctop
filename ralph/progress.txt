# Ralph Progress Log — ralph/cctop-v1
# Feature: cctop v1 (tasks/prd-cctop.md v1.1)
# Started: 2026-09-11
# Format: [US-XXX] PASS|FAIL | summary
#
[US-001] PASS | Cargo project with ratatui/crossterm/tokio/notify/serde/clap, 11 subcommand stubs, CI (fmt/clippy/test on macOS+Ubuntu), fixtures/README. Cargo.lock pins instability 0.3.7 and darling 0.20.10 for rustc 1.85.
[US-002] PASS | registry::Session/list/is_alive (libc kill 0, EPERM=alive), newest-first, malformed files skipped; fixtures/sessions busy+idle+dead+broken; 6 tests. Crate split into lib + bin. Note: brew upgrade of rust was building from source and was aborted; staying on 1.85 with lock-file pins.
[US-003] PASS | discover::resolve with precedence --session > --cwd > $CLAUDE_SESSION_ID > tmux pane TTY > $PWD (busy, newest); Env trait mocked in 11 tests; --wait polls; exit 2 with the spec'd message; `cctop run` wired to real registry.
[US-004] PASS | transcript::Line (User/Assistant/System/CostState/PermissionMode/QueueOperation/FileHistorySnapshot/Attachment/Unknown) with typed blocks, Usage incl. ephemeral_5m/1h + cache_ttl(); scripts/anonymise-transcript.py; fixtures/session-a.jsonl (1463 lines, 143 responses, 13-line response, cost-state, 9 turn_duration, 9 stop_hook_summary); 6 tests.
[US-005] PASS | tail::Tailer (thread + notify kqueue/inotify on dir and file, 1 s poll fallback, partial-line buffer, rewind on truncation, re-watch after rotation) emitting on tokio mpsc; parse_file; 6 tests incl. 50-append <500 ms; ignored 10 MB bench passes in ~0.4 s. notify uses macos_kqueue (FSEvents coalesced writes).
[US-006] PASS | metrics::Usage (5m/1h cache writes, cache_hit_ratio) + Aggregate/Turn: dedupe by message.id (fixture: 143 responses, 67,061 output tokens vs naive 214,682), 15 turns hand-verified, per-turn api_calls/models/effort/context_size/ttl/tool counts, turn_duration attached; 5 tests.
[US-007] PASS | pricing.toml (bundled, prefix-matched, user override merge), metrics::cost: Price (5m 1.25x / 1h 2x / read 0.1x defaults, explicit overrides), CostTracker (cost-state authoritative + deduped estimate since, approx flag, by_model), rates() trailing 15 min, ISO timestamp parser; price table reproduces the fixture's cost-state to <1 %; 6 tests.
[US-008] PASS | tools::Call/Stats: tool_use↔tool_result pairing by id (257/257 on fixture), mcp__server__tool → mcp:server + mcp_tool, input_summary ≤30 chars, approx durations from transcript timestamps, set_exact_duration hook for later, by_name() p50/p95/errors/tokens_to_ctx, top_ctx(n); known Bash call = 863 ms / 725 tokens; 6 tests.
[US-009] PASS | agents::Agent (meta.json + transcript, deduped usage, model from transcript over 'inherit', fork-context-ref contextLength, state running/done/failed with 60 s rule), load() and AgentWatcher (dir notify + per-file Tailer, late meta refresh); fixture fork agent (8 responses, 1415 output tokens); 4 tests.
[US-010] PASS | Turn gains hook_runs/hook_ms/hook_errors (stop_hook_summary) and elapsed_ms(now) (exact from turn_duration else live); Aggregate gains queued_prompts (queue-operation enqueue/remove/popAll) and away summaries; fixture: turn 3 = 95,830 ms, hook_ms sum 456, queue ends at 0; 1 test added.
[US-011] PASS | metrics::registry (metric! macro, 47 metrics across 9 panels with id/unit/formula/sources/caveats/estimate_when), markdown() + splice_readme(); `cctop metrics --md` and `--readme`; docs/metrics.md generated and README block spliced; drift tests fail on stale docs or README; 5 tests.
[US-012] PASS | ui::Panel trait (+ placement/flexible/spec), draw_frame with collapsed one-line form, ui::State (focus/hidden/now), ui::layout::solve: narrow stack / wide two-column + bottom, priority collapse→drop, Events floor 4 rows, slack to flexible panels; 6 insta snapshots (40×24, 60×51, 122×37, forced modes) + overlap/bounds checks.
[US-013] PASS | app::App (panels + State + Sink), global keys q/Ctrl-C ? Tab/BackTab Esc p w +/- 1-9 with focused-panel routing, pause buffering + PAUSED badge, toast footer (3 s), help overlay from BINDINGS; run_tui with TerminalGuard (raw/alt-screen/mouse restored on drop), tick-capped diff rendering; headless `--once --render-to --size WxH --keys`; `--session <fixture.jsonl>` fixture mode; transcript_path() slug mapping; 6 tests.
[US-014] PASS | Header panel (status pill BUSY/IDLE/WAITING/PAUSED/ENDED hh:mm, turn, elapsed, running tool, cwd, branch*, permission mode, effort, plan, uptime, cost with ≈ API, cpu/rss) with title `cctop ─ name ── model · vX`; State gained collectors (agg/cost/tools), SessionInfo (registry/fixture, refresh_alive freezes clock), ui::fmt helpers, git::info, App tick hooks; wired in main; fixture render verified; 5 tests.
[US-015] PASS | metrics::context (default_window per model, ContextView: size/window/prefix/history/velocity EMA-5/compactions ≥30% drop/threshold learned-or-80%, turns_until_compaction), State overrides for shim figures + learned thresholds, ui::widgets gauge/sparkline/band_style, Context panel (gauge with bands, breakdown, sparkline + velocity + autocompact est, compactions); fixture: 40 % est, 396k/1.00M, prefix 60k, ~9 turns; 7 tests.
[US-016] PASS | Tokens & Cost panel: five scaled bars with right-aligned k/M values, cache-hit ratio with bands, cost (≈ when approx) + burn $/h + input tokens/min, per-turn sparkline + last turn tokens/$; `a` toggles subagent inclusion (State.tokens_include_agents, agents loaded for fixtures and watched live); rates() now windows on last activity; fixture: 34.18M total, 98 % hit, $9.90 ($26.0/h); 2 tests.
[US-017] PASS | Tools panel: sortable (s/S over N/ERR/p50/p95/LAST/TOKENS/TOOL) filterable (f inline, Esc clears) table with ▶now, ERR colour bands, ≈ on approximate durations, top-ctx line pinned to the last row, j/k selection, Enter → full-height detail overlay (last 20 calls); App gained overlay routing and captures_input; sort/filter status in the border summary; 3 tests.
[US-018] PASS | events::Log (time-ordered insert, 5000 cap, tool start/end incl. ✗ errors, hook summaries, turn-done notes, compaction on ≥30% drop, away, cost-state) fed from State::apply; Events panel newest-last with kind colours, Enter → full-height overlay with j/k/g/G scroll, `/` search with highlight and n/N; overlay Esc gives the panel first refusal; 5 tests.
[US-019] PASS | alerts::Engine with 11 edge-triggered rules (context>80%, compaction≤2 turns, 5h 60/80/95, exhaustion before reset, cache-hit<50% over 5 turns, tool>60s, permission wait>30s, MCP exited, API retry increase); deliver() → note event + toast + osascript/notify-send for critical when --notify; State gained Limits, mcp_exited, permission_waiting_since_ms placeholders; App evaluates on tick and after data; 3 tests covering fire-once-per-crossing with resets.
[US-020] PASS | procs: ProcessTable trait (Ps via `ps -axo pid,ppid,pcpu,rss,etime,command`, fake for tests), descendants, MCP matching by command basename + args against ~/.claude/mcp.json, settings.json, <cwd>/.mcp.json (handles npx→`npm exec` rewrite), restart counting, exited detection, foreground shell command with elapsed; sampled every 1 s in a tick hook, feeds cpu/rss, mcp_exited, running command in the header; 4 tests.
[US-021] PASS | Agents & MCP panel: subagent rows (◐/✓/✗, type, description ≤28, elapsed, tokens, model), MCP rows from the process snapshot joined with mcp:<name> tool stats (calls, p95 or idle >5 min, ↻ restarts, exited in red), background tasks from tasks::load (generic per-file reader, ~/.claude/tasks/session-<id8>/ rescanned every 2 s; dirs are empty on this machine so the format is read tolerantly); 3 tests.
[US-022] PASS | files::Files (Read/Edit/Write/MultiEdit/NotebookEdit by file_path/notebook_path, R/E/W counts, reads_since_edit → re-read ⚠ at 3, numstat apply, head_commit/numstat via git), Files panel (relative paths, counts, +a −d green/red or — outside git, s cycles last-touch/touches/lines/name, border `n touched · +a −d`), 5 s git tick against the attach-time HEAD; 4 tests.
[US-023] PASS | Turn panel: elapsed + api calls + api/tools time split (new Turn.api_ms/tool_ms from consecutive timestamps) + retries from cost-state; running tool with input summary and pid (from process snapshot) / thinking / idle / ◆ waiting for permission; hooks runs·ms (+failed), permission waits (— until hooks installed), queued prompts; SessionInfo gained hooks_installed/permission_waits/permission_wait_ms; noted turn_duration < wall time; 3 tests. M1 panels complete.
[US-024] PASS | status::Sample model, store/store_in (atomic temp+rename to <home>/status/<session_id>.json), run_shim (tee stdin, exec original with same stdin, pass stdout/exit code, never fails), Watcher (mtime poll, 24 h five_hour series); install::with_shim/without_shim (idempotent), unified diff via `similar`, backup to <home>/backups, confirmation unless --yes, install marker; State::apply_status → exact context (drops est), plan, Limits + series; e2e verified against real settings (aborted on N); 6 tests, env-free.
[US-025] PASS | metrics::limits least-squares fit over 30 min (≥3 samples) → exhaustion epoch; Limits panel: 5 h / 7 d gauges with 60/85 bands, ↺ countdown, `exhausted in <t>, before/after reset ✗/✓`, `— run cctop install` without the shim; other busy sessions counted every 5 s from the registry and shown in the border summary (moved from the line to avoid clipping at 60 cols); Limits.exhaustion_ms kept in sync for the alert; 4 tests.
[US-026] PASS | hooks: `cctop hook` records {at,event,session_id,tool_use_id,tool_name,payload minus bulky fields} to <home>/events/<session>.jsonl (always exit 0, <5 ms), prune >7 d, offset-tailing Watcher, with_hooks/without_hooks merging cctop entries into existing hook groups (user hooks untouched, idempotent); install/uninstall now apply shim+hooks; State::apply_hook: exact tool durations (approx cleared), permission pending/waits (post − perm − median ≈), PreCompact → learned threshold + event, agent/notification/session events; e2e hook run and install diff (12 hook entries) verified; 5 tests.
[US-027] PASS | ledger::rows/sorted/calls_of (per-turn start, duration, api calls, usage classes, priced cost, tools×n, compaction C, effort, model); ui::ledger_view overlay owned by Context (Enter on Context or Tokens opens it), s/S sort over 8 columns, j/k/g/G, Enter → turn's tool calls with TOKENS→CTX, Esc steps back; headless `--keys Tab,Enter` verified; 3 tests.
[US-028] PASS | prefix::Prefix fed by transcript attachments (deferred_tools_delta names+lines → per-tool bytes grouped built-in vs mcp:<server>, mcp_instructions_delta blocks, skill_listing content/count, agent_listing_delta) and disk scan (CLAUDE.md/.claude/CLAUDE.md/CLAUDE.local.md up to git root, ~/.claude/CLAUDE.md, memory MEMORY.md); rows() reconciles against the first-call prefix with `system prompt + other`, sorted, top two highlighted; opened with `i` on Context (p is global pause), Enter stays the ledger; lib::slug(); 3 tests.
[US-029] PASS | advisor::{Rule, Advice, Saving, Engine} (ranked by saving, recency tiebreak, session dismissals, retired rules drop), rules A01 cache miss, A02 cache expiry (observed TTL), A03 runaway/repeated result, A04 re-reads, A05 explore-in-main, A06 compaction churn with explain() texts; Advisor panel 9 (two lines, `k of n`, n/N/x, Enter explanation overlay), evaluated with alerts on every tick/data batch; firing + non-firing tests per rule, empty-session test, purity test (no process/network in sources, idempotent evaluate); fixture is quiet (98 % cache hits), this live session shows A03.
[US-030] PASS | rules A07 idle MCP (prefix schema tokens per server × last call), A08 thinking share (>40% over 5 routine turns, effort-aware), A09 permission waits (>2 min or >5 prompts per tool; new permission_by_tool), A10 long foreground Bash (>60 s), A11 fresh-input spike (>5k), A12 chatty turns (>20/h, median prompt <32 chars; new Turn.prompt_chars) + explain() texts; firing and non-firing tests each; 12 rules registered.
[US-031] PASS | rules A13 rate-limit pacing (Avoids), A14 Opus subagent on search/summarise work (price delta), A15 error loop (same tool+input failed ≥3×), A16 hook overhead (>10 % of turn time), A17 big prefix (>25 % of window, names the largest row), A18 no hand-off (>3 h and >70 %) + explain() texts; 18 rules registered; `cctop advise [--session] [--json]` via a shared load_state() (transcript, agents, prefix scan, status, hooks); tests per rule; A15 fires on the anonymised fixture because all Bash commands read `make check`.
[US-032] PASS | load::{Target, resolve, state, state_from} shared by TUI/advise/query (main no longer duplicates loading); query::{summary, ledger_json, tools, files, agents, advice, prefix, events, explain} with {value,unit,metric_id,approx} values and {source:missing,hint} for absent sources; `cctop query <what> --session ...` (attach flags global), exit 2 without a session; docs/query.md; insta snapshots for summary and ledger; 2 tests.
[US-033] PASS | baseline::compute (7-day transcripts, sessions ≥3 turns, medians of cost/turn, tokens/turn, cache-hit, error rate, model mix by family) cached hourly in <home>/baseline.json; loaded in load::state and the TUI; Tokens panel shows `· 7d ×n.nt ×n.n$`; `cctop query baseline`; report::markdown (cost, model mix, context, top consumers, most expensive turns, advisor, vs-7d) + `cctop report [--out -]` to <home>/reports/<date>-<name>.md, triggered detached by the SessionEnd hook; `cctop export --json|--csv [--events] [--out]`; real machine: 19 sessions, $0.81/turn median, 1.4 s compute; 4 tests.
[US-034] PASS | split::{detect, command, manual_hint, run} for tmux/zellij/WezTerm/Kitty/iTerm2 (pane runs `cctop run --session <id>`, host spawns it), `cctop split [--size]` resolves the session first, exit 3 without a host; CLI refactor: Headless args split from Attach (flag clash), all subcommands implemented (no fallthrough); tmux e2e verified (59-col pane, parent = tmux server, q leaves neighbour alive); docs/hosts.md matrix — zellij/WezTerm/Kitty/iTerm2 not installed here, marked untested; TUI now freezes clock for dead fixtures and skips git when cwd is missing; 2 tests.
