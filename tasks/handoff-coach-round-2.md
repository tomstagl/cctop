# Coach — hand-off to round 2

Written 2026-09-15 at the end of the implementation session (`main` at `4e53bfc`).

## Where things stand

Phases 0–7 of `tasks/plan-cctop-coach.md` are implemented and committed; the
plan's **Progress** paragraph (§1) is the record of what shipped in which
commit and every decision taken on the way. CI-equivalent checks are green:
`cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`
(290 passed, 2 ignored), `npm run typecheck`, `npm test` (119 passed) in
`plugin/hooks`. The last release tag is `v0.2.0`; the plugin binary installed
on this machine is the 0.2.2 release, **not this build** — nothing of the coach
has run live yet, so `~/.cctop/*.advisor.json` is empty and `cctop coach-stats`
prints zero sessions.

Key facts a fresh session needs:

- 35 rules in `src/advisor/rules/{token,events,outcome}.rs`; the engine is
  `src/advisor/mod.rs` (one slot per class order, TTLs, cooldowns, budget,
  three-strikes snooze, persisted occupant/promotions/cooldowns so readers
  follow the running dashboard, exposure arm, demotions from the record).
- One coach object (`src/coach.rs`) and one dashboard object
  (`src/dashboard.rs`) drawn verbatim by the TUI (`src/ui/coach_view.rs`,
  `src/ui/dashboard.rs`) and the pane (`plugin/hooks/views/coach.tsx`,
  `overview.tsx`); `cctop query coach|dashboard|advice` carry them.
- Fixture B moments (`tests/pane/fixtures/coach-<moment>.json`, regenerated by
  `scripts/pane-fixtures.sh fixtures/session-b.jsonl b`) are asserted
  row-identical on both sides; fixture A by `scripts/pane-fixtures.sh`.
- Measurement: `cctop run --coach on|off|auto`, `cctop coach-stats [--since 4w]
  [--replay ~/.claude/projects]`, `cctop coach-replay <files|dirs>`;
  `~/.cctop/<session>.advisor.json` holds the fire records, the arm and the cost.
- Cross-session: `src/insights.rs` (Claude Code's `usage-data`, counts and enum
  verdicts only), `src/baseline.rs` (7-day medians incl. active hours),
  `src/history.rs` (history.jsonl slash commands and paste sizes).

Working practices that held: edits by small scripted substitutions; snapshot
acceptance with `INSTA_UPDATE=always cargo test` then
`sed -i '' '/^assertion_line: /d' src/snapshots/*.snap src/ui/snapshots/*.snap`
and delete `*.snap.new`; regenerate pane fixtures after any change to the coach
or dashboard objects; commit per chunk with the trailers
`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` and
`Claude-Session: <session url>`; record each phase in the plan's Progress
paragraph.

## Open items, reviewed

| # | Item | Verdict for round 2 |
|---|---|---|
| 1 | The coach has never run live; no fire records; dogfood (US-009) and the rate targets (US-012) need sessions on this build | **Do first** — it gates everything measurable |
| 2 | Site screenshots (`make site`) stale since the dashboard/coach landed; `v0.2.0` is the last tag | **Do first**, as part of the release |
| 3 | Pane PRD (`tasks/prd-cctop-pane.md`) not amended for the coach line / `coach` verb / `$.prompt.fill` policy / per-family pane-native sources (US-013); the per-family "Claude Code surface it complements / when the coach stays silent" table missing; PRD §6.4 TTL text does not mention the turn-end exception (A32 holds through the next turn, A40 three turns); PRD boxes never ticked | **Do** — docs debt before anyone else reads the PRD as the contract |
| 4 | A11 ignores `history.last_paste` (the tailer collects it; the PRD wants the paste size to feed A11) | **Do** — small, precise win |
| 5 | Numeric `/context` agreement on a live session; TUI ↔ pane slot agreement (persisted occupant) never verified live | **Do during dogfood** as verification items |
| 6 | Per-family control-row metrics over the pre-coach corpus (2026-08-12 → 09-12) not computed; the control arm's acted rate stands in | Later — only if the control arm proves too slow for the rare families (needs ≥ 2 weeks of records to judge) |
| 7 | `coach-replay` precision against verdict labels: no per-fire labels exist (verdicts are per candidate) | Later — a ~200-fire hand-label pass is research, not code; decide after dogfood |
| 8 | Coach cost misses the pane's `$.prompt.fill` drafts; the status line is not its own surface | Later — fold into a measurement-polish chunk with #6 |
| 9 | Personal 5-hour-block ceiling for API-key users (`baseline.rs`) | Defer — no API-key user in the loop |
| 10 | A35 (plan-first as a slot rule) | Defer by design until its trigger replays ≤ 1-in-5 wrong |
| 11 | Shift+digit jump from a light to the dashboard overlay; pane tests on the `-b` fixtures | Drop unless dogfood asks for them |

## Round 2, in order

1. **Release and turn the coach on for yourself.** `make site` (screenshots +
   guide), bump `Cargo.toml` to 0.3.0, tag `v0.3.0` (`.github/workflows/release.yml`),
   update the tap, `cctop install --yes` so the plugin's binary is this build,
   run every session with `cctop run --coach auto` (or `coach = "auto"` in
   `~/.config/cctop/config.toml`) for a working week. Verification items while
   at it: `/context` numbers vs the Context panel on a live session; the pane
   and the TUI show the same nudge when both run; each P0 rule (A19, A23, A25,
   A32, A42, A45) fires correctly at least once; every false positive gets an
   `x`.
2. **Docs reconciliation** (one commit): amend `tasks/prd-cctop-pane.md`
   (US-013 items), add the per-family "complements / stays silent" table to the
   coach PRD, amend §6.4's TTL sentence for turn-end rules, tick the PRD boxes
   that hold, and note the `stats-cache.json hourCounts` decision (absent on
   2.1.270; active hours come from session-meta `message_hours`).
3. **A11 paste size** from `history.last_paste` (fixture pair; replay check).
4. **After two weeks of records:** read `cctop coach-stats --since 2w --replay
   ~/.claude/projects`; act on demotions the table shows; then decide #6–#8.

## Hand-off prompt

```
Continue the cctop coach work: round 2 per @tasks/handoff-coach-round-2.md
(read it first, then @tasks/plan-cctop-coach.md §1 Progress for the decisions
taken). main is at 4e53bfc with Phases 0–7 shipped and CI green. Work the
round-2 list in order: (1) release — make site, bump Cargo.toml to 0.3.0, tag
v0.3.0, update the tap and cctop install so the plugin runs this build, then
start dogfooding with `cctop run --coach auto`; (2) the docs reconciliation
commit (pane PRD amendment, the per-family complements/silent table, the §6.4
TTL sentence, tick the PRD boxes); (3) A11 reads history.last_paste. Split in
reasonable chunks, run make check and npm test in plugin/hooks before each
commit, commit, then continue. Record progress in the plan's Progress
paragraph.
```
