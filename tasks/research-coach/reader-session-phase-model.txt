SUMMARY: Corpus: 81 real sessions across 7 projects (2026-08-12 to 2026-09-12; 753 turns, 672 human; 6,268 API calls; 6,222 tool calls; opus-5 3,800 / sonnet-5 2,315 calls) plus fixtures/session-a.jsonl, analysed with aggregate-only Python/jq scripts in the scratchpad (phases.py..phases4.py, drops.py, switch.py, est.py; outputs phases_out.json..phases4_out.json, turns.csv, segments.csv). Phases are short and interleaved rather than session-long: 70% of tool calls are Bash (auto mode steers the model to cat/sed/grep instead of Read/Grep/Glob, which barely appear), so any phase detector must classify the Bash command string; with that, a rule set over the last 5 calls reproduces a finer per-call labeler on 94.0% of 6,255 calls with zero-call latency in 93% of segments. Exploring is 32.5% of calls, 28% of wall time and 51% of all context growth (explore turns add p50 11.9k / p90 93k tokens, 3.1k per API call, vs verify 1.6k and commit 1.3k), and 46 main-context explore runs of >=8 calls cost p50 48.7k tokens each while an Explore subagent returns p50 71 tokens and keeps its ~55k of reads off the main context. Implement-to-verify is fast when it happens (p50 2 calls, 31 s, 79% same turn) but 35% of edit episodes are never verified before a commit or the end of the session and 32% of commit calls had no check in the preceding 10 calls. Task boundaries are hard: commits happen every ~2 turns and the turn after a commit touches fewer new files than average (19% fully new vs 30% baseline), idle gaps and away summaries also continue the same files, ai-title never changes in 47/52 sessions and TodoWrite is unused (TaskCreate/TaskUpdate replace it, 32 completed updates in 18 turns); the only above-baseline signals are TaskUpdate completed (70% new files) and a turn ending in AskUserQuestion. Cache TTL is effectively 1 h (49/49 API calls after >60 min gaps were cold, 1/53 after 15-60 min), each cold restart re-caches ~190k tokens, and all 10 mid-session model switches were cold. Long autonomous runs (>20 calls, 17% of tool turns, 54% of billed input) end in a text answer 94% of the time with the same error density as short runs; their cost is context volume, not failure: the top 5% of turns (ctx at end p50 472k, p50 30 API calls) are 42% of all billed input. Autocompaction is rare on 1M-window models (one real compact_boundary at 567k, taking 80.7 s and dropping 337k tokens, after which the session ended; other sessions ran to 921k and 953k uncompacted), and three of the four 'compactions' cctop's 30%-drop rule would count are <synthetic> zero-usage API-error lines.

OPEN: What is the real autocompact threshold for opus-5/sonnet-5 on 2.1.269: one auto compaction fired at 567,672 tokens (compactMetadata.preTokens) while other sessions ran to 921k and 953k without compacting - is a [1m] model setting or a per-session window difference involved? PreCompact never fired in the 8 hook spools, so cctop's learned threshold is empty. | Which cache TTL applies when: transcripts show ephemeral_1h on 6,258/6,268 API calls and 1 h behaviour in the gap analysis, but the live status shim reports prompt_cache.ttl '5m' with expires_at for the current session; the coach countdown must pick the right source per session. | Precision of the phase heuristic is measured against a heuristic labeler that shares the Bash regexes (94%) and against the model's Bash 'description' field (47-78% by phase); a hand-labelled sample (~200 calls) from the owner's own sessions is needed for a real precision figure, especially for Bash commands that both write and run. | Is the Explore agent's returned text (p50 71 tokens, max 266) the full agent answer or a truncated/summarised toolUseResult? If the agent's final message is longer in its own transcript, the main-context saving is still valid but the 'returns ~70 tokens' claim overstates compression. | Does the near-absence of Grep/Glob/Read (30% of calls are non-Bash) hold outside auto mode? 462 of 753 turns were auto; default/acceptEdits sessions (88 turns) may use Read/Grep more and change the A05 threshold. | File-set novelty is a partial ground truth for task boundaries (only 359 of 682 turns touch identifiable files). Could session-meta/facets underlying_goal or a few owner-labelled sessions give a better boundary ground truth before A18 is rewritten? | Three of four '> 30% context drop' events are <synthetic> zero-usage lines (API errors): does src/metrics/context.rs already skip zero-usage assistant lines, or does A06 currently count them as compactions in live sessions? | The one observed autocompaction (80.7 s, 337k tokens dropped, 1,376 messages preserved) was followed by the session ending after one more API call - is that the user's habit (compaction = abandon) and should the coach therefore push /clear + hand-off note well before any compaction risk rather than forecasting compaction turns?

[PH-00] Corpus, method and where the numbers live | lever=none | used=no | conf=high
  DETAIL: All statistics come from aggregate-only scripts over ~/.claude/projects/*/*.jsonl (81 sessions, 7 project slugs, first 2026-08-12 15:58, last 2026-09-12 17:52) plus fixtures/session-a.jsonl. Per tool call the scripts record name, Bash command category (regex over '&&'/';'-split segments with cd/env prefixes stripped, priority commit > implement > verify > ops > wait > gitread > explore), result length/4 as tokens, is_error, timestamps; per API call (deduped by message.id) cache_read/cache_creation/input/output, model, stop_reason; per turn (non-meta user line without tool_result) prompt length flags only (slash command, short ack, done/next words), idle before, away_summary, ai-title change, permission mode. Phases per call: category plus context rules (git status/diff or a re-read of a just-edited path within 5 calls = verify; single explore/verify sandwiched between edits in one turn = implement). Segments = runs of one phase. 77/81 sessions have >=1 tool call, 53 have >=20. Models: claude-opus-5 3,800 API calls, claude-sonnet-5 2,315, haiku-4-5 118, fable-5-1 58, <synthetic> 5. Permission mode of turns: auto 462, default 71, acceptEdits 17, plan 8, null 195.
  SRC: /private/tmp/claude-501/-Users-tom-code-cctop/28c68a61-3744-474d-83a5-58141249e54f/scratchpad/phases.py (parse, classify_bash, assign_phases), phases2.py, phases3.py, phases4.py, drops.py, switch.py, est.py; outputs phases_out.json, phases2_out.json, phases3_out.json, phases4_out.json, turns.csv, segments.csv
  EVID: phases_out.json: sessions 81, turns 753, human_turns 672, api_calls 6268, tool_calls 6222; phases4_out.json corpus: first_ts 2026-08-12T15:58:09, last_ts 2026-09-12T17:52:43, sessions_with_ge1_tool_call 77, sessions_with_ge20_calls 53, projects 7; permission_mode_turns {auto 462, default 71, acceptEdits 17, plan 8, null 195}
  ACT: Reuse phases.py's classify_bash/assign_phases as the reference implementation and turns.csv/segments.csv as regression data when porting the heuristic to Rust.

[PH-01] Under auto mode 70% of tool calls are Bash; Read/Grep/Glob-based rules (A05) are blind | lever=token_savings | used=partial | conf=high
  DETAIL: Bash accounts for 4,333 of 6,222 calls (69.6%): explore 1,611, ops 826, commit 547, verify 491, implement 454, gitread 357, wait 47. Read is 258, Edit 317, Write 144; Grep and Glob do not appear in the top-25 tool unigrams at all. The system reminder shown in auto mode instructs the model to use Bash (cat/sed/grep/heredocs) instead of Read/Edit/Write. Consequence: A05's 'consecutive Read/Grep/Glob >= 8' fires in only 4 sessions, while a run of >= 8 consecutive read-only calls of any kind (Bash explore/gitread included) occurs in 39 sessions (p50 longest run 7, p90 14, max 24). A phase detector must classify Bash command strings; tool names alone cover 30% of calls.
  SRC: phases4_out.json tool_unigrams; phases2_out.json A05_max_consecutive_Read_Grep_Glob_per_session vs A05_variant_any_readonly_run_per_session; src/advisor/rules.rs:236-273 (A05)
  EVID: tool_unigrams: Bash:explore 1611, Bash:ops 826, Bash:commit 547, browser 546, Bash:verify 491, Bash:implement 454, Bash:gitread 357, Edit 317, Read 258, Write 144, ToolSearch 109 (no Grep/Glob entry); A05_max_consecutive_Read_Grep_Glob sessions_ge8 4; any-readonly variant sessions_ge8 39, p50 7, p90 14, max 24
  ACT: Extend A05 and the coach to count Bash read-only commands (cat/sed/grep/ls/find/git status|diff|log) as exploration and reset on Edit/Write/sed -i/heredoc writes; keep the 8-call threshold.

[PH-02] Phase mix at call level and how many sessions contain each phase | lever=none | used=no | conf=high
  DETAIL: Per-call phases (after context rules): explore 2,023 (32.5%), implement 988 (15.9%), ops 826 (13.3%: ssh/gh api/curl/kubectl/docker/clawctl), verify 773 (12.4%), commit 548 (8.8%), browse 546 (8.8%, browser MCP, 9 sessions), plan 188 (3.0%, incl. AskUserQuestion 66, TaskUpdate 57, TaskCreate 28, ExitPlanMode 5), coord 118 (SendMessage/RemoteTrigger/ScheduleWakeup), mcp 91, wait 47 (sleep/until loops), deliver 33 (Artifact/DesignSync/SendUserFile), delegate 31 (Agent non-Explore), explore_sub 10 (Agent Explore). Sessions containing each phase: explore 77, implement 52, ops 46, commit 46, verify 45, plan 34, coord 31, wait 22, mcp 16, browse 9, delegate 9, deliver 7, explore_sub 4. Verify is composed of 476 Bash test/build/lint commands, 160 git status/diff right after edits, 132 re-reads of a just-edited file (106 via Bash, 26 via Read) and 5 Skill review calls.
  SRC: phases_out.json phase_call_counts, sessions_with_phase; phases2_out.json verify_phase_sources
  EVID: phase_call_counts {explore 2023, implement 988, ops 826, verify 773, commit 548, browse 546, plan 188, coord 118, mcp 91, wait 47, deliver 33, delegate 31, explore_sub 10}; verify_phase_sources {command:Bash 476, git status/diff after implement 160, re-read (Bash) 106, re-read (Read) 26, command:Skill 5}
  ACT: Use a 9-phase vocabulary in the coach (exploring, planning, implementing, verifying, committing, ops, browsing, waiting, answering); ops and browsing are real phases in this user's work (13% and 9% of calls) and should not be folded into exploring.

[PH-03] Phases are short, interleaved runs, not session-long stages | lever=better_outcomes | used=no | conf=high
  DETAIL: Segment (consecutive same-phase calls) statistics: explore 806 segments, calls p50 1 / p90 6 / max 24, wall p50 3.2 s / p90 59 s, context added p50 1,981 / p90 26,317 / max 260,177 tokens; implement 465 segments, p50 1 / p90 4 / max 22 calls, dctx p50 1,705 / p90 7,498; verify 430 segments, p50 1 / p90 4 / max 14, dctx p50 1,381 / p90 6,725; commit 361 segments p50 1 / p90 3, dctx p50 1,135; plan 126 segments p50 1, wall p50 21 s / p90 273 s (user thinking time); browse 49 segments p50 3 / p90 23 / max 122 calls, dctx p50 4.4k / p90 25k. Almost all segments live inside one turn (turns_spanned p90 = 1 for every phase). Turns mix phases: 132 turns have 1 phase, 125 have 2, 110 have 3, 72 have 4, 84 have 5-9. Wall-time share (gaps > 30 min excluded): explore 28.3% (15.7 h), verify 16.1%, ops 14.7%, implement 12.9%, commit 11.4%, plan 8.5%, browse 4.0%.
  SRC: phases_out.json phase_segments, distinct_phases_per_turn; phases2_out.json wall_time_share_by_phase, segment_wall_seconds_by_phase
  EVID: explore calls_per_segment {p50 1, p90 6, max 24}, dctx_tokens_per_segment {p50 1981, p90 26317, max 260177}; implement {p50 1, p90 4, max 22}; distinct_phases_per_turn {1:132, 2:125, 3:110, 4:72, 5:37, 6:29, 7:13, 8:4, 9:1}; wall_time_share explore 0.283, verify 0.161, ops 0.147, implement 0.129, commit 0.114, plan 0.085
  ACT: Design coach messages for a phase that typically lasts 1-6 calls and 3-60 s: show the current phase plus run length, and time suggestions on run-length thresholds (explore run >= 6) rather than on 'phase started' events.

[PH-04] Transition structure: explore -> implement -> verify -> commit -> explore is the dominant loop; failing verify is followed by a commit in 5 cases | lever=less_rework | used=no | conf=high
  DETAIL: Segment transitions: explore>implement 200, implement>verify 174, ops>explore 172, explore>ops 170, explore>verify 146, verify>explore 143, commit>explore 134, verify>commit 129, ops>implement 96, implement>explore 92, explore>commit 81, implement>commit 78, verify>implement 69. After an implement segment: verify 37% (174/465), explore 20%, commit 17%, ops 16%, plan 4%. After a verify segment that succeeded: explore 130, commit 124, implement 61; after a verify with a tool error: explore 13, implement 8, commit 5, ops 3. Trigrams: explore>explore>explore 842, implement>implement>implement 329, explore>explore>implement 112, implement>verify>verify 80, verify>verify>commit 53. Turn-level dominant-phase transitions: explore>explore 72, answer>answer 50, explore>answer 35, explore>ops 27.
  SRC: phases_out.json segment_transitions_top, phase_trigrams_top; phases2_out.json after_implement_segment, after_verify_segment, verify_outcome_then_next, turn_dominant_transitions_top
  EVID: after_implement_segment {verify 174, explore 92, commit 78, ops 76, plan 20, END 7}; verify_outcome_then_next {ok>explore 130, ok>commit 124, ok>implement 61, err>explore 13, err>implement 8, err>commit 5, err>ops 3}
  ACT: Coach rule: if the last verify call had is_error and the next call is Bash:commit, show 'committing after a failing check' (5 occurrences in a month; cheap, high-precision).

[PH-05] Phase-detection heuristic over the last 5 tool calls: 94.0% agreement, zero-call latency in 93% of segments | lever=better_outcomes | used=no | conf=medium
  DETAIL: Rules (inputs: tool name, Bash command category, file paths of the last 5 calls): (1) last = Bash:commit (git commit/push/tag/merge/rebase/add, gh pr create/merge, gh release) -> COMMITTING; (2) last in {EnterPlanMode, ExitPlanMode, TaskCreate/TaskUpdate/TaskList/TaskGet, TodoWrite, AskUserQuestion, Agent(subagent_type=Plan)} -> PLANNING (AskUserQuestion/ExitPlanMode also set WAITING_ON_USER until the next user line); (3) last = Bash:verify (cargo test|build|check|clippy|fmt|run, npm test|run|ci, npx, pytest, make, go test|build, tsc, vitest/jest, eslint/ruff/mypy, curl localhost, ./target/...) -> VERIFYING; (4) last in {Edit, Write, MultiEdit, NotebookEdit, Bash:implement (sed -i, heredoc/redirect into a source file, mv/cp/mkdir/rm/touch, git checkout|stash|restore|reset, npm/cargo add)} -> IMPLEMENTING; (5) last is read-only (Read, Grep, Glob, Bash cat/sed -n/grep/ls/find/git status|diff|log, WebFetch, WebSearch, ToolSearch): (a) Bash git status/diff/log with an implement call in the previous 4 -> VERIFYING; (b) path equals a path edited in the previous 5 calls -> VERIFYING (re-read after edit); (c) >= 2 implement in previous 4 -> IMPLEMENTING; (d) >= 2 verify in previous 4 -> VERIFYING; (e) else EXPLORING, run length = consecutive EXPLORING decisions; (6) Agent/Task -> EXPLORING (delegated; credit as off-main if subagent_type = Explore); (7) browser MCP -> BROWSING, other mcp__ -> MCP, Bash ssh/gh api|run|workflow/curl/kubectl/docker/clawctl -> OPS, Bash sleep/until/wait -> WAITING; (8) no tool_use and assistant text -> ANSWERING. Scored against the finer per-call labeler (which shares the Bash regexes, so this measures the windowing logic, not Bash classification): 5,880/6,255 agree (94.0%); disagreements explore->implement 142 and explore->verify 113 (interleaved lookups the labeler smooths the other way), implement->verify 50, plan->other 30. Latency to the first correct call inside a >= 2-call segment: 0 calls 1,024, 1 call 23, 2 calls 11, 3 calls 11, never 27 (2.5%). Independent cross-check of the Bash classifier against the model's own 'description' field (crude verb lists): commit 212/285 = 74%, explore 880/1,125 = 78%, verify 149/316 = 47%, implement 93/203 = 46% (the verify/implement confusions are mostly commands that both write and run, e.g. python3/cat/mkdir first tokens, which the priority order labels verify).
  SRC: phases4.py coach_phase(); phases4_out.json coach_heuristic_vs_labels, coach_heuristic_latency_calls; phases_out.json bash_cmd_vs_desc_confusion, bash_cmd_vs_desc_agreement; phases2_out.json bash_disagreement_first_tokens
  EVID: coach_heuristic_vs_labels {calls 6255, agree 5880, ratio 0.94, top: explore->implement 142, explore->verify 113, implement->verify 50, plan->other 30, verify->explore 26}; latency {0: 1024, 1: 23, 2: 11, 3: 11, -1: 27}; bash_cmd_vs_desc_confusion commit|commit 212, commit|implement 40, commit|verify 18, commit|explore 15; verify|verify 149, verify|implement 92, verify|explore 71; agreement 1334/2370 = 0.563 overall
  ACT: Implement the 8 rules as a pure function over tools.calls (last 5) in State; estimated precision ~90% for commit/explore/implement, ~75% for verify (Bash write+run commands are ambiguous; label them VERIFYING since the check is what matters). Hand-label ~200 calls from the owner's own sessions to replace the proxy precision numbers.

[PH-06] Task-boundary candidate signals: how often each occurs in 753 turns | lever=less_rework | used=no | conf=high
  DETAIL: commit in turn 182 (24%; commits are p50 2 turns apart, mean 2.4, p90 5, max 33; 46 sessions commit); /clear 21 turns (of 53 slash-command turns; /model 22 is the most used, /effort 3, /context 3, /plan 1, /goal 2); ai-title: 1,634 title lines but only 1 distinct title in 47 of 52 sessions, 2 in 5 (title changes 5 turns total); ExitPlanMode 4-5 calls, plan_mode attachments 11; TodoWrite 0 calls (TaskCreate 28, TaskUpdate completed 32 / in_progress 11 / pending 5 in 18 turns; task_reminder attachment itemCount is 0 in 72/149 injections, 5-18 otherwise; todo_reminder itemCount 0 in 18/18); AskUserQuestion in 53 turns (8 turns end with it); away_summary before 131 turns; idle > 30 min before 46 turns, > 60 min 31; gap between human prompts p50 157 s, p75 464 s, p90 2,647 s; prompts with done/next/finished words 96; short acks ('ok','go','next'...) 23; interrupted turns 16 (transcript markers: '[Request interrupted by user]' 11, '... for tool use' 2); compact_boundary 1; SessionStart.source in hook spool: startup 5, clear 2; Stop hook background_tasks empty 7 / non-empty 11 (18 Stops); turn_duration.pendingBackgroundAgentCount present on 17 of 457 lines (values 1-3).
  SRC: phases_out.json boundary_signals, turns_between_commit_turns; phases4_out.json ai_title, todowrite, taskupdate_status, hook_spool; jq over ~/.claude/projects (slash commands via <command-name>, attachment task_reminder/todo_reminder itemCount, system turn_duration keys)
  EVID: boundary_signals {commit_in_turn 182, title_changed 5, short_ack_prompt 23, prompt_mentions_done_next 96, slash_clear 21, slash_any 53, exit_plan_mode 4, task_completed_updates 32, turns_with_task_completed 18, away_summary_before_prompt 131, idle_before_gt_30min 46, question_turns 53, compactions_observed 4}; slash counts /model 22, /clear 21; ai_title distinct_titles_per_session {1: 47, 2: 5}; taskupdate_status {create 28, completed 32, in_progress 11, pending 5}; todowrite {}; hook_spool stop_background_tasks {empty 7, nonempty 11}; turn_duration pendingBackgroundAgentCount {1: 15, 2: 1, 3: 1, absent 440}
  ACT: Drop ai-title and TodoWrite as boundary sources; wire TaskUpdate(status=completed), AskUserQuestion/ExitPlanMode endings, Bash git commit, /clear (system local_command + SessionStart.source=clear) and Stop.background_tasks/pendingBackgroundAgentCount into State as boundary events.

[PH-07] Validated: commits and idle gaps are checkpoints, not task ends; only TaskUpdate-completed and question-ended turns precede genuinely new work | lever=less_rework | used=no | conf=medium
  DETAIL: Proxy ground truth: file novelty of the next turn = share of file basenames touched (Read/Edit/Write/Bash paths) that were not touched in the previous 5 turns; 'fully new' = 100%. Baseline over 359 turns with files: mean novelty 0.56, fully-new 30.4%. After a commit turn (94 with files): 0.447 / 19.1% (lower than baseline: the next turn keeps working on the same files); after commit AND text ending: 0.452 / 19.4%; after idle > 30 min: 0.481 / 25.9%; > 60 min: 0.459 / 25.0%; after away_summary: 0.455 / 18.2%; after a short ack: 0.511 / 17.6%; prompt mentioning commit/PR: 0.487 / 25.4%; prompt with done/next words: 0.581 / 27.1%; after title change (n=3): 0.432 / 0%; after ExitPlanMode (n=4): 0.42 / 0% (plan exit starts implementation on files already read: next-turn dctx p50 29.9k); after TaskUpdate completed (n=14): 0.70 / 28.6%; after a turn ending in AskUserQuestion (n=3): 0.889 / 66.7%. Also, the turn after a commit starts with exploration 37.6% vs 33.3% baseline and after idle > 30 min 41.3% - weak. Conclusion: no single cheap signal identifies 'task done'; a coach should present boundary nudges as 'checkpoint reached' with the context size, and reserve 'hand off / clear' for a composite: turn ended in text (not tool_use/question), no pending background tasks (Stop.background_tasks empty or pendingBackgroundAgentCount absent), and (commit in the last 3 turns or TaskUpdate completed in this turn or a done/next prompt). Caveat: only ~half of turns touch identifiable files (ops-heavy sessions), so novelty is a partial proxy.
  SRC: phases4.py boundary rows; phases4_out.json boundary_signal_file_novelty; phases_out.json after_boundary
  EVID: boundary_signal_file_novelty baseline {n_with_files 359, mean_novelty_vs_prev5 0.56, share_fully_new 0.304}; prev_commit_turn {94, 0.447, 0.191}; idle_gt_30min {27, 0.481, 0.259}; away_summary {77, 0.455, 0.182}; prev_task_completed {14, 0.70, 0.286}; prev_ended_question {3, 0.889, 0.667}; prev_exit_plan {4, 0.42, 0.0}; after_boundary after_commit_turn next_starts_exploring 0.376 vs baseline 0.333; after_exit_plan next_dctx p50 29887
  ACT: Implement A18/coach 'handoff-safe moment' as the composite above, phrase it as a soft checkpoint ('good moment to /clear if the next task is unrelated'), and never infer task completion from a commit alone.

[PH-08] Context growth per phase: exploring fills context fastest and accounts for 51% of all growth; Bash cat/sed and Read are the culprits | lever=token_savings | used=partial | conf=high
  DETAIL: Share of positive context growth by segment phase: explore 51.0%, implement 15.1%, ops 11.5%, verify 7.8%, commit 4.5%, browse 3.9%, plan 2.3%. Per turn by dominant phase (compacted turns excluded): explore 229 turns, dctx p50 11,935 / p90 93,382 / max 215,498, 3,130 tokens per API call, 11 tool calls p50 6; implement 71 turns, p50 9,695 / p90 90,621, 1,742 per API call, 16 tool calls p50 7; browse 24 turns p50 25,930 / p90 79,549 (1,408 per call but 20.7 calls p50 12); ops 89 turns p50 8,979 / p90 30,086, 1,784 per call; verify 38 turns p50 4,328 / p90 17,631, 1,590 per call; commit 39 turns p50 6,231, 1,310 per call; plan 15 turns p50 10,445, 3,138 per call; answer-only 120 turns p50 730. Result tokens per call: explore 707, ops 369, commit 278, verify 250, implement 121. By tool: Bash:explore 1.01M result tokens (39.4% of all), p50 223 / p90 1,585, 80 results > 3k; Read 420k (16.4%), p50 479 / p90 4,006, 34 > 3k; Bash:ops 305k (11.9%); commit 152k; gitread 132k; verify 91k; implement 84k; browser 81k (p50 100). 'cat <file>' whole-file reads: n=67, mean 2,631 tokens, p90 5,855, 28 > 3k; bounded (head/tail/sed -n/grep|wc) vs unbounded Bash explore have similar means (598 vs 545) so the nudge should key on the observed result size, not the command shape. Of 178 results > 3k tokens, 111 are explore, 19 commit (git push/diff output), 14 ops, 8 verify. Explore runs >= 8 calls in main: 46, result tokens p50 8.2k / p90 31k, dctx p50 38.8k (segment) / 48.7k (run) / p90 103k / max 150k, 29 of 46 are Bash-only, 14 span more than one turn; they are followed by implement 17, ops 10, verify 5.
  SRC: phases2_out.json context_growth_share_by_phase; phases_out.json context_growth_by_dominant_phase, results_gt3k_tokens_by_phase/tool, exploration_main_vs_sub; phases4_out.json result_tokens_by_tool; phases3_out.json bash_explore_bounded_vs_unbounded, bash_explore_first_token_vs_result_tokens
  EVID: context_growth_share_by_phase {explore 0.51, implement 0.151, ops 0.115, verify 0.078, commit 0.045}; explore dctx_per_turn {p50 11935, p90 93382, max 215498}, dctx_per_api_call 3130; verify dctx_per_api_call 1590; result_tokens_by_tool Bash:explore {calls 1611, p50 223, p90 1585, sum 1010372, share 0.394, gt3k 80}, Read {258, 479, 4006, 420520, 0.164, 34}; cat_whole_file {n 67, mean 2631, p90 5855, gt3k 28}; explore_runs_ge8 {n 46, dctx p50 38811, p90 102941, max 149978}
  ACT: Coach line during EXPLORING: 'exploring: N calls, +X k ctx this run' with a delegate nudge at run >= 6 and a 'narrow the command' nudge when a single explore result exceeds 3k tokens (Read without offset/limit, cat whole file).

[PH-09] Cache hit and the autocompact estimate by phase; the estimator and the compaction detector are both unvalidated by observed data | lever=less_rework | used=partial | conf=high
  DETAIL: Cache hit ratio (cache_read / total input) of API calls by phase: explore 0.942 (p10 0.90, lowest: large fresh tool results), mcp 0.962, ops 0.969, deliver 0.972, answer 0.972, plan 0.978, verify 0.981, browse 0.986, implement 0.988, commit 0.991. cctop-style estimate 'turns until 80% of window' (EMA alpha 0.2 of per-turn dctx) evaluated at each turn, p50 of finite estimates: explore 12.7 turns, mcp 8.4, delegate 9.2, commit 15.0, ops 15.3, implement 16.9, verify 18.4, plan 20.1, answer 21.1, browse 22.0; the estimate jumps > 50% or > 3 turns between consecutive turns in 19 of 565 steps (3.4%). Actual compactions: exactly one compact_boundary line in 81 sessions (trigger auto, preTokens 567,672 -> postTokens 230,014, cumulativeDroppedTokens 337,658, durationMs 80,690, preservedMessages 1,376) in session e3346d84 at turn 29/32 on opus-5; the session ended one API call later. In the 11 turns before it the estimator (800k threshold) said 13.7-21.9 turns; context grew 1.3k-30k per turn (max single call +4,185), i.e. the run-up was ordinary. Two other sessions reached 921,058 and 953,081 tokens (opus-5, 452 and 530 API calls) without any compaction, and one ran at 740,603. The three other '>30% drop' events cctop's context.rs would count as compactions are <synthetic>-model assistant lines with zero usage (API error placeholders): 179,762 -> 0 -> 185,244; 197,987 -> 0 -> 198,931; 953,081 -> 0 -> 862,747 (the last after a 35.5 h gap). PreCompact fired 0 times in the 8 hook spools.
  SRC: phases_out.json cache_hit_by_phase_of_api_call, turns_until_autocompact_by_phase, turns_until_autocompact_volatility; drops.py and est.py output; jq compact_boundary compactMetadata; ~/.cctop/events/*.jsonl event counts; src/metrics/context.rs:100-109 (COMPACTION_DROP_RATIO 0.30)
  EVID: cache_hit_by_phase explore {n 1745, mean 0.942, p10 0.9}, implement {956, 0.988, 0.984}, commit {542, 0.991, 0.987}; turns_until_autocompact p50_finite explore 12.66, implement 16.93, verify 18.38, answer 21.05; compactMetadata {trigger auto, preTokens 567672, postTokens 230014, durationMs 80690, preserved 1376}; est.py turn 26 ctx 566520 est_turns_to_800k 21.9; drops.py seq [179762, 0(<synthetic>), 185244], [953081, 0, 862747]; max_ctx sessions 921058 and 953081 with no compaction; hook_spool has no PreCompact key
  ACT: (1) Ignore assistant lines with model '<synthetic>' or zero usage in compaction detection and velocity; (2) learn the threshold from compact_boundary.compactMetadata.preTokens (transcript) as well as PreCompact; (3) in the coach, replace 'N turns until compaction' with 'ctx X k of window, +Y k per call in this phase' since compaction was observed once and the estimator said 22 turns when it was 3 away.

[PH-10] Exploration in main context vs subagents: 2,023 main explore calls vs 43 Agent calls; an Explore agent returns ~70 tokens for ~55k of off-main reading | lever=token_savings | used=partial | conf=high
  DETAIL: Main-context exploration: 2,023 calls, ~1.43M result tokens (707 per call), by tool Bash:explore 1,460, Read 200, Bash:gitread 187, ToolSearch 108, WebSearch 40, WebFetch 28. Agent/Task calls in main transcripts: 43 in 12 sessions (Explore 10, general-purpose 12, Plan 2, fork 2, backend 3, architecture 3, and 6 custom types); text returned to main p50 114 tokens, p90 484, max 995; Explore agents return p50 71, max 266 tokens. Subagent transcripts on disk: 75 agent-*.jsonl (34 directly under <session>/subagents/, 41 nested under subagents/workflows/wf_*/ which cctop's agents.rs does not scan). Explore agents (4 with transcripts): own context max p50 55k, 8 API calls, 13 tool calls (Bash 32, Read 19 across the 4), 79 s. general-purpose (14): ctxmax p50 74k / p90 108k / max 144k, tool calls p50 19 / p90 97 (Bash 446, Read 39, nested Agent 11), 136 s p50. workflow-subagent (41): ctxmax p50 103k / p90 256k / max 398k, tool calls p50 21 / p90 63 / max 106, 214 s p50, tools Bash 767, WebFetch 189, Read 80. fork (2): inherit the parent context (ctxmax 74k and 300k). Comparison: a main-context explore run of >= 8 calls adds p50 48.7k tokens to the main context permanently (billed on every later call), whereas the same reading inside an Explore agent costs its own ~55k once and leaves ~70 tokens in main. Agent calls themselves take p50 2.2 s / p90 25 s of main-thread wait (run in background otherwise).
  SRC: phases_out.json exploration_main_vs_sub; phases4_out.json subagent_transcripts, agent_calls_in_main, explore_agent_vs_main_run; find ~/.claude/projects -path '*subagents*' -name 'agent-*.jsonl'; agent-*.meta.json agentType counts
  EVID: exploration_main_vs_sub main_explore_calls 2023, main_explore_result_tokens_est 1429659, agent_calls 41, sessions_using_agents 12; agent_calls_in_main result_tokens_returned {p50 114, p90 484, max 995}; explore_agent_vs_main_run {main_explore_run_ge8_dctx_p50 48666, explore_agent_result_tokens_p50 71, max 266}; subagent_transcripts files 75, workflow_nested 41; by_type Explore ctxmax {p50 55249}, tool_calls {p50 13}; workflow-subagent ctxmax {p50 102910, p90 255774, max 398351}; meta agentType counts workflow-subagent 41, general-purpose 14, Explore 4
  ACT: Coach nudge at explore run >= 6 in main: 'delegate the rest to an Explore agent (keeps ~50k out of your context)'; show delegated exploration as a separate counter; fix agents.rs to walk subagents/** so workflow agents count.

[PH-11] Expensive turns (top 5% by billed input) are long, high-context tool loops, not thinking-heavy answers | lever=token_savings | used=partial | conf=high
  DETAIL: Threshold: 8.73M billed input tokens per turn; 33 turns = 42.2% of all billed input in the corpus. Shape: API calls p50 30 / p90 79 / max 338; tool calls p50 29 / p90 126 / max 293; context at end p50 472k / p90 892k / max 947k (all 33 > 100k; the rest of turns have p50 ctx 153k and p50 5 API calls); duration p50 534 s / p90 2,379 s / max 7,920 s; cache hit 0.991 (the tokens are cached but the volume is context x calls); models opus-5 26, sonnet-5 7. Dominant phase: ops 11, explore 7, browse 5, implement 5, verify 4, commit 1. Tools inside them: Bash:explore 331, Bash:verify 246, Bash:ops 210, browser computer 190, Bash:commit 168, Bash:implement 125, Edit 109, Bash:gitread 53, Read 47, Write 29. Call shares: explore 20%, verify 18.5%, implement 17%, browse 17%. Endings: 30 text, 1 interrupt, 1 api_error, 1 question. Session-level: billed input per session p50 2.85M / p90 34.7M / max 255M; 6 sessions exceed 400k context, 2 exceed 800k, 20 sessions run longer than 5 h wall time, 3 run > 3 h with max ctx > 700k.
  SRC: phases_out.json expensive_turns_top5pct; phases3_out.json session_totals
  EVID: expensive_turns_top5pct {threshold 8726271, n 33, share_of_all_billed_input 0.422, api_calls p50 30 p90 79 max 338, ctx_at_end p50 472141 p90 892199, duration_s p50 533.5, dominant_phase {ops 11, explore 7, browse 5, implement 5, verify 4}, top_tools Bash:explore 331, Bash:verify 246, Bash:ops 210, mcp__claude-in-chrome__computer 190, cache_hit_mean 0.991, rest_ctx_p50 152624}; session_totals sessions_gt_5h 20, max_ctx p90 337222, sessions_max_ctx_gt_400k 6, gt_800k 2
  ACT: Coach 'cost of continuing' line: tokens billed per call = current ctx; at ctx > 400k show 'next 30 calls ~ 12M tokens; hand off at the next checkpoint' - this targets 42% of spend with a rule that fires in 6 sessions a month.

[PH-12] Idle and time-of-day: cache TTL behaves as 1 h, every gap > 60 min is cold (~190k tokens re-cached), mid-session model switches are always cold | lever=token_savings | used=partial | conf=high
  DETAIL: 6,258 of 6,268 API calls carry cache_creation.ephemeral_1h_input_tokens > 0. Cache-write share of the call by gap since the previous API call: < 1 min 1.5% (16/5,552 cold), 1-5 min 3.0% (9/429), 5-15 min 5.4% (5/105), 15-60 min 1.4% (1/53), > 60 min 87.1% (49/49 cold, mean ctx 242k). First API call of a turn after idle > 5 min: mean write share 28.7%, 29 of 89 cold, 5.51M tokens re-cached in total (~190k per cold turn); after idle <= 5 min: 2.0%, 5/277. Prompts arriving 45-55 min after the last call: 5; 55-65 min: 2; 65-120 min: 2; > 2 h: 29 (users rarely return just before expiry; the countdown fires rarely but each miss is ~170-190k tokens; the live status shim reports recache_tokens_if_cold 171,882 for the current session and, notably, ttl '5m' with expires_at, which contradicts the 1 h behaviour seen in transcripts). Human prompts by local hour peak at 11:00 (73), 17:00-18:00 (64+62), 21:00 (57); none 01:00-05:00. Idle-before-prompt p50 by hour: 13:00-15:00 232-264 s (post-lunch), 17:00-19:00 37-52 s, 06:00 31,089 s (overnight resume); cold turns by hour: 18:00 6, 13:00 3, 17:00 3. Model switches between consecutive API calls: 10, all cold (write share 0.51, 0.71, 0.84, then 7 x 1.00); /model is the most-used slash command (22 uses). Wait after an AskUserQuestion: p50 213 s, mean 2,633 s (44 min), p90 2,911 s, max 44,530 s - a pending question is the most likely place for a cache-expiring idle.
  SRC: phases_out.json cache_write_share_by_gap_since_prev_api_call, first_api_call_write_share, prompts_by_local_hour, idle_p50_by_hour, cold_turns_by_hour, ttl_1h_api_calls; phases4_out.json prompt_idle_buckets_near_1h_ttl; switch.py output; phases3_out.json wait_after_AskUserQuestion_s; ~/.cctop/status/<newest>.json prompt_cache
  EVID: cache_write_share_by_gap >60m {n 49, mean_write_share 0.871, cold_gt50pct 49}, 15-60m {53, 0.014, 1}; first_api_call_write_share idle_gt_5min {n 89, mean 0.287, gt50pct 29, tokens_rewritten_sum 5513436}; prompt_idle_buckets {45-55m 5, 55-65m 2, 65-120m 2, >2h 29}; model_switch_api_pairs 10, cold_after_switch 10; status shim prompt_cache {ttl '5m', expires_at 1789228653, hit_ratio 0.979, recache_tokens_if_cold 171882}; wait_after_AskUserQuestion_s {p50 212.8, mean 2632.8}
  ACT: Coach countdown 'cache warm for mm:ss (cold restart ~ctx tokens)' driven by last API call time + TTL from status prompt_cache.expires_at when present, else 1 h; escalate when a question/permission is pending; warn before /model or /effort when ctx > 100k ('switch re-caches the whole context').

[PH-13] Implement -> first verification: fast when it happens (p50 2 calls, 31 s, same turn 79%) but 35% of edit episodes are never checked before commit or session end | lever=less_rework | used=no | conf=high
  DETAIL: 465 implement episodes (first implement call after a non-implement call). 300 reached a verify call before the next commit or the end of the transcript; 165 (35%) did not. Distance to the first verify: p50 2 calls / p90 14 / max 43; p50 0 turns / p90 1 / max 28 (238 of 300 in the same turn); wall p50 31 s / p90 523 s / max 3.7 h, 37 episodes > 10 min; context added between edit and check p50 3.3k / p90 21k tokens. Commit calls with no verify in the preceding 10 calls: 175 of 548 (32%). Turns containing an implement call: 225; of these 123 (55%) also contain a verify after the edit in the same turn. Verify after edits is mostly Bash test/build (476) but 160 are just git status/diff and 132 are re-reads of the edited file - a coach counting only test/build commands would rate verification lower still.
  SRC: phases_out.json implement_to_first_verify, commit_calls_without_verify_in_prev10; phases4_out.json implement_to_first_verify_seconds/dctx/gt10min; phases2_out.json turns_with_implement, turns_with_implement_then_verify_same_turn, verify_phase_sources
  EVID: implement_to_first_verify {implement_episodes 465, verified_before_commit_or_end 300, never 165, calls_until_verify p50 2 p90 14 max 43, turns_until_verify p50 0 p90 1 max 28, verify_in_same_turn 238}; implement_to_first_verify_seconds {n 291, p50 31.1, p90 522.9}; implement_to_first_verify_gt10min 37; commit_calls_without_verify_in_prev10 {commit_calls 548, unverified 175}; turns_with_implement 225, then_verify_same_turn 123
  ACT: Coach state after the first edit of a turn: 'edited N files, no check yet' turning amber after 10 min or 14 calls without a test/build; block-level nudge when a Bash:commit appears with no verify in the previous 10 calls (fires on ~32% of commits).

[PH-14] Long autonomous runs (> 20 tool calls with no user input) end well 94% of the time; their risk is context volume, and interrupts happen early (p50 3 calls) or not at all | lever=token_savings | used=no | conf=high
  DETAIL: Turns merged with any following non-human (task-notification/system) turns: 80 runs > 20 calls out of 471 turns with tools (17%); tool calls p50 32 / p90 76 / max 293; API calls p50 30 / max 338; duration p50 488 s / p90 1,324 s / max 7,920 s; context added p50 59.5k / p90 155.5k / max 634k; billed input p50 6.0M / p90 15.3M; share of all billed input 53.8%. Endings: text 75 (94%), question 3, interrupt 2, error 0; runs > 50 calls: 20 (text 17, question 2, interrupt 1). Tool error density inside long runs 4.2% vs 4.4% in short runs. Internal shape (77 long turns): 56 contain a verify (first verify at 40% of the way through, p50), 42 contain a commit; phase share explore 28%, implement 17%, verify 15%, browse 14%, ops 10.5%, commit 8% (short turns: explore 38%, ops 17%, implement 15%, commit 10%, verify 9%). Dominant phase: explore 33, implement 17, ops 11, browse 10. Interrupted turns overall (16): calls before interrupt p50 3 / p90 37 / max 125; dominant explore 5, verify 4, implement 2, plan 2. The 'blocked' ending (automode-blocked/permission-rule denial as last call) occurred 5 times; api_error 4.
  SRC: phases_out.json long_autonomous_runs_gt20_calls, error_density, interrupt_turn_calls, interrupt_turn_dom, turn_endings; phases4_out.json long_runs_gt20_shape
  EVID: long_autonomous_runs_gt20_calls {n 80, of_turns_with_tools 471, endings {text 75, interrupt 2, question 3}, tool_calls p50 32 p90 76 max 293, dctx p50 59547 p90 155502 max 633673, share_of_all_billed_input 0.538, gt50_calls 20, gt50_endings {text 17, interrupt 1, question 2}}; error_density long 0.042 vs short 0.044; long_runs_gt20_shape {with_verify 56, with_commit 42, first_verify_relative_position p50 0.4}; interrupt_turn_calls {p50 3, p90 37, max 125}
  ACT: Do not coach 'interrupt the run'; instead show a running 'this run: N calls, +X k ctx, ~Y M tokens billed' and, once ctx > 400k, prefer 'let it finish, then hand off'. Surface an early-course-correction cue only in the first ~5 calls of a turn (where real interrupts cluster).

[PH-15] Waiting moments: questions, permission prompts and idle prompts are where the user is absent and a notification beats an on-screen hint | lever=better_outcomes | used=partial | conf=high
  DETAIL: Turn endings: text 612, no_response 108 (slash/meta), interrupt 16, question 8 (53 turns contain AskUserQuestion; 66 calls), blocked 5, api_error 4. Turn duration (system turn_duration) p50 53.3 s, p90 340.6 s, max 7,920 s (n=457). Permission denials on user lines: automode-blocked 43, permission-rule 29, user-rejected 8, automode-unavailable 3. Hook spools (8 sessions): PermissionRequest 3, Notification permission_prompt 3 and idle_prompt 5, PostToolUseFailure 30 (is_interrupt false in all), Stop 18 (background_tasks empty 7), SubagentStart 41. Plan mode: 8 turns; ExitPlanMode results are large (p50 2,388 tokens, max 4.7k) and the following turn adds p50 29.9k. Queue operations (queued prompts) 453 lines; 'queued' promptSource on 24 human prompts; 'suggestion_accepted' on 80 prompts (the user accepts Claude's suggested next prompt - a boundary-adjacent moment).
  SRC: phases_out.json turn_endings, denials, permission_mode_turns; jq turn_duration durationMs; phases4_out.json hook_spool; phases3_out.json wait_after_AskUserQuestion_s; jq user promptSource|origin; phases4_out.json result_tokens_by_tool ExitPlanMode
  EVID: turn_endings {text 612, no_response 108, interrupt 16, question 8, blocked 5, api_error 4}; turn_duration n=457 p50 53.283 s p90 340.647 s max 7920 s; denials {automode-blocked 43, permission-rule 29, user-rejected 8, automode-unavailable 3}; hook_spool {PermissionRequest 3, Notification permission_prompt 3, idle_prompt 5, PostToolUseFailure 30 all is_interrupt=False, Stop 18}; promptSource counts typed|human 377, suggestion_accepted|human 80, system|task-notification 62, queued|human 24, sdk|human 18
  ACT: Coach WAITING state: show what is awaited (question / permission / idle), elapsed, and the cache countdown; fire the desktop notification path (alerts.rs critical()) on question/permission after 30 s since the user is typically away for minutes to hours.

[PH-16] The fixture session-a does not exercise the coding loop; coach tests need a second fixture | lever=less_rework | used=yes | conf=high
  DETAIL: fixtures/session-a.jsonl: 15 turns, 143 API calls, 257 tool calls, 0 compactions. Phase counts: browse 214, coord 16, verify 15, explore 7, mcp 2, implement 1, plan 1, deliver 1; segment sequence starts explore(1) verify(1) plan(1) verify(1) explore(3) coord(1) verify(3) coord(14) verify(4) ... and ends with two browser runs of 91 and 122 calls. There is no explore -> implement -> verify -> commit cycle, no commit at all, no AskUserQuestion, no idle gap > TTL and no subagent exploration, so phase, boundary, implement-to-verify and cache-expiry rules cannot be tested against it.
  SRC: phases_out.json fixture; /Users/tom/code/cctop/fixtures/session-a.jsonl (1,463 lines)
  EVID: fixture {turns 15, api 143, calls 257, phase_counts {explore 7, verify 15, plan 1, coord 16, mcp 2, deliver 1, browse 214, implement 1}, compactions 0}
  ACT: Add an anonymised fixture (or synthesise one from turns.csv/segments.csv shapes) containing: an 8+ call explore run, Edit -> cargo test -> git commit, an unverified commit, an AskUserQuestion with a > 1 h gap (cold restart), a /clear, a TaskUpdate completed and a <synthetic> zero-usage line.

[SUG-EXPLORE] Moment-specific suggestions for EXPLORING | lever=token_savings | used=partial | conf=high
  DETAIL: (1) At explore run length 6 in main (83 such moments in the month; 48 reach 8): 'Exploring: 6 calls, +X k ctx. Delegate the rest to an Explore agent - a run like this adds p50 49k (p90 103k) to your context; an Explore agent returns ~70-270 tokens.' (2) When one explore result exceeds 3k tokens (111 explore results/month; Read without offset/limit p90 4.0k; cat whole-file 28/67 > 3k): 'That read was N k tokens; use head/sed -n/grep or Read with offset+limit.' (3) When the same basename is read >= 3 times since its last edit (A04): keep, but reset counters on compaction/clear. (4) Cache hit in explore is the lowest of all phases (0.942, p10 0.90) because results are fresh input; no action beyond (1)/(2).
  SRC: phases4_out.json explore_run_length_at_call_when_coach_says_explore, result_tokens_by_tool; phases2_out.json explore_runs_ge8; phases3_out.json bash_explore_bounded_vs_unbounded; phases_out.json cache_hit_by_phase_of_api_call
  EVID: explore_run_length_at_call {6: 83, 7: 66, 8: 48, 10: 31, 15: 8, 20: 3, 24: 1}; explore_runs_ge8 dctx {p50 38811, p90 102941}; explore_agent_result_tokens_p50 71, max 266; results_gt3k_tokens_by_phase explore 111; Read p90 4006; cat_whole_file gt3k 28 of 67
  ACT: Two coach lines keyed on run length >= 6 and single result > 3k tokens; both computable from tools.calls already in State.

[SUG-IMPLEMENT-VERIFY] Moment-specific suggestions for IMPLEMENTING and VERIFYING | lever=less_rework | used=partial | conf=high
  DETAIL: (1) On the first Edit/Write/sed -i of a turn: start a 'no check yet' timer; show 'edited N files; last check: never / 12 min ago'. Turn amber at 10 min or 14 calls (p90 of verified episodes) without a verify - 37 episodes waited > 10 min and 165 of 465 never verified. (2) When a verify command errors (Bash:verify is_error) and the next call within 3 is Bash:commit: 'committing after a failing check' (5 occurrences). (3) When verify fails 3 times on the same command within the last 10 calls (tighten A15 to consecutive + recent): suggest stating the expected output / asking for a smaller step (P02/P13). (4) Verify turns are the cheapest phase per call (1,590 tokens/API call, results p50 75 tokens) - no token nudge needed there; but Bash calls > 60 s (116 in the month) and 38 backgrounded Bash calls suggest 'run in background' only when the command is a build/test and ctx is large.
  SRC: phases_out.json implement_to_first_verify, bash_calls_gt_60s, bg_bash_calls; phases4_out.json implement_to_first_verify_gt10min; phases2_out.json verify_outcome_then_next; phases_out.json context_growth_by_dominant_phase.verify
  EVID: implement_to_first_verify calls_until_verify p90 14; implement_to_first_verify_gt10min 37; never_verified 165/465; verify_outcome_then_next err>commit 5; bash_calls_gt_60s 116; bg_bash_calls 38; verify dctx_per_api_call 1590; Bash:verify result p50 75 tokens
  ACT: Add Turn-level fields first_edit_at, last_verify_at, edits_since_verify to State; the three rules are then predicates over them.

[SUG-COMMIT-BOUNDARY] Moment-specific suggestions for COMMITTING and checkpoint/handoff moments | lever=token_savings | used=partial | conf=medium
  DETAIL: (1) On Bash:commit with no verify in the previous 10 calls (175 of 548 commits): 'commit without a check in this run'. (2) After a turn that contains a commit and ends in text, with no pending background tasks: 'Checkpoint: ctx X k. If the next task is unrelated, /clear now (fresh start is ~50k prefix: ctx after /clear p50 50.7k, p90 61k) or write a hand-off note.' Keep it soft: the next turn after a commit continues on the same files 81% of the time. (3) At ctx > 400k on a 1M model (6 sessions) escalate at every checkpoint: each further API call bills the full ctx; the one observed autocompaction at 567k took 80.7 s, dropped 337k tokens and the session was abandoned right after; two sessions ran to 921k/953k. (4) After TaskUpdate(status=completed) or an AskUserQuestion-ended turn: highest-precision boundary signals (70% and 89% new files next) - surface 'task done?' here rather than after commits. (5) /clear is used 21 times a month but only after the fact; commits happen every ~2 turns (p50), so the checkpoint line will appear often and must be quiet (footer-level), not a toast.
  SRC: phases_out.json commit_calls_without_verify_in_prev10, turns_between_commit_turns; phases3_out.json ctx_after_clear; phases4_out.json boundary_signal_file_novelty, hook_spool; compactMetadata; drops.py output
  EVID: commit_calls_without_verify_in_prev10 {548, 175}; turns_between_commit_turns {p50 2, mean 2.4, p90 5}; ctx_after_clear {n 21, p50 50678, p90 61274}; prev_commit_turn share_fully_new 0.191 vs baseline 0.304; prev_task_completed mean_novelty 0.70; compactMetadata durationMs 80690, preTokens 567672; sessions_max_ctx_gt_400k 6
  ACT: Replace A18's 3 h timer with the composite checkpoint predicate (text ending + commit or task completed + no pending tasks) and grade its urgency by ctx: quiet < 200k, visible 200-400k, insistent > 400k.

[SUG-IDLE-CACHE] Moment-specific suggestions for IDLE/WAITING and model/effort switches | lever=token_savings | used=partial | conf=high
  DETAIL: (1) Countdown 'cache warm for mm:ss' from last API call + TTL (1 h observed; use status prompt_cache.expires_at when present). At T-5 min with a question or permission pending, notify: a cold restart re-caches ~ctx tokens (mean ~190k over 29 cold turns; live recache_tokens_if_cold 171,882). Frequency: 7 prompts/month arrived in the 45-65 min window, 31 after > 60 min. (2) When the user types /model or /effort with ctx > 100k (22 + 3 uses/month; 10/10 observed model switches were cold): 'switching re-reads N k tokens uncached; do it right after a /clear or at a checkpoint'. (3) After idle > 5 min the first call is cold 33% of the time (29/89) - show 'cold restart: +N k written' as a post-hoc explanation for the cost spike so the user learns the pattern (post-lunch 13:00-15:00 and 18:00 are the usual times). (4) AskUserQuestion waits mean 44 min: pair the question with a notification, not just a pane line.
  SRC: phases_out.json cache_write_share_by_gap_since_prev_api_call, first_api_call_write_share, cold_turns_by_hour, idle_p50_by_hour; phases4_out.json prompt_idle_buckets_near_1h_ttl; switch.py; slash command counts; ~/.cctop/status/*.json prompt_cache
  EVID: >60m gap 49/49 cold; 15-60m 1/53 cold; first_api_call_write_share idle_gt_5min {n 89, gt50pct 29, tokens_rewritten_sum 5513436}; prompt_idle_buckets {45-55m 5, 55-65m 2, >2h 29}; model_switch_api_pairs 10 cold 10; /model 22, /effort 3; wait_after_AskUserQuestion mean 2632.8 s; status prompt_cache.recache_tokens_if_cold 171882
  ACT: Add last_api_call_at + ttl to the coach model now (both in State); add a history.jsonl/local_command watcher for /model and /effort to warn before the switch lands.

[SUG-LONGRUN-PLAN-ANSWER] Moment-specific suggestions for long autonomous runs, PLANNING and ANSWER-only turns | lever=token_savings | used=partial | conf=medium
  DETAIL: (1) Long run (> 20 calls, no user input): show 'run: N calls, +X k ctx, Y M tokens billed so far' (p50 32 calls, +60k, 6M; p90 76 calls, +156k, 15M) and suppress interrupt advice (94% end in text; error density equal to short runs); once ctx > 400k, add 'hand off after this run'. (2) PLANNING: on ExitPlanMode (5/month) the next turn adds p50 30k and works on already-read files - this is the moment to set effort/model for implementation (10/10 later switches were cold) and to record the plan file path (plan_mode attachment carries planFilePath) as a hand-off artefact. Plan segments have the longest wall time per segment (p50 21 s, p90 273 s, user reading) so the coach can afford a longer message here. (3) ANSWER-only turns (120): p50 +730 ctx, thinking 36% of output, 1 API call; if > 20 such turns/hour with prompts < 32 chars (A12) suggest batching; gap between human prompts p50 157 s. (4) OPS phase (826 calls, 15% of wall time, results 369 tokens/call, 14 > 3k): nudge '| head/jq' only on an observed > 3k result, since bounded and unbounded command shapes had similar mean sizes (598 vs 545 tokens).
  SRC: phases_out.json long_autonomous_runs_gt20_calls, context_growth_by_dominant_phase (answer, plan, ops), gap_between_prompts_s; phases4_out.json boundary_signal_file_novelty.prev_exit_plan, result_tokens_by_tool; phases3_out.json bash_explore_bounded_vs_unbounded; jq plan_mode attachment keys (isSubAgent, planExists, planFilePath, reminderType)
  EVID: long runs tool_calls p50 32 p90 76, dctx p50 59547 p90 155502, billed p50 5995575 p90 15280026, endings text 75/80; after_exit_plan next_dctx p50 29887, novelty 0.42, fully_new 0.0; plan segment seconds p50 20.8 p90 273; answer dctx_per_turn p50 730, thinking_share 0.358; gap_between_prompts_s p50 157; Bash:ops result p50 122 p90 975 gt3k 14; bounded mean 598 vs unbounded 545
  ACT: Three low-frequency, high-value coach rules: run-cost line (fires ~80x/month), plan-exit setup line (5x/month), ops/explore oversized-result line (per result > 3k).

