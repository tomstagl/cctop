SUMMARY: Corpus: 81 main transcripts under ~/.claude/projects (77 with API calls, 2026-08-12..09-12, CC 2.1.220-2.1.269, 6,291 deduplicated main API calls, 6,255 tool results, 75 subagent transcripts / 1,277 calls), analysed with jq extraction filters + python aggregation in the scratchpad (calls.jq/results.jq/prompts.jq/attach.jq/compact.jq/coststate.jq/bash.jq -> anatomy.py, anatomy2.py, anatomy3.py; outputs anatomy_out.txt, anatomy2_out.txt, anatomy3_out.txt). Priced at cctop's pricing.toml the corpus is $954 of main-thread spend + $152 subagent spend; the cost split is cache reads 66% ($630), cache writes 24% ($229, of which $130 is cold rebuilds), output 10% ($96, thinking $26), fresh input 0%. Every token added to a session is re-read on average 106 times (sum of context over calls 1.55 G tokens vs 14.6 M tokens of final contexts), so the two levers that matter are (a) what is added to context and how early, and (b) whether the cache stays warm. Attribution of the 1.55 G token-calls: tool_use inputs written by the model 22.7% (Bash command text alone 15.7%), fixed prefix 22.3%, tool results 17.7% (Bash 11%), retained thinking blocks 9.8% (thinking is preserved in context on these models, confirmed by residual test), injected system-reminder attachments 9.7%, assistant prose 3.7%, user prompts 1.0%, unattributed 12.9% (tokenizer/char-4 error, loaded tool schemas, images). Cold calls (cache_creation > 50% of context) are 1.8% of calls but 14.6% of spend: 50 after >1h gaps, 36 session starts, 10 model switches, 20 in-session (7 right after ToolSearch, 6 mid-turn listing re-injection); a keep-alive ping costs 5% of the re-cache it prevents (e.g. $0.45 vs $8.72 at 872k). Calls made at >300k context are 26.5% of calls but 53.6% of cost ($0.23-0.50 per call vs $0.07-0.10 below 300k; per turn $2.63-7.32 vs $0.41-0.97), yet only one auto-compaction exists in the corpus (567k -> 230k, 80.7 s, summary 3.7k tokens, retained 40%) because 1M-window models are used. Subagent spend is invisible to the main ledger: 13.7% of total, 0.8% of subagent output returned to main context, and in this very session 41 workflow agents cost $118 vs $10 for the main thread. Re-read waste via the Read tool is negligible here (9 exact re-reads, 3.3k tokens) because auto-mode bash-first steering routes exploration through Bash (read-file/search via Bash = 1.8x the Read tool's tokens, 62% of all tool-result tokens are Bash). cctop today measures the prefix as a single number, ignores attachments, thinking retention, subagent cost, cold-call causes and the context-size cost gradient; each of these has a concrete jq recipe below.

OPEN: Does the compaction summarization request reuse the main cached prefix? claude-strings.txt line 305631 shows a request built with skipSystemPromptPrefix:!0 and its own system text next to the total_tokens_reminder handling; docs claim it reads the warm prefix. Only cost-state (not transcript usage) records it, so it cannot be verified from transcripts. | Why did session e3346d84 auto-compact at 567,672 tokens on opus-5 (1M window, documented threshold ~967k)? Check ~/.claude/settings.json autoCompactWindow, CLAUDE_CODE_AUTO_COMPACT_WINDOW or /autocompact history for that session. | prompt_snapshot.systemPrompt only totals 14-32k chars (3.5-8k tokens) while /context reports System prompt 10.2k tokens; the static pre-__SYSTEM_PROMPT_DYNAMIC_BOUNDARY__ part appears truncated to 1,210 chars in the snapshot. Is the snapshot a partial record? | Can built-in tool schemas (31.2k tokens = 2/3 of the prefix) be removed from the request via permissions.deny / --disallowedTools, and do MCP tools loaded by ToolSearch stay in the tools array for the rest of the session (they appear to, given the re-cache pattern)? | 7 cold new-turn calls in 2.1.263 recordsv-workspace sessions (a9a7f841, 09d44071, cdbcb2fc, 2462cac8, dcf4df9d, 6d5e88e4) had no attachment change and gaps of 20-326 s; possible causes are scheduled_task_fire prompts or a prefix edit (CLAUDE.md) — needs the status-line prompt_cache.last_miss_cause to confirm. | The 12.9% unattributed context (residual) is assumed to be chars/4 tokenizer error plus loaded tool schemas and images; a count_tokens call on a sampled request body would pin the true chars/token ratio for Bash output vs prose. | Thinking retention was inferred from ctx-growth residuals (median 60 vs 193 tokens) and the claude-api skill note on preserved thinking; whether Claude Code ever strips thinking (e.g. at compaction, via clear_thinking) was not observed in this corpus. | Attachment rendering for versions before 2.1.266 (no .rendered field) was extrapolated from 2.1.266+ averages; task_reminder rendering ratio 0.28 was measured on one 2.1.232 session only. | Images were counted at a flat 1,500 tokens; toolUseResult.file.dimensions would allow exact (w*h/750) figures.

[T01] Cost anatomy: 66% of spend is re-reading context; each token is re-read ~106 times | lever=token_savings | used=partial | conf=high
  DETAIL: Deduplicating assistant lines by message.id and pricing with /Users/tom/code/cctop/pricing.toml: corpus main-thread cost $954.50 = cache_read $630.09 (66.0%) + cache_write $228.52 (23.9%) + output $95.82 (10.0%, thinking $25.56 = 2.7%) + fresh input $0.07. Sum of context over all calls = 1,553,323,124 token-calls; sum of each session's final context = 14,586,959 tokens => average re-read multiplier 106.5x. A median opus-5 call: ctx 199,984 tokens, $0.148, of which input side $0.123 (91%) and output side $0.0125. Context tax: 1k tokens added at mid-session costs a further $0.0144 median ($0.0218 p75, $0.1325 max) in cache reads over the rest of the session; a 5k-token tool result at mid-session = $0.072 median. Per-turn median context growth 6,129 tokens (p90 42,434); calls per turn median 5 (p90 22, max 338); turns/session median 6, calls/session median 43 (p90 177).
  SRC: ~/.claude/projects/*/*.jsonl assistant lines .message.usage.{input_tokens,cache_read_input_tokens,cache_creation_input_tokens,cache_creation.ephemeral_5m_input_tokens,ephemeral_1h_input_tokens,output_tokens,output_tokens_details.thinking_tokens}; scratchpad/calls.jq + anatomy.py (CORPUS block), anatomy2.py section (h)
  EVID: 'total main est. cost USD 954.50 ; total context tokens processed 1,553,323,124 ; total output 4,490,992 ; total thinking 1,264,529 ; cache_read 1,525,869,793 ; cache_creation 27,436,248 ; fresh input 17,083'; 'cost split: cache_read $630.09, cache_write $228.52, fresh $0.07, output $95.82'; 'sum of final contexts 14,586,959; sum of ctx over calls 1,553,323,124; average re-read multiplier 106.5x'. Recipe: jq -r 'select(.type=="assistant")|[.message.id,.message.usage.cache_read_input_tokens,.message.usage.cache_creation_input_tokens,.message.usage.input_tokens,.message.usage.output_tokens]|@tsv' F | sort -u -k1,1 | awk '{cr+=$2;cc+=$3;i+=$4;o+=$5;ctx+=$2+$3+$4} END{print cr,cc,i,o,ctx}'
  ACT: Show the marginal cost of adding tokens NOW (remaining_calls_estimate x cache_read_price per 1k) next to every large tool result and in the coach; rank all advice by token-calls saved (tokens x expected remaining calls) rather than raw tokens.

[T02] Fixed prefix: median 47.9k tokens, 2/3 of it is built-in tool schemas; it is 22% of all re-read tokens | lever=token_savings | used=partial | conf=high
  DETAIL: Prefix = first main API call cache_read+cache_creation+input per session: min 16,000, p25 34,907, median 47,934, p75 58,219, max 84,433 (n=77). Sonnet-5 sessions median 59,118 vs opus-5 43,832 (older Aug sessions carried more); 2.1.263 median 32,820, 2.1.269 median 36,276. The only /context capture in 81 transcripts (Sonnet 5, 1M window) breaks it down: System prompt 10.2k (1.0%), System tools 31.2k (3.1%), Skills 4.8k (0.5%, 49 skills), Messages 42.9k, Free 877.9k, Autocompact buffer 33k; 'MCP tools 297 tools 0 tokens (loaded on-demand)'. So the user-trimmable part is skills+CLAUDE.md+memory (<6k here); the 31k of built-in tool schemas cannot be trimmed. prompt_snapshot attachments (record-only, not sent) list the dynamic system-prompt sections: '# Claude in Chrome browser automation' 4,107-4,549 chars (~1.1k tokens) is the largest in 12 of 12 snapshots, '# auto memory' 12,820 chars (~3.2k tokens) in 2 sessions, '# Memory' 2,074, '# Delivering work' 2,017. Prefix accounts for 346.7M token-calls = 22.3% of all context processed ($126 of cache reads); in the median session (>=20 calls) prefix x calls = 40.6% of tokens processed (p25 29.9%, p75 50.2%). 41/77 sessions started with >=50% of the prefix already cached from another session (median warm share 52%).
  SRC: first assistant line usage; system local_command line with '<local-command-stdout> Context Usage' (session 28c68a61, cctop project); attachment.type=prompt_snapshot .attachment.systemPrompt[]; anatomy.py section (1), anatomy2.py (g)
  EVID: 'prefix distribution over 77 sessions: min 16,000 p25 34,907 median 47,934 p75 58,219 max 84,433'; /context stdout lines '⛁ System prompt: 10.2k tokens (1.0%)', '⛁ System tools: 31.2k tokens (3.1%)', '⛁ Skills: 4.8k tokens (0.5%)', '⛁ Messages: 42.9k tokens (4.3%)', 'Autocompact buffer: 33k tokens (3.3%)', 'MCP tools · /mcp (loaded on-demand) └ 297 tools · 0 tokens'; 'prefix 346,692,696 22.3% $126.31'. Recipe: jq -r 'select(.type=="system" and .subtype=="local_command")|.content|select(test("Context Usage"))' F | sed 's/\x1b\[[0-9;]*m//g' | grep -oE '(System prompt|System tools|MCP tools|Custom agents|Memory files|Skills|Messages|Free space|Autocompact buffer): [0-9.]+k? tokens'
  ACT: Parse the /context stdout when the user runs it (and prompt them to run it once at session start) to replace cctop's 'system prompt + other' 27k-42k bucket; tell users the only trimmable prefix parts are skills (49 skills = 4.8k), CLAUDE.md/memory and the Chrome-MCP section (~1.1k/call when claude-in-chrome is connected).

[T03] System-injected attachments cost ~2.3k tokens per turn and 9.7% of all re-read tokens; total_tokens_reminder is 26 tokens on every tool result | lever=token_savings | used=no | conf=high
  DETAIL: attachment lines carry .rendered (the exact system-reminder text sent to the model) from CC 2.1.266 on; types with .rendered are sent, types without it in 2.1.269 are record-only (prompt_snapshot, deferred_tools_record, batching_reminder_sent, command_permissions, hook_system_message, goal_status). Rendered sizes (avg bytes -> tokens): skill_listing 13,344 B (~3.3k tok, re-sent whenever skills change; 122 lines/77 sessions), deferred_tools_delta 12,159 B (~3.0k, the MCP tool-name listing, re-sent when servers connect; 103 lines), edited_text_file 7,422 B (~1.9k, IDE-edited file snippets; 115 lines), mcp_instructions_delta 3,576 B (~0.9k), agent_listing_delta 2,841 B (~0.7k), session_context 1,168 B, hook_blocking_error 911 B (44 lines, Ralph stop hook), queued_command 922 B (background-task notifications ~950-1,245 B), environment 376 B, bash_output_audience_note 203 B (~51 tok, 52 lines), total_tokens_reminder 104 B (~26 tok, 5,754 lines = 0.92 per tool result). task_reminder renders as one '#id. [status] subject' line per task (binary: case"task_reminder"...e.content.map((m)=>`#${m.id}. [${m.status}] ${m.subject}`)), measured 0.28 x json/4 (a 5.6k-json-token reminder adds ~700 tokens; session a6d60f35 had 64 of them). Per session: rendered attachment tokens median 2,264 per turn, 273 per API call; e3346d84 111k tokens (task_reminder 43k, deferred_tools_delta 43k, skill_listing 9k), a6d60f35 98.6k. Attribution: attachments = 151.2M token-calls (9.7%, $65.5): skill_listing 3.0%, edited_text_file 2.1%, deferred_tools_delta 1.5%, total_tokens_reminder 1.1% (17.8M token-calls, $7.5), task_reminder 0.9%. All are appended inside user messages after the cached prefix, so they are cache-written once and cache-read thereafter; they never leave context until compaction (a session with 524 tool results carries 13.6k tokens of 'tokens left' reminders).
  SRC: attachment lines: .attachment.type, .rendered[].content (2.1.266+), .version; scratchpad/attach.jq, anatomy.py section (2), anatomy2.py (a),(e); binary strings claude-strings.txt line 305859
  EVID: 'total_tokens_reminder n=5754 rendered_n=957 avg_rendered=104 B (~26 tok)'; 'skill_listing n=122 avg_rendered=13344 B (~3336 tok)'; 'deferred_tools_delta n=103 avg_rendered=12159 B (~3040 tok)'; 'median per-turn rendered attachment tokens: 2264 ; median per-call: 273'; 'ratio residual/json_tokens median 0.28' for task_reminder; 2.1.269 record-only: 'batching_reminder_sent false 53', 'prompt_snapshot false 30', 'deferred_tools_record false 17', 'hook_system_message false 44'. Recipe: jq -r 'select(.type=="attachment")|[.attachment.type,(if has("rendered") then (.rendered|tojson|length) else "" end)]|@tsv' F | awk -F'\t' '{c[$1]++;b[$1]+=$2} END{for(k in c) print c[k], int(b[k]/4), k}' | sort 
  ACT: Add a 'harness overhead' row (tokens/turn) to the Context panel computed from .rendered lengths; coach rules: 'task list re-injects N tokens every turn — mark tasks done/delete stale ones', 'skills listing re-sent 3x this session (9k) — avoid installing/toggling plugins mid-session', 'IDE edits re-inject file snippets (edited_text_file 1.9k each)'; treat total_tokens_reminder/bash_output_audience_note as fixed harness cost (not user-actionable) when explaining unattributed context.

[T04] Tool-result tokens: Bash delivers 62%, the top 5% of results carry 44%; offloading already keeps 739k tokens out | lever=token_savings | used=partial | conf=high
  DETAIL: 6,255 main tool results, tokens = chars/4 (+1,500 per image): Bash 4,333 calls 1.78M tokens (62.0%, 222 errors), Read 258 calls 440k (15.3%), mcp:claude-in-chrome 533 calls 362k (12.6%, 201 images ~ 300k of it), WebFetch 55k (1.9%), DesignSync 40k, mcp:dynatrace 39k, RemoteTrigger 37k, WebSearch 29k, Edit 14k (0.5%), Agent 9.4k (0.3%). Buckets: <500 tokens 4,928 results 568k (19.8%); 500-2k 1,039 results 1.08M (37.5%); 2k-8k 268 results 991k (34.5%); >8k 20 results 236k (8.2%). Top 5% (312 results >= 1,829 tokens) = 44.3% of tool tokens; top 1% (62) = 17.4%; median 112, p90 1,259, p99 5,088, max 16,477 (a Read). Bash results never exceed 8k (30k-char inline cap): 25 outputs were offloaded (toolUseResult.persistedOutputSize sum 2.95 MB ~ 739k tokens avoided; 13.2k tokens kept as previews). Error results: 283, 58k tokens (2.0%). In context attribution tool results are 275M token-calls = 17.7% ($109): Bash 11.0%, Read 2.9%, chrome 1.8%. Grep/Glob: 0 calls in the whole corpus (auto-mode bashFirst steering).
  SRC: user lines .message.content[] tool_result (content text length, image blocks), .toolUseResult.persistedOutputSize, joined to assistant tool_use .name via tool_use_id; scratchpad/results.jq + anatomy.py section (3)
  EVID: 'Bash 4333 1,781,132 62.0% 222 2,954,180'; 'Read 258 440,113 15.3%'; '<500 4928 568,239 19.8% | 500-2k 1039 1,077,447 37.5% | 2k-8k 268 991,172 34.5% | >8k 20 236,379 8.2%'; 'top 5% of results (n=312, >= 1,829 tokens each) hold 44.3% of all tool-result tokens; top 1% (n=62) hold 17.4%'; 'offloaded Bash outputs: n=25, persisted bytes total 2,954,180 (would have been ~738,545 tokens), kept in context 13,198 tokens'; 'results with images: 188 images: 201'. Recipe: jq -r -f results.jq F (scratchpad) or jq -r 'select(.type=="user")|.message.content[]?|select(.type=="tool_result")|[.tool_use_id,((.content|if type=="string" then length else (map(.text//"")|join("")|length) end)/4|floor)]|@tsv' F
  ACT: Coach on a live 'result >= 1.8k tokens' event with its remaining-call tax (T01), suggesting head/tail/grep filters or Read offset/limit; count image results separately (~1.5k tokens each) and warn on screenshot loops; report persistedOutputSize as 'kept out of context' credit; drop the Grep/Glob-based A05 assumptions when auto-mode bashFirst is on (auto_mode attachment .bashFirst=true).

[T05] Thinking: 28% of output tokens, 50% on the first call of a turn, and it STAYS in context (preserved thinking) = 9.8% of re-read tokens | lever=token_savings | used=partial | conf=high
  DETAIL: Per effort (perTurnEffort else effort): medium 6,051 calls thinking 1.20M / output 4.28M = 28.1% (198 thinking tokens/call), high 97 calls 28.3% (301/call), xhigh 25 calls 29.8% (697/call), none 118 (haiku) 34.2%; 96% of calls run at medium. By call type: first-call-of-turn with tool_use 50.1% thinking (178.6k/356.6k), first-call final-text 35.8%, tool-loop calls 27.1%, tool-loop final text 17.8%. By model: opus-5 24.7%, sonnet-5 36.4%, fable-5-1 33.3%. Thinking output cost $25.56 = 2.7% of spend. Retention test on 5,624 same-turn call pairs: residual of ctx growth vs 'previous output incl. thinking stays' median 60 tokens vs 193 for 'thinking dropped'; on 510 new-turn pairs 17 vs 70, and 'all thinking of the finished turn stripped' leaves +956 median => thinking blocks remain in context for the whole session (claude-api skill prompt-caching.md line 240: 'On ... Opus 4.5 and later, and Sonnet 4.6 and later, previous-turn thinking blocks are preserved by default'). Attributed: thinking(retained) 152.4M token-calls = 9.8% ($64.17), up to 18.3% of context processed in e370b5c1 (157k thinking tokens, 41.9% of its output). So a thinking token costs its output price once plus cache-read price x remaining calls (at 100+ re-reads, opus-5 $0.50/M x 100 = $0.05/k, i.e. more than its $0.025/k output price).
  SRC: assistant lines .message.usage.output_tokens_details.thinking_tokens, .effort, .perTurnEffort, .message.model; anatomy.py section (4), anatomy2.py (b),(a)
  EVID: 'medium 6051 1,201,493 4,281,176 28.1% 198 $927.29'; 'first-call-of-turn+tool_use 527 178,623 356,584 50.1%'; 'tool-loop-call+tool_use 5074 972,693 3,586,738 27.1%'; 'same-turn hypothesis=think median residual 60' vs 'no_think 193'; 'new-turn hypothesis=think_last 17' vs 'strip_turn 956'; 'thinking(retained) 152,362,237 9.8% $64.17'. Recipe: jq -r 'select(.type=="assistant")|[.message.id,(.perTurnEffort//.effort),.message.usage.output_tokens,(.message.usage.output_tokens_details.thinking_tokens//0)]|@tsv' F | sort -u -k1,1 | awk -F'\t' '{o[$2]+=$3;t[$2]+=$4} END{for(k in o) print k, t[k], o[k], t[k]/o[k]}'
  ACT: Fix A08's saving model: thinking saved = tokens x (output price + cache_read price x remaining calls); show effort per turn and suggest /effort low (or per-turn effort) for tool-loop-heavy routine turns where thinking share is >30%; note that /clear or compaction is the only way to drop accumulated thinking from context.

[T06] Cache: 1h TTL is the norm; 116 cold calls (1.8%) cost 14.6% of spend; a keep-alive costs 5% of the rebuild it prevents | lever=token_savings | used=partial | conf=high
  DETAIL: 6,260 of 6,291 calls report ephemeral_1h_input_tokens > 0 (25 calls 5m TTL, 6 none), so the 5-minute TTL rules are moot on this account. Cold call = cache_creation > 50% of context and context >= 10k: 116 calls, $139.21 (14.6% of main cost), extra vs warm (write price - read price) $129.98 (13.6%); cache_creation on cold calls 16.3M tokens (median 75k, max 872k). Causes: session-start 36 (36/77 sessions had a cold first call; median first-call cache_read is 52% of prefix), ttl-expired gap >1h 50 (median gap of non-start cold calls 8,096 s, p75 27,259 s), model-switch 10, in-session unexplained 20 (7 immediately after a ToolSearch call, 6 with skill/agent/mcp listing re-injection mid-turn at ctx 50-75k, 7 new-turn cold calls in 2.1.263 recordsv sessions with no attachment change). Sessions where cold rebuilds are >=30% of cost: 39531def 48.1% (7 cold), 38cfc99e 39.1% (6), e69dbf92 30.0% (5). Keep-alive economics (1h TTL, opus-5 write $10/M vs read $0.50/M): a6d60f35 894k ctx idle 799 min -> re-cache $8.72 vs one keep-alive read $0.45; 12f423aa 864k -> $8.37 vs $0.43; total ttl-expired re-cache $104 vs $87 if pinged once per idle hour (so pinging only pays when the user will resume within ~1 h; otherwise /clear or resume-from-summary). Warm calls: median cache_creation (suffix growth) 892 tokens, p90 3,534; gaps > TTL that stayed warm 4, gaps <= TTL that went cold 30. Corpus hit ratio 0.982.
  SRC: assistant usage cache_creation.ephemeral_1h_input_tokens/ephemeral_5m_input_tokens, timestamps (gap = this call's first line ts - previous call's last line ts), .message.model; anatomy.py section (5), anatomy2.py (c),(i)
  EVID: 'calls with 1h TTL cache writes: 6260 ; 5m TTL: 25 ; neither (no write): 6'; 'cold calls ... 116 of 6291 (1.8%); cost of cold calls $139.21 = 14.6% of main cost; EXTRA cost vs warm $129.98 = 13.6%'; "cold causes: {'session-start': 36, 'ttl-expired(gap>1h)': 50, 'unexplained(prefix-change/no-cache-hit)': 20, 'model-switch': 10}"; "largest cold calls: [('a6d60f35', 'ttl-expired(gap>1h)', 894444, 872098, 47992, 8.28), ...]"; keep-alive table "('a6d60f35', 894187, 799, 0.447, 8.72)". Recipe: jq -r 'select(.type=="assistant")|[.timestamp,.message.id,(.message.usage|.input_tokens+.cache_read_input_tokens+.cache_creation_input_tokens),.message.usage.cache_creation_input_tokens,(.message.usage.cache
  ACT: Coach countdown from last call time + observed TTL (or status-line prompt_cache.expires_at): 'cache expires in mm:ss; a trivial message now costs $X (ctx x read price), letting it lapse costs $Y (ctx x 1h write price) on your next message'; classify each cold call by cause (session-start / ttl / model-switch / ToolSearch / listing change) instead of A01's byte heuristic; suggest /clear or resume-from-summary when the idle gap will exceed 1h.

[T07] ToolSearch (deferred MCP tool loading) and listing changes invalidate the cache: 1.2M tokens re-cached, worst case a full 318k rebuild | lever=token_savings | used=no | conf=high
  DETAIL: Loading a deferred tool changes the request's tools array, which sits before system and messages in the cache prefix, so everything is re-written. 109 ToolSearch calls in 46 sessions; the next call re-cached >2,000 unexplained tokens in 46 cases (total 1,215,842 tokens, extra $7.03), and in 6% of cases re-cached >=50% of the context (e370b5c1 318k tokens $1.21 extra, ee06f37c 152k $1.45, 43ce655e 132k, 36768880 114k, f210d500 111k $1.05). Partial misses overall (warm calls whose cache_creation exceeds new content by >5% of ctx and >=2k): 83 calls, 1.06M tokens, $10.02 (1.1%); 29 tools_changed, 51 in-turn unknown (largest a6d60f35 255k tokens $2.42 in a 816k context, 28c68a61 68k), 3 new-turn unknown. The same mechanism explains 6 in-session cold calls where skill_listing/agent_listing_delta/mcp_instructions_delta/deferred_tools_delta were re-injected mid-turn (a late-connecting MCP server).
  SRC: assistant tool_use name==ToolSearch followed by next call's cache_creation minus (previous output + tool results + reminders); attachment deferred_tools_delta/skill_listing timestamps; anatomy3.py, anatomy2.py (c)
  EVID: 'ToolSearch calls followed by a call: 109; of which re-cached > 2000 unexplained tokens: 46; total unexplained re-cache 1,215,842 tokens, extra cost $7.03'; 'share of ToolSearch calls whose next call re-cached >=50% of context: 6%'; "largest: [('e370b5c1', 358405, 318222, 317631, 1.207, 'sonnet-5'), ('ee06f37c', 184620, 152753, 152063, 1.445, 'opus-5'), ...]"; "causes: {'in-turn(unknown)': 51, 'tools_changed(ToolSearch/deferred_tools_delta)': 29, 'new-turn(unknown...)': 3}"
  ACT: Coach rule: 'ToolSearch at N k context will re-cache the whole prefix (~$X); load every MCP tool you will need in the first turn (one ToolSearch with a comma list), or ask via Bash/CLI instead'; mark the call after a ToolSearch/listing change as an expected rebuild so A01 does not blame the prefix; suggest disconnecting MCP servers that connect late (mcp_instructions_delta mid-turn).

[T08] Subagent spend is invisible: 13.7% of total cost, 0.8% of subagent output returned to the main context, one session 12x its main thread | lever=token_savings | used=partial | conf=high
  DETAIL: 75 subagent transcripts (subagents/agent-*.jsonl and subagents/workflows/wf_*/agent-*.jsonl; legacy isSidechain lines: 0), 1,277 calls, 117.5M context tokens processed, 1.16M output tokens, est. $151.67 = 13.7% of main+sub. Only 9,166 tokens (0.8% of subagent output) came back into main transcripts as Agent/Task/Workflow tool_result text (46 results, median 264 tokens, p90 307, max 995); background agents return their message later as a queued_command 'task-notification' attachment (~950-1,245 B). Per agent: first-call context (agent prefix) median 38,658 tokens, final context median 72,887, max 398k (28c68a61 workflow agents on fable-5-1: 14-16 calls, 86-91k output each); model mix fable-5-1 27, sonnet-5 26, opus-5 7, haiku 1, synthetic 14. This session (28c68a61) shows the extreme: 41 workflow agents, 427 calls, 46M context tokens, $117.61 vs $9.75 for the main thread; 12f423aa: 1 agent $6.36 vs main $148. toolUseResult.totalTokens is present on only 5 Agent results (foreground completions).
  SRC: ~/.claude/projects/<slug>/<session>/subagents/**/agent-*.jsonl assistant usage (dedupe by message.id); main user lines tool_result for Agent/Task/Workflow; attachment queued_command with .rendered containing 'background-task'; anatomy.py section (6)
  EVID: 'corpus: subagent output 1,160,480 tokens, subagent est. cost $151.67 (13.7% of main+sub), returned into main 9,166 tokens = 0.8% of subagent output; subagent context processed 117,545,477'; '28c68a61 41 427 45,965,641 942,757 275,132 117.61 9.75 3 911 0.1%'; 'agent prefix (first call ctx) median 38,658 ; agent final ctx median 72,887 ; agent calls median 10'; 'Agent/Task/Workflow tool_result sizes (tokens): n=46 median 264 p90 307 max 995 ; totalTokens field present on 5'. Recipe: find ~/.claude/projects/*/<session>/subagents -name 'agent-*.jsonl' | xargs jq -r -f calls.jq | sort -u -k4,4 | awk '{o+=$12; ctx+=$7+$8+$9} END{print ctx,o}'
  ACT: Walk subagents/workflows/wf_*/ too (GAP-01), show 'agents: $X (N% of session)' in the header/coach, and per running agent its context size and model; coach: 'agent X is at 398k context on fable-5-1 — its 1M window will not compact; give agents narrower tasks or a cheaper model'.

[T09] 58% of output tokens are tool_use inputs; Bash command text alone is 35% of all output and 15.7% of all re-read context | lever=token_savings | used=no | conf=high
  DETAIL: Splitting each call's non-thinking output by content-block characters: thinking 1.26M (28.2%), text 0.62M (13.9%), tool_use input 2.60M (58.0%). By tool: Bash 1.79M tokens (1.59M of it the command string), Write 288k (274k content), Edit 200k (121k new_string), Agent 51k (48k prompts), chrome computer 37k, AskUserQuestion 35k, SendMessage 31k, TaskUpdate 20k. 'Code written through the model' via Write/Edit/MultiEdit/NotebookEdit payloads = 396k tokens = 8.8% of output; Bash heredoc file writes 184 calls (283k command tokens) in 32 sessions (12f423aa: 55 heredoc writes, 0 Write calls). 194 tool_use payloads >= 1k tokens total 476k tokens (largest Bash 11.2k, 9.6k, 8.4k in 12f423aa; Write 8.7k, 6.4k); 128 Bash commands >= 1k tokens = 336k. Output per call median 392, p90 1,468, p99 5,653, max 20,635; 0 max_tokens stops. Because tool_use inputs stay in context, tool_use_input is the largest re-read category: 353M token-calls (22.7%, $162), Bash 243M (15.7%, $117) > Bash results (11.0%); in 12f423aa tool_use input was 56% of everything re-read.
  SRC: assistant lines: content block types/lengths (text.text, tool_use.input as JSON, input.command/content/new_string/prompt) joined to output_tokens per message.id; anatomy.py section (7), anatomy2.py (a), anatomy3.py
  EVID: 'output split: thinking 1,264,529 (28.2%), text 623,896 (13.9%), tool_use input 2,602,567 (58.0%)'; 'Bash 4333 1,790,022 1,591,993'; 'Write 144 287,752 274,126'; "'code written through the model' ... = 395,605 tokens = 8.8% of all output; Bash command text = 1,591,993 tokens (35.4%)"; 'tool_use_input:Bash 243,147,421 15.7% $116.73'; '12f423aa 6.8 9.4 56.0 ...' (prefix/tool_result/tool_use_input shares); 'top heredoc sessions: [(12f423aa, 55, 0), ...]'. Recipe: jq -r 'select(.type=="assistant")|.message.content[]?|select(.type=="tool_use")|[.name,(.input|tojson|length/4|floor),((.input.command//.input.content//.input.new_string//"")|length/4|floor)]|@tsv' F | awk -F'\t' '{n[$1]++;t[$1]+=$2;p[
  ACT: Add 'tokens the model wrote into tool inputs' to the Tools panel (per tool) and a coach event on any tool_use input >= 1k tokens: 'this 11k-token heredoc is billed as output ($0.28 on opus-5) and re-read ~N more times ($0.55)'; advise writing long files once via Write/Edit and passing data by path instead of inline JSON in commands; in bash-first auto mode, flag heredoc-heavy sessions.

[T10] Re-read waste via the Read tool is negligible in this corpus (0.7% of Read tokens) because exploration runs through Bash | lever=less_rework | used=yes | conf=high
  DETAIL: 257 Read calls (440k tokens). Exact re-reads (same path, same offset/limit or full read, no Edit/Write/MultiEdit/NotebookEdit in between): 9 calls, 3,264 tokens = 0.7% of Read tokens, 0.1% of all tool-result tokens (two of them 1.5k-token image re-reads in 12f423aa/a7f231d9). Different-range re-reads (paging): 20 calls, 39k tokens. Most re-read basenames are tool-result spill files (bnjimkjv9.output etc.). Identical Bash command re-runs: 78 (11.6k result tokens, 0.7% of Bash tokens; 27 build/test, 27 network, 3 git) and only 1 read-file/search command re-run. Bash categories: read-file (cat/sed/head/tail/wc) 496 calls 584k tokens = 32.8% of Bash result tokens, search/list (grep/rg/find/ls/jq/awk) 549 calls 215k (12.0%), git 587 calls 200k (11.2%), network/infra 623 calls 158k, fs-mutate/echo 270 calls 144k, build/test/run 558 calls 111k (6.2%), shell-script 118 calls 78k, heredoc writes 184 calls 31k; Bash-based reading+searching = 798k tokens = 1.8x the Read tool. run_in_background used 38 times.
  SRC: assistant tool_use Read {file_path,offset,limit} and Edit/Write/MultiEdit/NotebookEdit file_path in timestamp order, joined to result sizes; Bash .input.command first token classification (scratchpad/bash.tsv, never printed); anatomy.py section (8), anatomy2.py (d)
  EVID: 'corpus: 257 Reads (439,997 tokens); exact re-reads 9 (3,264 tokens = 0.7% of Read tokens, 0.1% of all tool-result tokens); paged/different-range re-reads 20 (39,020 tokens)'; 'identical Bash command re-runs (same session, exact text): n=78, result tokens of the repeats 11,589 (0.7% of Bash result tokens)'; 're-run of read-file/search commands specifically: n=1, 1,240 tokens'; 'read-file 496 583,655 32.8%'; 'read-file + search/list via Bash: 798,247 result tokens vs Read tool 440,113 tokens => 1.8x'. Recipe: jq -r 'select(.type=="assistant")|.message.content[]?|select(.type=="tool_use" and (.name=="Read" or .name=="Edit" or .name=="Write"))|[.name,.input.file_path,(.input.offset//""),(.input
  ACT: Demote A04 (re-reads) below the context-size and cache rules; extend file tracking to Bash cat/sed/head/tail targets so bash-first sessions get the same duplicate-read signal; the real waste is large one-shot reads (largest 16.5k) not repeats.

[T11] Compaction: one auto-compaction in 81 transcripts; it kept 40% of a 567k context, took 81 s, and its summarization call is not in the transcript usage | lever=less_rework | used=partial | conf=high
  DETAIL: system line subtype compact_boundary (session e3346d84, CC 2.1.263, opus-5): compactMetadata {trigger: auto, preTokens 567,672, postTokens 230,014, cumulativeDroppedTokens 337,658, durationMs 80,690, preCompactDiscoveredTools [ExitPlanMode, Monitor, SendMessage, TaskStop], preservedMessages.uuids 1,376 entries}. The summary is a user line with isCompactSummary=true, 14,954 chars (~3.7k tokens), preceded by 10 attachments (listings re-injected) and followed by an assistant placeholder line with all-zero usage; the summarization request itself only shows up in cost-state (cost-state output 6,435 tokens above transcript main+sub for that session). The session ended 2 turns later, so no post-compaction re-growth could be measured; cctop's ledger shows those turns 28-32 with 0 API calls. No other >=30% context drop between consecutive main calls exists in the corpus (n=0), while 25/77 sessions exceeded 200k, 5 exceeded 500k and 2 exceeded 800k (a6d60f35 953k, 12f423aa 921k) without compacting — 1M-window models autocompact only near 967k. Auto-compaction at 567k on a 1M model implies a lowered window (/autocompact or env) in that session — unverified. Binary snippet near the compaction path shows a request built with `skipSystemPromptPrefix:!0` and its own system text, i.e. the summary request may not share the main cached prefix (open question).
  SRC: system lines subtype compact_boundary .compactMetadata; user lines .isCompactSummary; cost-state modelUsage vs transcript sums; scratchpad/compact.jq, coststate.jq; anatomy.py section (9); claude-strings.txt line 305631
  EVID: 'marker: e3346d84 compact_boundary auto pre 567672 post 230014 dropped 337658 durationMs 80690 ... preserved_msgs 1376'; 'summary ... summary_chars 14954'; 'context drops >=30% between consecutive main calls ...: n=0'; 'max context per session: >200k: 25, >500k: 5, >800k: 2 sessions of 77; median max ctx 135,332'; cost-state diff row 'e3346d84 6435 7227244 90.93 87.01'. Recipe: jq -c 'select(.type=="system" and .subtype=="compact_boundary")|.compactMetadata|{trigger,preTokens,postTokens,cumulativeDroppedTokens,durationMs,preserved:(.preservedMessages.uuids|length)}' F; jq -r 'select(.isCompactSummary==true)|.message.content|length' F
  ACT: Read compact_boundary.compactMetadata for exact pre/post/duration instead of the 30%-drop heuristic; show 'compaction will keep ~40% and cost ~80 s + a summary call not visible in the transcript'; because 1M models rarely compact, make the coach recommend manual /compact <focus> or /clear at task boundaries once context passes ~300k (see T12).

[T12] Context size is the cost multiplier: calls above 300k context are 26.5% of calls but 53.6% of spend; a turn at >600k costs 18x a turn at <100k | lever=token_savings | used=partial | conf=high
  DETAIL: Per call by context at call time: <100k 1,284 calls $93.86 ($0.073/call), 100-300k 3,339 calls $348.90 ($0.104), 300-600k 1,211 calls $282.71 ($0.233), >600k 457 calls $229.04 ($0.501). Per turn (turn = non-meta user prompt): <100k $0.409/turn (5.9 calls), 100-300k $0.973 (9.7 calls), 300-600k $2.632 (12.9 calls), >600k $7.319 (16.7 calls, 18.1k output). Cache-read cost of the context portion above 300k across all calls = $176.26 (18.5% of corpus) — the upper bound that a 'compact/clear at 300k' discipline would save in reads alone, before counting the write side. Top session a6d60f35 (opus-5, 58 turns, 529 calls, 75k -> 897k context, $166.83): $91.0 of it spent on the 154 calls made above 600k. Cost per turn overall: median $0.489, p90 $3.47, max $127.25 (a 338-call turn).
  SRC: assistant usage per call (ctx = input+cache_read+cache_creation) with pricing.toml; turns from non-meta user lines without tool_result; anatomy3.py, anatomy2.py (h)
  EVID: '<100k calls 1284 cost $93.86 (9.8% of cost, 20.4% of calls) avg $0.073/call'; '>600k calls 457 cost $229.04 (24.0% of cost, 7.3% of calls) avg $0.501/call'; 'ctx_bucket turns cost/turn$ calls/turn output/turn: <100k 153 0.409 5.9 3211 | 100-300k 355 0.973 9.7 6815 | 300-600k 102 2.632 12.9 8746 | >600k 38 7.319 16.7 18113'; 'cache-read cost of the context portion above 300k tokens, over all calls: $176.26 (18.5% of corpus cost)'; "a6d60f35 ... {'<100k': (19, 1.91), '100-300k': (159, 25.69), '300-600k': (197, 48.24), '>600k': (154, 91.0)}". Recipe: cctop query ledger --session F | jq '.[]|[.turn,.api_calls.value,.cache_read.value,.cost.value]' then bucket by cache_read/api_calls
  ACT: Show '$ per turn at current context' and 'this turn would cost $X at 100k' in the coach; fire a boundary rule when context > 300k AND the last turn ended cleanly (final text, no pending tasks, git clean): 'run /compact <focus> or /clear now: next turns cost ~$2.6 each vs ~$0.4 after'.

[T13] Model mix: opus-5 is 85% of spend; model switches inside a session are full cache rebuilds; cost-state reveals hidden auxiliary usage | lever=token_savings | used=partial | conf=high
  DETAIL: Main calls by model: claude-opus-5 3,800 calls / 45 sessions, cache_read 1.085G, cache_creation 18.7M, output 3.13M, $808.31 (84.7%); claude-sonnet-5 2,315 / 30 sessions $125.28 (13.1%); claude-fable-5-1 58 / 2 sessions $18.08 (1.9%); claude-haiku-4-5 118 / 9 sessions $2.83 (0.3%). 9 sessions used >1 model in the main thread (e370b5c1 sonnet 273 / opus 192; 1919f7e3 opus 58 / haiku 54; 83f0e9b9 sonnet 55 / fable 33); 10 cold calls were caused by model switches (e370b5c1 487k tokens re-cached, $4.63 extra). usage.speed is 'standard' on 6,290/6,291 calls (fast mode never used); effort medium on 96% of calls. cost-state.modelUsage (Claude Code's own totals) exceeds transcript main+sub sums: e370b5c1 +85k output tokens ($121.10 vs $102.44 estimated), 12f423aa +19.8k ($163.54 vs $147.97), c77fd198 +30k; haiku-4-5 appears in cost-state of 9 sessions with no transcript lines (titles/classifiers, <=$0.19), so transcript-only estimates undercount sessions by ~5-15%.
  SRC: assistant .message.model, .message.usage.speed, .effort; cost-state lines .modelUsage[*].{inputTokens,cacheReadInputTokens,cacheCreationInputTokens,outputTokens,costUSD}; scratchpad/coststate.jq; anatomy.py sections (9),(10)
  EVID: 'claude-opus-5 3800 45 7,614 1,085,110,486 18,746,709 3,131,914 773,599 808.31 84.7%'; 'sessions using >1 model in main: 9'; "speed/fast-mode calls: {'standard': 6290, '': 1}"; "e370b5c1 85259 26474582 121.1 102.44 {'claude-haiku-4-5-20251001': (2950, 43, 0.0), 'claude-opus-5': (134513566, 161292, 84.84)}"; cold cause 'model-switch': 10. Recipe: jq -c 'select(.type=="cost-state")|.modelUsage|to_entries|map({m:.key,o:.value.outputTokens,cr:.value.cacheReadInputTokens,c:.value.costUSD})' F | tail -1
  ACT: Treat a /model switch as an expected full rebuild and warn before it ('switching now re-caches 487k tokens ≈ $4.60'); prefer cost-state.modelUsage as the authoritative session total and label the transcript estimate as lower bound; show effort/speed in the header (both are already in the transcript).

[T14] Per-turn context composition for the biggest sessions: prefix 7-35%, tool inputs 16-56%, results 9-29%, thinking 7-18%, attachments 5-19%, unattributed 4-17% | lever=token_savings | used=no | conf=medium
  DETAIL: Attribution of every call's context by when each piece entered (token-calls, per session): a6d60f35 prefix 15.6% / tool_result 14.2% / tool_use_input 20.8% / assistant_text 5.9% / attachment 17.0% / thinking 10.8% / prompt 1.3% / residual 14.3% (255M token-calls); 12f423aa 6.8/9.4/56.0/1.5/4.6/6.6/0.1/14.9 (231M); e370b5c1 12.8/22.2/21.0/4.6/4.7/18.3/1.2/15.1 (210M); e3346d84 14.5/16.0/28.8/3.8/19.1/9.6/4.6/3.6; a9896da4 34.9/21.1/16.4/3.5/11.0/7.2/1.3/4.6. Corpus grouped: tool_use_input 22.7%, prefix 22.3%, tool_result 17.7%, residual 12.9%, thinking 9.8%, attachment 9.7%, assistant_text 3.7%, user_prompt 1.0%. The residual is consistent with chars/4 under-counting code/JSON (Opus 4.7+ tokenizer ~1-1.35x), images, and ToolSearch-loaded schemas; it is not user prompt text (1.0%).
  SRC: anatomy2.py section (a): events (prefix, tool results+26, rendered attachments, prompts, assistant text/tool_use chars-proportional, thinking) accumulated per call and priced at cache_read
  EVID: 'grouped: tool_use_input 353,270,612 22.7% $162.21 | prefix 346,692,696 22.3% $126.31 | tool_result 275,457,304 17.7% $109.21 | residual(unattributed) 199,940,053 12.9% $81.64 | thinking(retained) 152,362,237 9.8% $64.17 | attachment 151,189,170 9.7% $65.53 | assistant_text 58,132,506 3.7% $24.85 | user_prompt 16,278,545 1.0% $7.22'
  ACT: Replace the single 'messages' number in the Context panel with a stacked breakdown (prefix / tool inputs / tool results / thinking / harness reminders / prose) computed from the transcript with these rules; the coach can then name the biggest movable slice for this session.

[T15] Images are ~1.5k tokens each and invisible to cctop's tokens_to_ctx (Read of PNG shows 0) | lever=token_savings | used=no | conf=medium
  DETAIL: 201 image blocks in 188 main tool results (mcp:claude-in-chrome screenshots plus Read of .png files: toolUseResult.file.type image, base64 380-462 KB each). At ~1.5k tokens per image that is ~300k context tokens (about a third of the chrome server's 362k estimate). cctop query tools on 12f423aa reports Read: 10 calls, tokens_to_ctx 0 because the result content is an image block, not text.
  SRC: user tool_result content[].type==image, toolUseResult.file.{type,base64,dimensions,originalSize}; cctop query tools --session 12f423aa...; results.jq column n_images
  EVID: 'results with images: 188 images: 201'; cctop tools output '{"tool":"Read","calls":..."value":10 ... "tok":0}'; jq sample '{"ctype":"array","clen":445932,"turkeys":["file","type"],"filekeys":["base64","dimensions","originalSize","type"]}'
  ACT: Count image blocks as ~(w*h)/750 tokens (dimensions are in toolUseResult.file.dimensions) in tokens_to_ctx; coach: 'screenshot loop: 12 screenshots this turn ≈ 18k tokens — use get_page_text/find/read_page instead of computer screenshots'.

[T16] cctop query cross-check: prefix 'other' bucket 27k-42k, ledger turns after compaction show 0 calls, tokens_to_ctx blind to images/offloading | lever=none | used=yes | conf=high
  DETAIL: cctop query summary on e3346d84 returns turns 32, api_calls 407, context.size 566,520, prefix 47,932, compactions 0 (the compact_boundary is not detected because no later API call exists), cost $90.93 (from cost-state), tokens_to_ctx 76,346 (this analysis: 76,039 chars/4 — agreement), thinking 66,066. query prefix rows: 'system prompt + other' 41,967 of 47,932 (e3346d84) and 27,140 of 34,905 (12f423aa) unattributed; mcp rows are name-listing sizes (Resend 1,017 tokens/103 tools). query ledger --last 6 shows turns 28-32 with 0 API calls (post-compaction user lines). cctop's turn count (non-meta user lines without tool_result) matches this analysis (771 turns corpus-wide). cctop currently reads none of: attachment .rendered sizes, thinking retention, subagents under subagents/workflows/, compactMetadata, cold-call cause, per-tool tool_use input size, cost-state modelUsage for other models.
  SRC: target/release/cctop query summary|prefix|tools|ledger --session <path> on e3346d84 and 12f423aa transcripts and fixtures/session-a.jsonl; docs/metrics.md
  EVID: summary: '"context":{"compactions":..."value":0},"prefix":..."value":47932},"size":..."value":566520}'; prefix: '{"kind":"other","name":"system prompt + other","tokens":41967}'; ledger: '{"turn":28,"api_calls":0,...}' through turn 32; tools on 12f423aa: Read tok 0
  ACT: Use the recipes in T02/T03/T05/T06/T08/T11 to fill the gaps: /context parse for prefix rows, rendered attachment bytes, compactMetadata, subagent workflow dirs, image token estimate, tool_use input tokens per tool.

[T17] Sessions start cold half the time and the prefix is shared across sessions within the 1h TTL | lever=token_savings | used=no | conf=high
  DETAIL: 36 of 77 sessions had a cold first call (cache_creation ~ prefix); 41 sessions read >=50% of the prefix from cache on the first call (median warm share 52%) because the system prompt + tool block is identical across sessions of the same version/model and the 1h cache is account-wide. First-call cache_read median by version: 2.1.263 26,668, 2.1.269 30,857 (of prefixes 32.8k/36.3k). Session-start cold writes total ~1.66M tokens (36 x ~46k) ≈ $9 of the $130 cold-rebuild extra; the expensive cold calls are the 50 ttl-expired resumes (14.6M tokens).
  SRC: first assistant line usage per session; anatomy2.py (g); anatomy.py (5)
  EVID: 'sessions whose first call already read >=50% of the prefix from cache (warm from another session within the 1h TTL): 41 of 77; median warm share 52%'; "'session-start': 36"; 'non-session-start cold calls: 80, cache_creation 14,597,430 tokens, extra cost $120.80'
  ACT: Do not alarm on a cold first call (expected, ~$0.45 on opus-5); focus the coach on resume-after-idle: when a session is resumed after >1h, tell the user the first message will re-cache the whole context (ctx x 1h write price) and offer /clear or resume-from-summary if the task is done.

[T18] Error results and interrupted/backgrounded tools are small token sinks (2% of tool tokens) | lever=less_rework | used=yes | conf=high
  DETAIL: 283 error tool results (is_error=true), 58,246 tokens = 2.0% of tool-result tokens; Bash 222 errors of 4,333 calls (5.1%), chrome 14, Edit 7, mcp:github 5, mcp:clawctl 5, Agent 3. run_in_background=true on 38 Bash calls; identical command re-runs are dominated by build/test (27) and network (27) commands, i.e. legitimate retries, not loops.
  SRC: user tool_result .is_error, toolUseResult.{interrupted,timedOutAfterMs,backgroundTaskId}; anatomy.py (3), anatomy2.py (d)
  EVID: 'error results: n=283, tokens 58,246 (2.0% of tool tokens)'; 'Bash 4333 ... errors 222'; 'run_in_background=true: 38 calls'; 're-runs by category: build/test/run 27, network/infra 27, other:cd 10, git 3'
  ACT: Keep A15 but scope it to a rolling window; error tokens are not a cost lever in this corpus.

