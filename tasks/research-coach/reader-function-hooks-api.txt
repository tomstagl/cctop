[E01] Engine event catalogue: 31 subscribable engine events (EngineEventOf) | lever=none | used=partial | conf=high
  DETAIL: A hooks module registers with `on(pattern, hook)` or `on(pattern, matcher, hook)` (type `On`, L3977); patterns are an event name, a glob (`tool.*`, `classic.*`, `*`) or a negation (`!tool.describe`). Every hook is `($, e, next)`; `e` is frozen plain data, `next(e)` runs the rest of the chain and core. A failing hook (throw, budget overrun, wrong shape) is skipped, so a coach plugin can never block the session by accident. The chain order is tiers `prepend, user, append, builtin, core` (TIERS L7021).
  SRC: /Users/tom/code/cctop/plugin/.claude/types/claude-code.d.ts L2417-2760 (EngineEventOf), L3977-3987 (On), L4421 (Pattern)
  ACT: Register one `*` observer hook to log `next.event` names and payload keys in a real session to confirm which of the 31 fire in this build before designing the coach.

[E02] turn.start / turn.complete carry per-turn usage, duration, end reason and refusal | lever=token_savings, less_rework | used=partial | conf=high
  DETAIL: `turn.start` gives `{ text, turnId }`. `turn.complete` (fires "at the point its duration is reported") gives `answer`, `durationMs`, `isAborted`, `turnId`, `agentId?` (subagent turns fire too, so a hook sees every subagent's turn), `usage?: TurnUsage` (input_tokens, output_tokens, cache_read_input_tokens, cache_creation_input_tokens, model — "its responses' token counts summed and the model of the last"), and `reason: 'answer'|'aborted'|'refusal'|'error'`; on a refusal `refusal: { category, explanation }`. A hook may return `{ text }` to show a line beneath the answer ("a synopsis, a TL;DR line") without touching the transcript.
  SRC: claude-code.d.ts L7534-7626 (TurnCompleteFields, TurnCompleteReason, TurnRefusal), L7628-7655 (TurnStartInput/Result), L7850-7862 (TurnUsage)
  ACT: Use `turn.complete` as the coach's tick: compute cost/turn, cache-hit ratio and aborted/error/refusal counts live, and optionally return `{ text }` to print a one-line coach note beneath the answer at zero token cost. The pane PRD (US-003) only reads durationMs and reason; add usage and refusal.

[E03] turn.step streams every model request with per-request usage, stopReason, and a rewritable effort/model | lever=token_savings, better_outcomes | used=partial | conf=high
  DETAIL: `turn.step` is the one streaming event (async generator hook). Its input is `{ turnId, index, model, effort?, messageCount }`; only `model` and `effort` are rewritable going down ("`next({ ...e, model })` or `effort` sends another"). Its result (and the final `stop` chunk) carries `answer`, `toolUses[{name,input}]`, `stopReason` and `usage: TurnUsage|null`. Chunks are `text`, `thinking`, `tool`, `input`, `stop`, `engine`. `TurnStopReason` includes `'compaction'` and `'model_context_window_exceeded'`, which are the only live signals that a request hit the window or triggered a server-side compaction.
  SRC: claude-code.d.ts L7683-7715 (TurnStepInput), L7733-7765 (TurnStepResult), L7767-7778 (TurnStepStopChunk), L7842 (TurnStopReason), L7657-7840 (chunks)
  ACT: Read `usage` off each `stop` chunk to toast a cache miss the moment it happens ("this request re-wrote 142k tokens"), count thinking chunks vs text chunks per step for the A08 thinking share, and (opt-in) rewrite `effort` to `low` on steps whose previous step was pure tool_use. The pane PRD uses turn.step only for tool starts.

[E04] tool.call envelope and result: wrapping next() times the call including permission wait; PostToolUse.duration_ms excludes it | lever=token_savings, less_rework | used=partial | conf=high
  DETAIL: `tool.call` fires for every tool the model or a plugin runs, with `{ tool, tool_use_id, agentId?, ...args }` typed per built-in (`e.tool === 'Bash'` narrows to `{ command, timeout?, run_in_background?, description? }`). `next(e)` resolves to `{ ref, result, text }` or `{ ref, result, text, isError: true }`; `text` is "the result as the model reads it", so `text.length/4` is the tokens the result put into context. Because `next(e)` spans "the permission check and its dialog, then the tool", wall time around `next` includes the permission prompt, while `classic.PostToolUse.duration_ms` is documented as "Excludes permission-prompt and hook time"; the difference isolates the permission wait per call.
  SRC: claude-code.d.ts L2424-2432 (tool.call), L7110-7185 (ToolCallResult), L7065-7108 (ToolCallEnvelope/Reserved), L4725-4735 (PostToolUseHookInput.duration_ms)
  ACT: Record `t0` before `next(e)` and pair the tool_use_id with `classic.PostToolUse.duration_ms`; permission_wait = wall − duration_ms − hook time. This gives A09 (permission waits) exactly, where the transcript can only infer from timestamp gaps. The pane PRD wraps next only for duration and isError.

[E05] tool.check exposes the permission verdict, the deciding rule and whether a dialog will open | lever=less_rework, better_outcomes | used=no | conf=high
  DETAIL: `tool.check` fires "when the engine decides whether a tool call may run, after the tool.call and PreToolUse hooks and before the mode settles an ask". Input `{ tool, input, tool_use_id? }`; `next(e)` resolves to `{ decision: 'allow'|'ask'|'deny', reason?, rule? }` where `rule` is "The settings rule that decided, as written (`Bash(git push:*)`)". `$.tool.check({tool,input})` runs the same chain as a dry run ("nothing runs, no dialog opens"). A hook may override the decision.
  SRC: claude-code.d.ts L2433-2444 (tool.check), L7202-7251 (ToolCheckInput/Result), L1901-1913 ($.tool.check)
  ACT: Count `decision === 'ask'` per (tool, command prefix) live; after N asks for the same prefix, toast "3 prompts for `npm test` — add `Bash(npm test:*)` to permissions.allow" with the exact rule string. The pane PRD lists tool.check in its data-source table but no story uses it.

[E06] agent.spawn / agent.offer / $.agent.list expose subagent type, model resolution, background flag and live status | lever=token_savings | used=partial | conf=high
  DETAIL: `agent.spawn` fires before a subagent's model is resolved with `{ tool_use_id, prompt, description, subagentType, provider, model?, parentModel, parentAgentId?, permissionMode?, background, fork, name?, cwd? }`; a hook may rewrite `model` (an alias like `haiku`) or `deny`. `$.agent.list()` returns `AgentInfo[] { id, description, type, status ('running'|'completed'|'failed'|'killed'|...), parentId?, spawnedBy?, name? }`. `agent.offer` reveals which agent types are offered to the model and from which source.
  SRC: claude-code.d.ts L176-266 (AgentSpawnInput), L268-301 (AgentSpawnResult), L63-108 (AgentInfo), L129-160 (AgentOfferInput)
  ACT: When `subagentType` is `Explore`/search-like and `model` is undefined with `parentModel` an Opus id, toast the A14 advice at spawn time (before the cost is incurred), and optionally, with the user's stored consent, rewrite `model: 'haiku'`. The pane PRD only counts spawns and lists agents.

[E07] prompt.submit exposes every prompt's text, attachments, queued state (turnId/wait) and 16 origin kinds | lever=token_savings, less_rework | used=no | conf=high
  DETAIL: `PromptSubmitInput` is `{ text, attachments?: [{type:'image'|'audio'|'document', mediaType?, filename?}], context?, turnId?, wait, origin }`. `turnId` is set only for a prompt "typed over that turn, or delivered into it" — a queued prompt; `wait` is true when the user used ctrl+x enter. `origin.kind` distinguishes `composer`, `bridge`, `sdk`, `task-notification`, `scheduled-trigger`, `peer`, `peer-send-message`, `projects-relay`, `channel`, `coordinator`, `observer`, `observer-activity`, `auto-continuation`, `unclassified`, `slack-ping`, `plugin`. A hook may attach `context` (model-only text, ≤ 32000 chars) or `{ drop: reason }` ("The text is shown to the user as the reason").
  SRC: claude-code.d.ts L5154-5205 (PromptSubmitInput), L5207-5245 (PromptSubmitResult), L5010-5119 (PromptOrigin)
  ACT: Measure prompt length at submit (A11 paste detection before the tokens are spent: text.length > 20000 → toast "large paste; point Claude at the file instead"), count queued prompts per turn (Turn panel's 'queued prompts' exactly), and separate human turns from machine-injected ones (`origin.kind !== 'composer'`) so A12 (tiny prompts) does not count task notifications.

[E08] prompt.suggest / prompt.fill: the engine's own next-prompt suggestion pipeline is hookable and usable by a plugin | lever=better_outcomes, token_savings | used=no | conf=high
  DETAIL: `prompt.suggest` fires for "the engine's guess after a turn (the prompt-suggestion service)" (`origin.kind: 'suggestion'`) or a plugin's `$.prompt.suggest`; the text is shown dim in the empty prompt box, Tab to take. `$.prompt.fill` writes a draft into the box. Both resolve `{ isShown|isFilled: false }` while the box holds text, a turn runs, or headless.
  SRC: claude-code.d.ts L5253-5314 (PromptSuggestInput/Origin/Result), L4951-5008 (PromptFill*), L1858-1890 ($.prompt.fill/suggest)
  ACT: After `turn.complete`, when the coach has a concrete action (`/compact focus on the failing test`, `/model sonnet`), call `$.prompt.suggest({ text })` so the user accepts it with one Tab — zero model cost and no interruption. Also observe the engine's own suggestions (origin 'suggestion') to learn what it proposes.

[E09] prompt.section / prompt.context / tool.describe / skill.prompt expose the exact fixed prefix by named part | lever=token_savings | used=no | conf=high
  DETAIL: `prompt.section` fires "once per named section of the system prompt" with `{ name (e.g. 'env_info_simple', 'memory'), text|null }`; `prompt.context` once per conversation with `blocks: [{ name: 'claudeMd'|'userEmail'|'attachedProject'|'currentDate'|..., text }]`; `tool.describe` once per tool with `{ tool, description, provider }` (provider `mcp:<server>` for MCP tools); `skill.prompt` with `{ skill, text }` on every expansion. Summing `text.length/4` across these gives the fixed-prefix token cost per section, per CLAUDE.md, per tool/MCP server and per skill invocation. Answers are cached for the session; "an unstable answer spends the model's prompt cache on every call".
  SRC: claude-code.d.ts L2559-2598 (prompt.section/context/tool.describe), L5121-5142 (PromptSectionInput), L4902-4939 (PromptContextBlock(s)), L7253-7283 (ToolDescribeInput), L6682-6702 (SkillPromptInput)
  ACT: Observe (pass-through) these events to compute A17 (prefix share of window) and A07 (per-MCP-server schema cost) exactly instead of estimating; list the top prefix contributors in the coach with their real sizes. Never return an unstable value from these hooks.

[E10] session.compact exposes trigger (incl. precompute), instructions, the full message list, tokensBefore/tokensAfter and skip | lever=token_savings, less_rework | used=partial | conf=high
  DETAIL: `SessionCompactInput` = `{ trigger: 'manual'|'auto'|'plugin'|'precompute', agentId?, instructions?, messages: SessionMessage[] }`; result `{ messages, tokensBefore?, tokensAfter? }` or `{ skip: reason }`. `precompute` is the engine computing a compaction ahead of time ("its result is kept for the compaction that comes"). A hook may rewrite `instructions` (what the summarizer is told), veto with `{ skip }`, or read before/after sizes. `$.session.compact({ instructions })` triggers one between turns and "rejects while a turn runs".
  SRC: claude-code.d.ts L6176-6282 (SessionCompactArgs/Compacted/Input/Result/Skipped/Trigger), L2676-2687 (session.compact event), L1795-1810 ($.session.compact)
  ACT: Log compaction count, trigger and the real tokens saved (before−after) for A06; show "auto-compaction pre-computed" as an early warning when `trigger === 'precompute'` fires; offer a one-key `$.session.compact({ instructions })` with a coach-written focus line at a natural boundary. The pane PRD only counts compactions.

[E11] classic.SessionStart carries resume/fork cache economics: seconds_since_last_response, prompt_cache_likely_expired, estimated_cache_write_usd | lever=token_savings | used=no | conf=high
  DETAIL: The classic SessionStart payload adds `source: 'startup'|'resume'|'clear'|'compact'|'fork'`, `model?`, `session_title?`, and for resume/fork `seconds_since_last_response`, `context_tokens` (last response's input+cache_read+cache_creation+output), `prompt_cache_likely_expired` ("exceeds the prompt-cache TTL, so the first request re-caches context_tokens") and `estimated_cache_write_usd`. The engine-native `session.start` only has `{ cwd, surface, isInteractive }`.
  SRC: claude-code.d.ts L6545-6571 (SessionStartHookInput), L6573-6603 (SessionStartInput/Result)
  ACT: On `classic.SessionStart` with `prompt_cache_likely_expired`, toast "resumed after 47 min: first request re-writes 180k tokens (≈$0.68); consider starting fresh with a hand-off note" (A02/A18 at the exact moment it matters). Also read `source` to count `/clear` and `compact` restarts.

[E12] classic.PreModelSwitch / PostModelSwitch quantify the cache forfeited by a model switch and can deny it | lever=token_savings | used=no | conf=high
  DETAIL: Both carry `from_model`, `to_model`, `requested_model`, `source` (`command|picker|sdk`, Post adds `auto|resume`), `context_tokens`, `prompt_cache_warm` ("a switch then forfeits it"), `cache_ttl: '5m'|'1h'`, `estimated_cache_write_usd`, `pricing: 'configured'|'catalog'|'default'`. PreModelSwitch's result reads `permissionDecision: 'allow'|'deny'|'ask'` + `permissionDecisionReason`; PostModelSwitch reads `additionalContext`.
  SRC: claude-code.d.ts L4743-4783 (PreModelSwitchHookInput), L4659-4698 (PostModelSwitchHookInput), L830-854 (ClassicResultFields)
  ACT: Toast the exact re-cache cost when the user switches model mid-session; the `cache_ttl` field is the authoritative TTL for cctop's A02 (currently inferred from API usage fields).

[E13] classic.PermissionRequest / PermissionDenied / Notification expose the permission dialog and its suggested allow-rules, which no transcript line records | lever=less_rework, better_outcomes | used=no | conf=high
  DETAIL: `PermissionRequest` = `{ tool_name, tool_input, permission_suggestions?: PermissionUpdate[] }` where PermissionUpdate is `addRules|replaceRules|removeRules { rules: [{toolName, ruleContent?}], behavior, destination }`, `setMode { mode }`, `addDirectories|removeDirectories`; its result `decision: { behavior: 'allow', updatedInput?, updatedPermissions? } | { behavior: 'deny', message?, interrupt? }`. `PermissionDenied` = `{ tool_name, tool_input, tool_use_id, reason }` (result may `retry`). `Notification` = `{ message, title?, notification_type }`. `PermissionMode` is `'default'|'acceptEdits'|'bypassPermissions'|'plan'|'dontAsk'|'auto'`.
  SRC: claude-code.d.ts L4461-4466 (PermissionRequestHookInput), L4451-4459 (PermissionRequestDecision), L4468-4506 (PermissionUpdate*), L4434-4443 (PermissionDeniedHookInput), L3912-3921 (NotificationHookInput), L4445 (PermissionMode)
  ACT: Count PermissionRequest per tool/prefix and keep `permission_suggestions` so the coach can print the exact rule and destination the dialog would add ("add `Bash(cargo test:*)` to projectSettings"); count PermissionDenied per tool as rework. Time from PermissionRequest to the matching PostToolUse is the permission wait.

[E14] classic.PreCompact / PostCompact expose custom instructions and the produced summary text | lever=token_savings | used=partial | conf=high
  DETAIL: `PreCompact` = `{ trigger: 'manual'|'auto', custom_instructions: string|null }` (a block here vetoes; core then answers session.compact `{ skip }`); `PostCompact` = `{ trigger, compact_summary: string }` "The conversation summary produced by compaction". The summary's length is the tokens the compaction spent on output and the size of what survives.
  SRC: claude-code.d.ts L4737-4741 (PreCompactHookInput), L4650-4657 (PostCompactHookInput)
  ACT: Record summary length per compaction and whether instructions were given; nudge "give /compact a focus line" when `custom_instructions` is null on a manual compaction (better summaries = less rework).

[E15] classic.Stop / SubagentStop / StopFailure expose background tasks, session crons and API error kinds at turn end | lever=less_rework, better_outcomes | used=no | conf=high
  DETAIL: `Stop` = `{ stop_hook_active, last_assistant_message?, background_tasks?: BackgroundTaskSummary[] { id, type ('shell'|'subagent'|'monitor'|'workflow'), status, description, command?, agent_type?, server?, tool?, name? }, session_crons?: [{ id, schedule, recurring, prompt }] }`. `SubagentStop` adds `agent_id`, `agent_transcript_path`, `agent_type`. `StopFailure` = `{ error: SDKAssistantMessageError, error_details?, last_assistant_message? }` with error in `'authentication_failed'|'oauth_org_not_allowed'|'account_on_hold'|'verification_required'|'billing_error'|'rate_limit'|'overloaded'|'invalid_request'|'model_not_found'|'server_error'|'unknown'|'max_output_tokens'|'cloud_credential_error'`.
  SRC: claude-code.d.ts L6771-6789 (StopHookInput), L6859-6878 (SubagentStopHookInput), L6764-6769 (StopFailureHookInput), L403-434 (BackgroundTaskSummary), L6312-6330 (SessionCronSummary), L6065 (SDKAssistantMessageError)
  ACT: Show 'waiting on N background tasks' in the Turn/coach line from Stop.background_tasks; on StopFailure `rate_limit`/`overloaded`/`max_output_tokens` show the cause and the concrete next step (wait for reset, retry, split the task) — the transcript only shows a generic api error line.

[E16] classic.PostToolUse / PostToolUseFailure / PostToolBatch: per-tool duration without permission time, error text, interrupt flag, batch size | lever=less_rework | used=partial | conf=high
  DETAIL: `PostToolUse` = `{ tool_name, tool_input, tool_response, tool_use_id, duration_ms? }`; `PostToolUseFailure` = `{ tool_name, tool_input, tool_use_id, error, is_interrupt?, duration_ms? }`; `PostToolBatch` = `{ tool_calls: [{ tool_name, tool_input, tool_use_id, tool_response? }] }` "fires exactly once with the full batch". Batch length measures how much the model parallelises tool calls.
  SRC: claude-code.d.ts L4700-4735 (PostToolBatch*, PostToolUseFailure, PostToolUse)
  ACT: Use PostToolUseFailure.error + tool_input to detect the A15 same-command-same-error loop live (third identical failure → toast "same error 3×: interrupt and give the fix"), and PostToolBatch length to report parallelism.

[E17] classic.UserPromptSubmit.source and UserPromptExpansion expose who authored a prompt and the expanded size of every slash command / MCP prompt | lever=token_savings | used=no | conf=high
  DETAIL: `UserPromptSubmit` = `{ prompt, source?: 'user'|'sdk'|'system'|'loop_wakeup'|'schedule_wakeup'|'poll_event', session_title? }`. `UserPromptExpansion` = `{ expansion_type: 'slash_command'|'mcp_prompt', command_name, command_args, command_source?, prompt }` where `prompt` is the expanded text the model receives, i.e. the token cost of invoking that skill/command.
  SRC: claude-code.d.ts L8090-8102 (UserPromptSubmitHookInput), L8081-8088 (UserPromptExpansionHookInput)
  ACT: Attribute tokens to skills: `prompt.length/4` per `command_name` gives a 'cost per skill invocation' row; exclude non-`user` sources from human-behaviour rules (A12).

[E18] classic.Elicitation / ElicitationResult expose MCP server input requests and the user's response | lever=less_rework | used=no | conf=high
  DETAIL: `Elicitation` = `{ mcp_server_name, message, mode?: 'form'|'url', url?, elicitation_id?, requested_schema? }` ("Hooks can auto-respond (accept/decline) instead of showing the dialog"); `ElicitationResult` = `{ mcp_server_name, elicitation_id?, mode?, action: 'accept'|'decline'|'cancel', content? }`.
  SRC: claude-code.d.ts L2360-2388
  ACT: Count elicitations per MCP server and the wait time between the two events as another 'waiting on user' bucket in the Turn panel.

[E19] classic.InstructionsLoaded reveals every CLAUDE.md load and its reason, including nested loads mid-session that change the prefix | lever=token_savings | used=no | conf=medium
  DETAIL: `InstructionsLoaded` = `{ file_path, memory_type: 'User'|'Project'|'Local'|'Managed', load_reason: 'session_start'|'nested_traversal'|'path_glob_match'|'include'|'compact', globs?, trigger_file_path?, parent_file_path? }`. Related lifecycle events: `ConfigChange { source: user_settings|project_settings|local_settings|policy_settings|skills, file_path? }`, `FileChanged { file_path, event }`, `CwdChanged`, `DirectoryAdded`, `Setup { trigger: init|maintenance }`, `WorktreeCreate/Remove`, `TaskCreated/TaskCompleted { task_id, task_subject, teammate_name? }`, `TeammateIdle`, `MessageDisplay { turn_id, message_id, index, final, delta }`.
  SRC: claude-code.d.ts L3317-3329 (InstructionsLoadedHookInput), L1280-1284 (ConfigChangeHookInput), L3015-3020 (FileChangedHookInput), L3626-3651 (MessageDisplayHookInput), L6931-6966 (Task*/TeammateIdle)
  ACT: A `nested_traversal` or `path_glob_match` load mid-session means new instruction text entered context (and if it lands in the prefix, a cache rewrite) — surface it as the cause of a sudden cache_creation spike (A01 root-cause) and list the loaded files with sizes via `$.fs.stat`.

[E20] BaseHookInput on every classic event carries permission_mode, effort.level, prompt_id, agent_id/agent_type | lever=token_savings, better_outcomes | used=no | conf=high
  DETAIL: All `classic.*` payloads extend `BaseHookInput { session_id, transcript_path, cwd, prompt_id?, permission_mode?, agent_id?, agent_type?, effort?: { level } }`. `permission_mode` is the only live plan-mode signal (value `'plan'`); `effort.level` is "Active effort level for the current turn ... after any silent downgrade for the selected model" and is present on tool-use-context hooks. There is no dedicated plan-mode, todo or effort-change event in the file (0 hits for Todo/TodoWrite/ExitPlanMode/EnterPlanMode).
  SRC: claude-code.d.ts L436-469 (BaseHookInput); grep counts: Todo 0, TodoWrite 0, ExitPlanMode 0, EnterPlanMode 0, 'plan' 20 (all PermissionMode/agent.offer/planId)
  ACT: Read `effort.level` from `classic.PostToolUse` to show the real effort per turn (cctop currently infers effort from transcript `perTurnEffort`/`effort` keys) and `permission_mode === 'plan'` to suppress edit-related advice while planning; use `prompt_id` to join hook events to transcript entries.

[E21] ui.render components beyond Pane: Spinner phase, TurnDuration, PromptHint, SessionMode, AbovePrompt band, ToolGroup/ToolUse rows are all hookable and rewritable | lever=better_outcomes, less_rework | used=partial | conf=high
  DETAIL: `RenderComponent` = AskUserQuestion, UserMessage, AssistantMessage, ToolUse, ToolResult, ToolGroup, Spinner, TurnDuration, InfoNotice, SessionMode, PromptHint, AbovePrompt, Pane. Props of interest: `Spinner { word, message, mode: 'requesting'|'responding'|'thinking'|'tool-input'|'tool-use' }` (live phase of the turn), `TurnDuration { word, durationMs }`, `PromptHint { isDraft, isWorking, hint }` (rewrite `hint` = replace the dim line under the prompt), `SessionMode { modes: string[] }` (add a mode label to the footer), `AbovePrompt { hasSurvey, isWorking, maxRows, scroll }` (a band where a hook draws Buttons with digit hotkeys pressed from an empty composer), `ToolUse { tool, input, isRunning, isErrored, isInterrupted, output }`, `ToolGroup { calls[], isActive, isExpanded }`. The permission dialog is not renderable ("drawn by the engine alone"); `$.ui.notice` adds a line to it.
  SRC: claude-code.d.ts L5352 (RenderComponent), L5674-5990 (RenderPropsOf), L606-651 (ButtonProps.hotkey), L5912-5948 (AbovePrompt)
  ACT: The minimal coach view can be an `AbovePrompt` tree: one line of advice plus `1: do it  2: snooze` plain Buttons with hotkeys — no pane, works below 110 columns, and the digit is pressed from an empty composer. Spinner.mode gives the live turn phase for the Turn panel without the transcript. The pane PRD only hooks `{ component: 'Pane' }`.

[E22] Op events: every `$` call by any plugin is itself a hookable event with {value}|{deny} results | lever=none | used=partial | conf=high
  DETAIL: `OpEventOf` lists 46 op events (`model.complete`, `model.classify`, `model.fork`, `audio.play`, `audio.speak`, `mcp.call`, `session.cwd/model/turns/id/messages/repo/surface/surfaces/authorize/usage`, `turn.abort`, `tool.list/register`, `command.list/register`, `config.list`, `agent.list`, `ui.toast/status/log/notice/invalidate/open/close`, `fs.read/write/list/exists/stat/ancestors`, `store.get/set/delete/keys`, `http.fetch`, `process.run`, `settings.read`, `env.get/set`). `ui.close` carries `origin.kind: 'plugin'|'person'|'unload'`. A hook above the caller can deny or answer any of them; `next.origin` names the caller.
  SRC: claude-code.d.ts L4006-4252 (OpEventOf), L4260-4320 (OpValueOf), L4362-4391 (PaneCloseInput/Origin)
  ACT: Hook `model.complete`/`model.fork` to count and cost other plugins' side completions (they are API spend not in the transcript); hook `ui.close` with origin `person` to persist that the user closed the coach.

[E23] next.trace exposes per-link hook timing; on classic.* events the engine entry's ms is the settings hooks' own time | lever=less_rework | used=no | conf=medium
  DETAIL: After `next()` settles, `next.trace` is `TraceEntry[] { index, plugin, tier, event, outcome, reason?, ms, chunks?, received, returned }`; `ms` is "Its own wall time, its `next()` calls' time in flight taken out. The engine entry's is everything beneath the last hook". The classic chain is documented as "[managed settings hooks, ...hooks modules, the other settings hooks as core]", so on `classic.PostToolUse`/`classic.Stop` the core entry's `ms` is the time the user's settings command hooks took.
  SRC: claude-code.d.ts L7476-7532 (TraceEntry/TraceOutcome), L3798-3866 (Next.trace), L714-726 (ClassicEventOf chain comment)
  ACT: After `await next(e)` on `classic.PostToolUse` and `classic.Stop`, read `next.trace.at(-1).ms` to measure settings-hook overhead per event precisely (A16 exact, vs cctop's inference from transcript timestamps). Verify empirically that the round trip is small enough not to swamp the figure.

[E24] command.run / config.set / config.describe / attribution.text observe every slash command and /config change | lever=token_savings | used=no | conf=high
  DETAIL: `command.run` fires with `{ command, args, origin: PromptOrigin }` for `/compact`, `/clear`, `/model`, `/cost`, plugin commands etc.; a hook may answer `{ text }` without running. `config.set` fires with `{ key ('theme','verbose','autoCompact','<plugin>.<field>'), value, previous, provider, origin }` and may deny. `$.config.list()` returns rows with `kind`, `value`, `options`, `isLocked`. `attribution.text` exposes the commit/PR trailer text.
  SRC: claude-code.d.ts L1195-1248 (CommandRunInput/Result), L1408-1454 (ConfigSetInput/Result), L1361-1400 (ConfigRow), L2600-2630 (config.* events)
  ACT: Count `/compact`, `/clear`, `/model` invocations live; detect `autoCompact` being turned off via config.set and warn about the context-window error path; use `$.config.list()` to read the current `autoCompact` value for the 'turns until autocompact' metric.

[E25] session.receive / session.attach / session.detach expose inbound deliveries and remote surfaces | lever=none | used=no | conf=high
  DETAIL: `session.receive` fires before a delivery is queued with `{ origin.kind: 'bridge'|'task-notification'|'scheduled-trigger'|'peer'|'peer-send-message'|'projects-relay'|'slack-ping'|'unclassified', text, event?: { source, kind, data, untrustedKeys } }`; a hook may rewrite or `{ consumed }`. `session.attach/detach` give `{ surface, clientId, viewport? }` / `{ reason: 'detach'|'end' }`.
  SRC: claude-code.d.ts L6421-6514 (SessionReceive*), L6136-6165 (SessionAttach*), L6332-6359 (SessionDetach*)
  ACT: Count machine-injected deliveries per session to explain turns that were not typed (a plausible cause of 'many tiny prompts').

[A01] $.session.usage(): the status line's own context, rate-limit and cost figures, polled (no push event) | lever=token_savings | used=yes | conf=high
  DETAIL: Returns `SessionUsage { context: { tokens?, window, percent? }, rateLimits: [{ kind: 'five_hour'|'seven_day'|'spend_limit'|..., percentUsed (0-100+), resetsAt? ISO }], cost?: { usd } }`. `context.tokens` = "uncached, cache-written and cache-read together (the status line's `total_input_tokens`)" — input side only, no output; absent until the first response after a compaction. Nothing fires when a threshold is crossed; a plugin must poll from a timer (PRD: `$.clock.every(1000)` while a turn runs).
  SRC: claude-code.d.ts L1778-1794 ($.session.usage), L6284-6303 (SessionContextUsage), L6305-6310 (SessionCost), L6400-6419 (SessionRateLimit), L6605-6628 (SessionUsage)
  ACT: Already planned (US-003). Note the definition differs from `PostModelSwitch.context_tokens` (which adds output tokens) and from cctop's transcript-derived context size; document which one the coach quotes.

[A02] $.session.messages(): the live post-compaction conversation with per-tool-result text, so context composition by tool is measurable without the JSONL | lever=token_savings | used=no | conf=high
  DETAIL: Returns `SessionMessage[] { role, text, toolUses: [{ tool_use_id, tool, input, result?, text?, isError? }], toolResults?: [{ tool_use_id, text, isError, result? }], handle? }` — "one entry per user or assistant message; ... A long transcript answers its newest 4096". Because it reflects what the engine holds now (after compaction), `sum(text.length/4)` per tool over `toolResults` is the tokens each tool currently occupies in context; the JSONL transcript keeps pre-compaction entries and needs cctop's own reconstruction.
  SRC: claude-code.d.ts L1721-1735 ($.session.messages), L6369-6398 (SessionMessage), L7388-7413 (ToolResultSummary), L7436-7474 (ToolUseSummary)
  ACT: Poll it after `turn.complete` (not inside hooks) to rank 'what is filling the context' by tool and by file path (`input.file_path`) — A03/A04/A05 with exact live numbers and the same-file re-read count after compaction. Beware the 4096-message cap and cost of serialising a large conversation across the plugin port.

[A03] $.session getters: id, cwd, model, turns, repo, surfaces, authorize | lever=none | used=yes | conf=high
  DETAIL: `id()` (transcript file name), `cwd()`, `model()` ("as `/model` shows it"), `turns()` ("how many prompts the user has sent this session"), `repo()` → `{ root, remote, internal, name }|null`, `surfaces()` → `('terminal'|'desktop'|'mobile')[]`, `authorize()` → opaque credential handle for `$.http.fetch(url, { auth })` to a first-party host only.
  SRC: claude-code.d.ts L1736-1777, L1811-1823, L6167-6174 (SessionAuthorization), L6516-6543 (SessionRepo)
  ACT: Planned in the pane PRD. `surfaces()` tells whether a desktop/mobile client is attached so the coach can prefer `Svg`-free trees.

[A04] $.model.fork/complete/classify: side completions, fork sharing the session's prompt cache with usage returned | lever=better_outcomes | used=no | conf=high
  DETAIL: `fork({ prompt })` runs "one tool-less completion over the session's OWN transcript, sharing the main thread's prompt cache" and returns `{ text, usage: { input_tokens, output_tokens, cache_read_input_tokens, cache_creation_input_tokens } }` or null "on a cold snapshot or an API error". `complete({ model, prompt, system?, maxTokens=256 })` is a plain completion; `classify(text, labels, { model })` picks a label with the small fast model by default. All three are real API spend not recorded in the transcript's usage.
  SRC: claude-code.d.ts L1620-1680 ($.model), L3653-3715 (ModelCompleteRequest, ModelForkRequest/Result/Usage)
  ACT: An opt-in 'ask the session what to do next' coach action: `$.model.fork({ prompt: 'In one line, what is the cheapest next step?' })` costs roughly one cache read of the context plus ≤256 output tokens; show the returned `usage` so the cost is visible. Not for the default coach loop (every call is spend).

[A05] $.ui.toast / status / log / notice / ask / invalidate / open / close: display channels with zero model cost | lever=token_savings, better_outcomes, less_rework | used=partial | conf=high
  DETAIL: `toast(text, { timeoutMs = 4000 })` = the notification bar "the way the engine's own 'context left' notice appears"; `status(text|undefined)` = a pinned line under the prompt, "One per plugin"; `log(text)` = a dim transcript row "not sent to the model" (SDK hosts get it as `ui_log`); `notice(tool_use_id, text)` = a line under the open dialog of that call, removed when it resolves; `ask(question, options)` = the engine's AskUserQuestion dialog resolving to the chosen label (rejects when dismissed or in `-p`); `invalidate('ui.render')` ≤ 10/s; `open({ id, title, focus? })` waits undrawn below 144 columns (110 once asked); `close({ id })`.
  SRC: claude-code.d.ts L1479-1618 (CoreEngineInterface.ui), L7042-7053 (ToastOptions), L319-340 (AskOptions), L4393-4419 (PaneOpenArgs)
  ACT: The minimal coach: `$.ui.status` for the one persistent line ("ctx 62% · cache 91% · $1.20 · 5h 40%"), `$.ui.toast` for event-driven nudges (cache miss, 3rd identical failure, permission repeat), `$.ui.notice(tool_use_id, ...)` to annotate a permission dialog with "asked 4× this session — allow-rule: Bash(npm test:*)". The pane PRD lists these only as diagnostics.

[A06] $.prompt.submit / fill / suggest: submitting costs a turn; fill and suggest are free | lever=better_outcomes, token_savings | used=no | conf=high
  DETAIL: `submit({ text })` runs "when the session is idle" as a user turn under the plugin's name (full model turn cost). `fill({ text })` writes the draft into the prompt box (`{ isFilled }`), `suggest({ text })` shows a dim Tab suggestion (`{ isShown }`); both are refused while a dialog holds the keys, the box holds text, or a turn runs.
  SRC: claude-code.d.ts L1841-1890 ($.prompt), L4945-5008 (PromptFill*), L5247-5314 (PromptSuggest*)
  ACT: Coach actions should use `fill` (puts `/compact <focus>` in the box for the user to press Enter) or `suggest` (Tab), never `submit`, so no token is spent without the user's keypress.

[A07] $.tool.list / check / register / call: tool inventory, dry-run permission check, own tool declaration | lever=token_savings, less_rework | used=no | conf=high
  DETAIL: `list()` → `ToolInfo[] { name, description, mcp }` "in the order the model sees them" (count of `mcp: true` = MCP schema load). `check({ tool, input })` → `{ decision, reason?, rule? }` without running anything. `register({ name, description, inputSchema? })` adds `mcp__<plugin>__<name>` to the model's tools from the next prompt (adds to the prefix). `call({ tool, ...args })` runs a tool through hooks, the permission dialog and the tool (`ToolCallResult`).
  SRC: claude-code.d.ts L1893-1933 ($.tool), L7338-7358 (ToolInfo), L7415-7434 (ToolSpec)
  ACT: `$.tool.list()` at session.start gives the MCP tool count per server (name prefix `mcp__<server>__`) for the A07 overhead estimate; do not `$.tool.register` a coach tool (it would grow the prefix on every request).

[A08] $.command.register/run/list and $.config.list/set: native commands without a model turn and programmatic slash commands | lever=better_outcomes | used=yes | conf=high
  DETAIL: `command.register({ name, description, argumentHint?, immediate? })` declares `/<name>` served by a `command.run` hook returning `{ text }` — no model turn. `command.run({ command, args })` runs any slash command "queued and run once the session is idle". `config.list()` rows carry `key`, `kind`, `value`, `options`, `isLocked`; `config.set({ key, value })` changes a row through the other plugins' hooks.
  SRC: claude-code.d.ts L1935-1975 ($.command), L1978-2010 ($.config), L1255-1278 (CommandSpec), L1361-1400 (ConfigRow)
  ACT: Planned for `/cctop` (FR-2). A `/coach` native command can print the current top advice as `{ text }` instantly; a coach Button can call `$.command.run({ command: 'compact', args: focus })` after explicit consent.

[A09] $.settings.read({ source? }): the merged (or per-source) settings.json — permissions.allow/deny, hooks, model, env, enabledPlugins | lever=less_rework | used=no | conf=high
  DETAIL: Returns a plain snapshot "keyed as a settings.json is"; sources `user|project|local|flag|policy` rise in precedence. "Every key crosses as the source holds it, `env` and the helper commands included: nothing is filtered." The OAuth session and ~/.claude.json are never read.
  SRC: claude-code.d.ts L2213-2235 ($.settings.read), L6630-6652 (Settings, SettingsReadArgs, SettingsSource)
  ACT: Read `permissions.allow` to avoid suggesting rules that already exist, and `hooks` to name which configured command hook is slow (A16) and where it lives (which source), so the advice says 'PostToolUse hook in .claude/settings.json'.

[A10] $.fs.* (4 MiB cap) and $.fs.ancestors: read the CLAUDE.md stack exactly as the engine loads it | lever=token_savings | used=partial | conf=high
  DETAIL: `read/write/list/exists/stat` (relative to the session cwd or absolute; "A read or a write over 4 MiB rejects"). `ancestors({ names, of? })` returns `[{ dir, name, content }]` "the way the engine reads CLAUDE.md ... the content with its `@include`s after it". `stat` gives `{ kind, size, mtimeMs }`.
  SRC: claude-code.d.ts L2036-2100 ($.fs), L3037-3106 (FsAncestor, FsAncestorsRequest, FsEntry, FsStat)
  ACT: Use `ancestors({ names: ['CLAUDE.md'] })` for the exact CLAUDE.md token share (A17) with per-file sizes; do not read the JSONL transcript through `$.fs.read` (long sessions exceed 4 MiB) — keep `cctop query` via `$.process.run` for that.

[A11] $.store: per-plugin JSON persisted across sessions (4 MiB) — the place for baselines, snoozes and acknowledged advice | lever=better_outcomes | used=yes | conf=high
  DETAIL: `get/set/delete/keys`; values are JSON ("a Date is its ISO string"); "A JSON file of the plugin's own under the user's Claude Code configuration directory"; rejects a store over 4 MiB.
  SRC: claude-code.d.ts L2101-2137 ($.store)
  ACT: Store per-project baselines (median cost/turn, typical cache hit) so the coach can say 'this session is 3× your usual cost per turn', and remember snoozed rule ids so advice is not repeated.

[A12] $.clock / $.process.run / $.http.fetch / $.env / $.audio / $.mcp.call: host access from the sandbox | lever=none | used=yes | conf=high
  DETAIL: `clock.now/sleep/after/every` (timers in the plugin environment); `process.run(argv, { cwd, env, stdin, timeoutMs })` → `{ exitCode, stdout, stderr }` ("timeout 30 s by default, ten minutes at most", whole output read, no shell, "Git runs with repo hooks off"); `http.fetch(url, init)` through the host; `env.get/set` with literal names; `audio.play/speak` (macOS `afplay`/`say`); `mcp.call(server, tool, args)` with the engine's connection and no permission prompt.
  SRC: claude-code.d.ts L2139-2248 ($.clock, $.http, $.process, $.env), L1684-1728 ($.audio, $.mcp), L4839-4881 (ProcessRunInit/Result)
  ACT: Planned (poller via `$.process.run(['cctop','query',...])`). `$.audio.speak` is an optional audible nudge for 'permission prompt waiting' when the user has looked away.

[A13] $.turn.abort({ turnId }) lets a plugin cancel the running turn | lever=less_rework, token_savings | used=no | conf=high
  DETAIL: "Cancels the running model turn: the one whose id `turn.start` handed this plugin, its running tools stopped, no interruption marker." Raises `turn.abort`; the next prompt the plugin submits is the context. Rejects when `turnId` is not the running turn's.
  SRC: claude-code.d.ts L1826-1840 ($.turn.abort), L4116-4118 (OpEventOf 'turn.abort')
  ACT: Only behind an explicit Button press: 'Stop this loop' when A15 (same failure 3×) fires mid-turn — the coach shows the button, the user presses it. Never automatic (FR-15 read-only stance).

[S01] Show a suggestion without a model turn: seven zero-token channels, ranked by intrusiveness | lever=better_outcomes, less_rework, token_savings | used=partial | conf=high
  DETAIL: (1) `$.ui.status(text)` — persistent one-liner under the prompt (one per plugin); (2) `$.ui.toast(text, { timeoutMs })` — transient bar; (3) `ui.render` rewrite of `PromptHint.hint` — replaces the dim hint line while idle/working; (4) `ui.render` `SessionMode.modes` — a footer label such as `coach: cache cold`; (5) `ui.render` `AbovePrompt` — a band with `Text` + plain `Button`s with digit hotkeys pressed from an empty composer (a survey-like row `1: compact  2: ignore`); (6) `$.ui.notice(tool_use_id, text)` — a line inside an open permission dialog; (7) `$.ui.log` — a dim transcript line; plus `$.prompt.suggest` (Tab) and `$.ui.open` Pane (planned). None of these reach the model ("not sent to the model", "leaves the transcript and the model untouched").
  SRC: claude-code.d.ts L1479-1618 ($.ui), L5876-5948 (SessionMode, PromptHint, AbovePrompt props), L606-651 (ButtonProps)
  ACT: Design the coach as status line (always) + AbovePrompt band (when there is an action with a hotkey) + toast (events); reserve the Pane for the full dashboard. Each is a separate `on('ui.render', { component })` registration.

[S02] Inject guidance into the model: three grades with different token costs and cache effects | lever=better_outcomes, token_savings | used=no | conf=high
  DETAIL: Grade 1, per-event notes: classic `additionalContext: string[]` on UserPromptSubmit, UserPromptExpansion, SessionStart, Setup, PostModelSwitch, SubagentStart, PostToolUse, PostToolUseFailure, PostToolBatch, Stop, SubagentStop (`ClassicResultFields`), plus `PreToolUseResult.additionalContext`. Grade 2, structured context: `PromptSubmitInput.context` (attach on the way down) and `ToolCallResult.context` — each "capped at 32000 characters", "never shown the user", "one block after the prompt as typed"; these become part of the conversation, billed as fresh input on that request and re-read (cache-read) on every later request until compaction. Grade 3, prefix rewrites: `prompt.section`, `prompt.context`, `tool.describe`, `skill.prompt`, `attribution.text` — cached, but any change invalidates the prefix cache ("an unstable answer spends the model's prompt cache on every call"). Also `initialUserMessage`/`sessionTitle` (SessionStart) and `$.prompt.submit` (a whole turn). There is no `hookAdditionalContext` identifier in this file (0 hits); the names are `additionalContext` and `context`.
  SRC: claude-code.d.ts L746-854 (ClassicResult, ClassicResultFields), L4824-4837 (PreToolUseResult), L5154-5245 (PromptSubmitInput/Result.context), L7110-7185 (ToolCallResult.context), L2559-2598 (prompt.section/context caching), grep hookAdditionalContext=0, additionalContext=14
  ACT: If the coach ever injects (the pane PRD §6 forbids it), use Grade 1 on `classic.Stop` or `classic.UserPromptSubmit` only, keep it under ~200 chars, and never touch Grade 3 mid-session. Quote the cost to the user: N chars/4 tokens fresh now + re-read every request until compaction.

[S03] classic.PostToolUse.updatedToolOutput can replace what the model sees of a tool result — the one lever that removes tokens rather than adding them | lever=token_savings | used=no | conf=high
  DETAIL: `ClassicResultFields.PostToolUse` = `'additionalContext' | 'updatedToolOutput' | 'updatedMCPToolOutput'`; `updatedToolOutput?: unknown` "`hookSpecificOutput.updatedToolOutput` (PostToolUse): replaces what the model sees of the tool's result." Combined with `ToolCallResult.text` (the model-facing text) a plugin could cap a 20k-token `ls -R` or `cat` result before it enters context. Likewise `tool.call` may answer `{ result }` itself or `{ deny }`, and `classic.PreToolUse` may return `updatedInput` (e.g. add `offset/limit` to a Read, `run_in_background: true` to a long Bash).
  SRC: claude-code.d.ts L797-806 (ClassicResult.updatedToolOutput/updatedMCPToolOutput), L846 (ClassicResultFields.PostToolUse), L4824-4837 (PreToolUseResult.updatedInput)
  ACT: Out of scope for a read-only dashboard (FR-15) but the highest-leverage enforcement path for A03/A04/A10: an opt-in 'guard' mode could truncate results above N tokens with a note, or rewrite `Bash.run_in_background` for commands matching a slow-list. Surface it as a future option, gated by an explicit /config row.

[S04] Observable only through hooks (not in the JSONL transcript): the definitive list | lever=token_savings, better_outcomes, less_rework | used=no | conf=high
  DETAIL: (a) permission prompts: `classic.PermissionRequest` (+ `permission_suggestions`), `classic.PermissionDenied.reason`, `tool.check {decision, rule}`, permission wait = tool.call wall − `PostToolUse.duration_ms`; (b) `classic.Notification { notification_type }`; (c) `classic.Elicitation/ElicitationResult`; (d) compaction: `session.compact.trigger` incl. `precompute`, `tokensBefore/After`, `{ skip }`, `PreCompact.custom_instructions`; (e) turn end: `turn.complete.reason` (`answer|aborted|refusal|error`), `refusal.category`, `isAborted`, `StopFailure.error` (rate_limit, overloaded, max_output_tokens, billing_error ...); (f) per-request `stopReason` (`compaction`, `model_context_window_exceeded`, `max_tokens`) and `effort` on `turn.step`; (g) queued prompts: `PromptSubmitInput.turnId`/`wait`, `PromptOrigin.kind`, `UserPromptSubmit.source`; (h) cache economics on model switch and resume (`prompt_cache_warm`, `cache_ttl`, `estimated_cache_write_usd`, `prompt_cache_likely_expired`); (i) the exact fixed prefix by section/tool/CLAUDE.md (`prompt.section`, `prompt.context`, `tool.describe`), skill expansion size (`UserPromptExpansion.prompt`), nested CLAUDE.md loads (`InstructionsLoaded.load_reason`); (j) `BaseHookInput.effort.level` and `permission_mode` per event; (k) hook timing via `next.trace[].ms`; (l) live turn phase `Spinner.mode`; (m) `Stop.background_tasks` / `session_crons`; (n) other plugins' `model.*` spend via op events. Items already visible in the transcript via `toolUseResult` (Agent usage/toolStats, Bash persistedOutputPath/timedOutAfterMs, Read truncatedByTokenCap) are not in this list.
  SRC: claude-code.d.ts: L4461 PermissionRequest, L4434 PermissionDenied, L7230 ToolCheckResult, L3912 Notification, L2360 Elicitation, L6215 SessionCompactInput, L7534 TurnCompleteFields, L6764 StopFailure, L7842 TurnStopReason, L5154 PromptSubmitInput, L4743 PreModelSwitch, L6545 SessionStartHookInput, L5121 PromptSectionInput, L3317 InstructionsLoaded, L436 BaseHookInput, L7476 TraceEntry, L5836 Spinner, L6771 StopHookInput
  ACT: Prioritise for the coach, in order of user value per implementation cost: permission repeats (E05/E13), cache-miss-as-it-happens (E03), resume/switch cache cost (E11/E12), same-failure loop (E16), waiting-on-background/rate-limit cause (E15), prefix breakdown (E09).

[S05] Live per-request cache economics from turn.step usage make A01/A02 real-time instead of after-the-fact | lever=token_savings | used=partial | conf=high
  DETAIL: Each `stop` chunk / `TurnStepResult.usage` gives `cache_creation_input_tokens` vs `cache_read_input_tokens` for that single request. A request where `cache_creation_input_tokens` ≈ the whole context (and `cache_read_input_tokens` ≈ 0) is a cold rewrite; the toast can name the likely cause from other hook signals seen since the previous request: a `classic.InstructionsLoaded` (nested CLAUDE.md), a `config.set`, a `PostModelSwitch`, a `session.compact`, a `tool.register`, or elapsed time > `cache_ttl` from PreModelSwitch/PostModelSwitch.
  SRC: claude-code.d.ts L7767-7778 (TurnStepStopChunk), L7733-7765 (TurnStepResult.usage), L7850-7862 (TurnUsage), L3706-3715 (ModelForkUsage fields)
  ACT: Emit one toast per cold write above a threshold (e.g. > 50k cache_creation tokens): "cache rewritten (142k tokens ≈ $0.53) — cause: 42 min idle > 5 m TTL". cctop's A01/A02 rules run on transcript usage after the turn; this moves them to the moment of the request.

[S06] Effort can be observed per turn and (with consent) rewritten per request | lever=token_savings | used=partial | conf=high
  DETAIL: Observation: `BaseHookInput.effort.level` on classic tool-context events ("after any silent downgrade for the selected model", also `CLAUDE_EFFORT` env) and `TurnStepInput.effort` on every request. Enforcement: `turn.step` "A hook rewrites `model` or `effort` going down". The Agent result exposes `usage.output_tokens_details.thinking_tokens` per subagent.
  SRC: claude-code.d.ts L436-469 (BaseHookInput.effort), L7683-7715 (TurnStepInput.effort), L8615-8618 (BuiltinToolResults.Agent.usage.output_tokens_details.thinking_tokens)
  ACT: Show the effective effort in the coach status line (cctop infers it from transcript `effort`/`perTurnEffort` keys); offer '/effort low for the next N tool-only steps' as an explicit Button that sets a flag the turn.step hook honours.

[R01] BuiltinToolResults.Agent: per-subagent usage, thinking tokens, toolStats and totalTokens on completion | lever=token_savings | used=unknown | conf=high
  DETAIL: The Agent tool's result is `{ agentId, agentType?, content, resolvedModel?, modelsUsed?, totalToolUseCount, totalDurationMs, totalTokens, usage: { input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens, server_tool_use, service_tier, cache_creation: { ephemeral_1h_input_tokens, ephemeral_5m_input_tokens }, output_tokens_details: { thinking_tokens } }, toolStats?: { readCount, searchCount, bashCount, editFileCount, linesAdded, linesRemoved, otherToolCount }, status: 'completed', prompt, worktreePath?, worktreeBranch? }` or `{ status: 'async_launched', agentId, description, resolvedModel, modelsUsed, outputFile }` or `{ status: 'remote_launched', taskId, sessionUrl, outputFile }`. Available on `tool.call` `result` for the model's Agent calls and, for plugin-raised spawns, as `AgentCallRecord`.
  SRC: claude-code.d.ts L8578-8661 (BuiltinToolResults.Agent), L48-61 (AgentCallRecord)
  ACT: Use `toolStats.searchCount + readCount` vs `editFileCount` with `resolvedModel` to classify a subagent as search/summarise work on an Opus model (A14 exact); `cache_creation.ephemeral_1h_input_tokens > 0` is a direct read of the 1 h TTL. Check whether cctop's transcript parser already reads these from `toolUseResult`.

[R02] BuiltinToolResults.Bash: persisted/truncated output, timeouts, backgrounding, git operations and edit diffs | lever=token_savings, less_rework | used=unknown | conf=high
  DETAIL: Bash result fields: `stdout`, `stderr`, `interrupted`, `backgroundTaskId?`, `backgroundedByUser?`, `timedOutAfterMs?` ("hit its timeout and was auto-backgrounded"), `persistedOutputPath?`/`persistedOutputSize?` ("set when output is too large for inline"), `returnCodeInterpretation?`, `noOutputExpected?`, `staleReadFileStateHint?`, `ghRateLimitHint?`, `gitOperation?: { commit?, push?, branch?, pr? }` ("Client-facing — lets clients render git activity without re-parsing stdout"), `bashEditDiff?: { files[], moreFiles, changedFiles? }` ("Per-file diff of the working-tree changes this command made"). Bash input: `command`, `timeout? (max 600000)`, `run_in_background?`, `description?`, `dangerouslyDisableSandbox?`.
  SRC: claude-code.d.ts L8662-8760 (BuiltinToolResults.Bash), L8273-8284 (BuiltinToolInputs.Bash)
  ACT: `persistedOutputSize` vs inline text length tells how much of a big output the model actually saw (a truncated result is a rework risk); `timedOutAfterMs` and `backgroundTaskId` give the A10 long-command signal exactly; `gitOperation.commit` marks natural session boundaries for A18.

[R03] BuiltinToolResults.Read: 'file_unchanged' dedup results and truncatedByTokenCap change how re-reads should be counted | lever=less_rework | used=unknown | conf=high
  DETAIL: Read's result is a union: `{ type: 'text', file: { filePath, content, numLines, startLine, totalLines, truncatedByTokenCap? } }`, `image`, `notebook`, `pdf`, `parts`, and `{ type: 'file_unchanged', file: { filePath }, source?: 'seeded' }` — "Set when the dedup matched a startup-seeded entry (CLAUDE.md / nested memory) rather than a prior Read tool_result". A `file_unchanged` re-read injects no content, so it should not count toward A04; `truncatedByTokenCap` means the model saw a partial page ("the content is a partial first page") — a likely follow-up read.
  SRC: claude-code.d.ts L8880-8960 (BuiltinToolResults.Read)
  ACT: In both the transcript parser and any hook-based counter, exclude `type === 'file_unchanged'` from re-read counts and treat `truncatedByTokenCap` as 'read was partial; suggest offset/limit'. Verify what cctop's A04 does today with these result shapes.

[R04] BuiltinToolResults.Edit/Write/NotebookEdit: exact lines added/removed, userModified and staged flags | lever=less_rework | used=unknown | conf=high
  DETAIL: Edit result: `{ filePath, oldString, newString, originalFile, structuredPatch[{oldStart,oldLines,newStart,newLines,lines}], userModified, replaceAll, gitDiff?: { additions, deletions, changes, patch }, staged? }`; Write: `{ type: 'create'|'update', filePath, content, structuredPatch, originalFile, gitDiff?, userModified?, staged? }`. `userModified` = the user edited the proposal in the permission dialog; `staged` = held for review, file unchanged.
  SRC: claude-code.d.ts L8800-8840 (Edit), L9190-9234 (Write), L8845-8870 (NotebookEdit)
  ACT: Count `userModified` edits (the user corrected Claude at the dialog — a rework signal cctop does not have) and use `gitDiff.additions/deletions` for the Files panel's lines ± exactly.

[R05] BuiltinToolResults.ToolSearch / Skill / ScheduleWakeup / Workflow: deferred-tool count, forked skills, loops | lever=token_savings | used=unknown | conf=medium
  DETAIL: `ToolSearch` result: `{ matches, query, total_deferred_tools, pending_mcp_servers?, failed_mcp_servers? }`; `Skill` result: `{ success, commandName, allowedTools?, model?, status: 'inline' } | { status: 'forked', agentId, result, background? }`; `ScheduleWakeup`: `{ scheduledFor, clampedDelaySeconds, wasClamped, stopped?, cancelledWakeups? }`; `Workflow`: `{ status, taskId, taskType, workflowName, runId, transcriptDir, ... }`.
  SRC: claude-code.d.ts L9095-9110 (ToolSearch), L9060-9090 (Skill), L9040-9058 (ScheduleWakeup), L9150-9188 (Workflow)
  ACT: `total_deferred_tools` is the count of tool schemas kept out of the prefix by deferral (a positive A07 signal); `failed_mcp_servers` names servers whose config is dead weight; forked skills should be attributed to their `agentId` in the Agents panel.

[R06] Generated BuiltinToolInputs table has 29 tools and omits Glob, Grep, TodoWrite, AskUserQuestion, ExitPlanMode — matchers on those names will not type-check | lever=less_rework | used=partial | conf=medium
  DETAIL: `/plugin-types` writes BuiltinToolInputs "from each tool's input schema" of the session that ran it. This file lists: Agent, Bash, CronCreate, CronDelete, CronList, DesignSync, Edit, EnterWorktree, ExitWorktree, ListAgents, ListMcpResourcesTool, Monitor, NotebookEdit, PushNotification, Read, ReadMcpResourceDirTool, ReadMcpResourceTool, RemoteTrigger, ReportFindings, ScheduleWakeup, SendMessage, Skill, TaskOutput, TaskStop, ToolSearch, WebFetch, WebSearch, Workflow, Write. Because `BuiltinToolCallInput` is the union over declared names once the table is non-empty, `e.tool === 'Grep'` is a compile error even though the runtime event still arrives. `claude-code-mcp.d.ts` (661 lines, 79 `mcp__` entries) is the developer's own MCP servers and is machine-specific.
  SRC: claude-code.d.ts L8250-8574 (BuiltinToolInputs), L542-548 (BuiltinToolCallInput), L575-581; claude-code-mcp.d.ts L1-8 header
  ACT: Regenerate `/plugin-types` from a default-mode session (this one was likely generated under auto mode where Glob/Grep are withheld) before writing tool matchers; in code, compare `e.tool` as `string` where the table is incomplete.

[S07] Limits and gaps that shape the coach design | lever=less_rework | used=partial | conf=high
  DETAIL: No push event for context-percent thresholds (poll `$.session.usage()`); no plan-mode/todo events (only `permission_mode` on classic events and `PermissionUpdate.setMode` inside PermissionRequest suggestions); no engine version on `$`; permission dialog cannot be drawn by a plugin (only `$.ui.notice`); `$.ui.status` is one line per plugin; `$.ui.open` pane is undrawn below 144/110 columns; `ui.render` is cached per props and needs `$.ui.invalidate` (≤10/s); `$.fs` 4 MiB per read; `$.store` 4 MiB total; `$.process.run` ≤ 10 min and reads whole output; hooks have a time budget and a failing hook is skipped silently (`.catch` handler gets `HookFailure { kind: 'throw'|'timeout', message?, budget }`); `$.ui.ask` rejects in `-p`; `turn.step` hooks must be async generators.
  SRC: claude-code.d.ts L1507-1514 (invalidate ≤10/s), L1596-1608 (open width rule), L3162-3182 (HookFailure), L2036-2046 ($.fs 4 MiB), L2101-2135 ($.store), L4445 (PermissionMode)
  ACT: Keep the coach's always-on surface to `$.ui.status` + toasts (work at any width and in the classic renderer) and make the pane optional; add `.catch` on every registration to log failures through `$.ui.log`.

[S08] What the pane PRD already plans vs what it leaves unused (mapping) | lever=none | used=partial | conf=high
  DETAIL: Planned (tasks/prd-cctop-pane.md L53-62 data-source table, US-001..US-009): `$.ui.open/close`, `ui.render {component:'Pane'}`, `$.ui.resolve`, `$.ui.invalidate`, `$.clock.every/after/now`, `$.session.usage/id/model/turns/cwd/repo`, events `turn.start` (turnId,text), `turn.step` (tool starts only), `turn.complete` (durationMs, reason), `tool.call` (timing, isError, text length), `session.compact` (count), `session.start`, `agent.spawn`, `$.agent.list`, `$.process.run`, `$.command.register`+`command.run`, `$.store`, `$.config` (mentioned), `$.fs.*`, `$.env.get('HOME')`, `$.ui.log/toast/status` (diagnostics only), `skill.prompt` (degrade). Explicit non-goals (§6): no `$.tool.call`, no prompt injection, no `prompt.context`. Unused: all `classic.*` events, `tool.check`, `prompt.submit/suggest/fill`, `prompt.section`/`tool.describe`, `turn.step` usage/stopReason/effort rewrite, `turn.complete.usage/refusal`, `$.session.messages()`, `$.settings.read`, `$.fs.ancestors`, `$.model.*`, `$.turn.abort`, `ui.render` for Spinner/PromptHint/SessionMode/AbovePrompt/ToolUse, `next.trace`, op-event hooks.
  SRC: /Users/tom/code/cctop/tasks/prd-cctop-pane.md L53-62, L100-110 (US-003), L217-231 (FR-10, FR-15, §6 non-goals), L248-254 (§8)
  ACT: Add a 'coach' story to the pane PRD covering: `classic.PermissionRequest/PermissionDenied`, `tool.check`, `prompt.submit` (paste size, queued), `turn.step` usage, `classic.PreModelSwitch/PostModelSwitch`, `classic.SessionStart` resume fields, `classic.StopFailure`, `classic.Stop.background_tasks`, `$.ui.status/toast`, `AbovePrompt` buttons and `$.prompt.suggest`.

