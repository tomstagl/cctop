SUMMARY: Mined all 81 transcripts under ~/.claude/projects (7 projects with content, 11 Aug–12 Sep 2026, 47,134 flattened events: 760 prompt turns, 12,361 assistant lines, 6,255 tool calls, 4,333 Bash results, 461 Edit/Write calls; models opus-5/sonnet-5 with 1M windows, 25 sessions past 200k context) plus 77 subagent transcripts, 45 usage-data/session-meta and 40 facets files. Hard, exactly-detectable rework signals are rarer than expected: 11 user interruptions (1.4% of turns, 9 of them within the first 5 tool calls, all followed by a re-prompt within ~15 s — interruptions are steers, not aborts), 9 Edit/Write errors in 461 calls with zero 'String to replace not found', and only 8 same-command consecutive Bash failure loops (failures are git/gh plumbing, 1 cargo, 0 npm/pytest). The high-volume signals are instead: 63 mid-turn steers absorbed via queue-operation (6x more than interrupts), 76 permission/auto-mode denials (27% of all tool errors), 78 runs of >=5 serial single-tool assistant messages (53 harness batching reminders), same-file churn (15 files edited >=8x per session, max 76; 37 turn/file cases >=4 edits; 67 edit->re-read->edit sequences; 126 redundant re-reads), and idle-gap cache loss (22% of prompts arrive >5 min after the last event; every gap >2 h missed the cache, 49 misses re-wrote a median 183k / total 10.0M cache-creation tokens; observed effective TTL ~1 h). Keyword-based 'correction prompt' detection is unreliable (186/673 prompts hit broad markers but only 6 are short genuine corrections; corrections do not follow visible tool errors — 1/74). Compaction is essentially absent (1 compact_boundary line: 567k->230k in 80.7 s; 0 >=30% context drops), so with 1M models the lever is context growth, not compaction. Claude Code already computes user_interruptions, tool_error_categories and user_response_times deterministically in session-meta (matches my counts in 8/10 overlapping sessions) and LLM-judges tooling_loop/wrong_approach/excessive_changes/user_rejected_action in facets — but offline, only for ~12% of these transcripts, and cctop reads none of it. Several harness-native signals cctop drops today would give a coach view free evidence: interruptedMessageId, queue-operation.reason=absorbed_mid_turn, queued_command origin, away_summary (fires at 3 min idle), batching/silent-turn reminders, edited_text_file, staleRecovered/staleReadFileStateHint, toolUseResult.gitOperation (commit/push/PR milestones), file-history-delta, compact_boundary.compactMetadata.

OPEN: Why do 21 /clear invocations leave no >=30% context drop in the same transcript file — does /clear start a new session file (new sessionId), and if so which line links the two? (Check ~/.claude/sessions/*.json and history.jsonl sessionId around the /clear timestamps.) | toolUseResult.interrupted was never true in 4,333 Bash results although 5 interruptions hit a running Bash — does Esc during Bash only surface via the PostToolUseFailure hook (is_interrupt) in 2.1.269, or via the interruptedMessageId key alone? | The observed cache-miss curve (4% at 5–30 min, 24% at 30 min–2 h, 100% >2 h) implies a 1 h TTL for opus-5/sonnet-5 here; does the status-line prompt_cache.ttl / expires_at agree, and is the 5-min default ever in effect on this account? | batching_reminder_sent, silent_turn_reminder and total_tokens_reminder only appear in claude-fable-5-1 sessions — are they model-gated or CLI-version-gated, and will they exist for opus/sonnet users of cctop? | Subagent transcripts (77 files, 1,588 Bash / 58 Edit / 40 Write calls) were only scanned for Edit errors; same-file churn, failure loops and test/build loops inside Ralph/workflow subagents (where the real test runs happen) are unmeasured. | Correction-prompt detection has no reliable deterministic proxy (6 true cases in 673 prompts); would the owner accept a small Haiku classification at turn end using the facets friction taxonomy, or should the coach avoid 'correction' language entirely? | file-history-delta lines are written for every modified file but no rewind was ever used — is /rewind logged anywhere (local_command, queue-operation, sessions json) so a coach could measure whether its rewind nudge is followed? | session-meta first_prompt stores 40 characters of the user's prompt text — if cctop ingests session-meta, it must not display or forward that field.

[RW-01] User interruptions: 11 in 760 turns, always an early steer followed by a re-prompt within seconds | lever=less_rework | used=no | conf=high
  DETAIL: Definition: a non-meta user line whose text block equals/contains '[Request interrupted by user]' (9) or '[Request interrupted by user for tool use]' (2). 9 of 11 carry a top-level key `interruptedMessageId` = the assistant message.id that was cut off; merging all assistant lines with that id gives the running tool: Bash 5 (one with 2 parallel Bash), Read 1, Skill 1, mcp__claude-in-chrome__computer 1, none (text streaming) 1. Tool calls already made in the interrupted turn: 1,11,125,1,2,1,2,3,2,2,5 -> 9/11 within the first 5 calls; the outlier (125 tools, 19.9 min, 52,874 output tokens) was a browser-automation run. Waste per interrupted turn otherwise 194–6,553 output tokens. In 10/11 the user's next prompt came 5–66 s later (median 15 s), typically 30–280 chars: an interrupt is a correction, not an abandonment. `toolUseResult.interrupted` was never true in 4,333 Bash results, so Esc during Bash is NOT visible there; the hook PostToolUseFailure.is_interrupt (audit UNS-11) is the alternative. Reliability: high (exact marker) but guard against the marker text inside tool_result content (4 false hits from a subagent report in this corpus): require content[].type=='text' and no tool_result block. Claude Code's session-meta `user_interruptions` uses the same definition (3 across its 45 sessions; matches mine in 8/10 overlaps, mismatches are sessions that continued after the meta was written).
  SRC: ~/.claude/projects/*/*.jsonl user lines, keys interruptedMessageId + message.content[].text; jq: cat *.jsonl | jq -c 'select(.type=="user" and (.message.content|type)=="array" and (.message.content|all(.type!="tool_result")) and ((.message.content|map(select(.type=="text")|.text)|join("\n"))|test("\\[Request interrupted by user"))) | {timestamp, interruptedMessageId}'
  EVID: Corpus: 9 x '[Request interrupted by user]', 2 x '[Request interrupted by user for tool use]' (text form, len=42); 9 lines have interruptedMessageId (msg_011…); merged tools on target ids: ['Bash'] x4, ['Bash','Bash'], ['Read'], ['Skill'], ['mcp__claude-in-chrome__computer' x2], None; next prompt latency after interrupt (s): 59,15,-,317,66,5,6,15,35,17,10; toolUseResult.interrupted==true count: 0; session-meta sum user_interruptions=3/45 sessions
  ACT: Coach line on interrupt: 'Turn cut after N tool calls / X output tokens (tool: Bash) — Claude will re-plan from your next message; state the correction in one line'. Mark the turn as wasted in the ledger (tokens spent, no end_turn). Parse interruptedMessageId to attribute the waste to the exact API call.

[RW-02] Permission denials and auto-mode classifier blocks are 27% of all tool errors (76 of 283) | lever=less_rework | used=partial | conf=high
  DETAIL: Three distinct denial texts appear as is_error tool_results / string toolUseResult: (a) 'The user doesn't want to proceed with this tool use. The tool use was rejected…' = manual rejection, 12 (Bash 4, Edit 4, ExitPlanMode 2, AskUserQuestion 2); (b) 'Permission for this action was denied by the Claude Code auto mode classifier. Reason…' = auto-mode block, 43 (Bash 39, Edit 1, mcp__claude-in-chrome__computer 1, Skill 1, ScheduleWakeup 1); (c) 'File is in a directory that is denied by your permission settings' / 'Permission to use Bash with command <cmd> has been denied' = settings deny, 21 (Bash 19, Edit 2). Also 'User rejected tool use' 3. Permission-mode lines show auto mode in 1,273 of 1,410 lines (90%), so the classifier is the main gate. Reliability: high (fixed strings). session-meta's 'User Rejected' category (12 over 45 sessions) is the same signal, offline.
  SRC: user lines: .message.content[] tool_result.is_error==true + .toolUseResult (string); jq: cat *.jsonl | jq -r 'select(.type=="user" and (.toolUseResult|type)=="string") | .toolUseResult | if test("doesn.t want to proceed|User rejected") then "user_rejected" elif test("auto mode classifier") then "auto_mode" elif test("denied by your permission settings|Permission to use") then "settings" else empty end' | sort | uniq -c
  EVID: Counts: user_rejected {Bash:4, Edit:4, ExitPlanMode:2, AskUserQuestion:2} + 'User rejected tool use' 3; auto_mode_denied {Bash:39, Edit:1, mcp computer:1, Skill:1, ScheduleWakeup:1}; settings_denied {Bash:19, Edit:2}; permission-mode lines: auto 1273, default 54, plan 53, acceptEdits 30; session-meta tool_error_categories 'User Rejected'=12
  ACT: Classify is_error results into rejected/auto-denied/settings-denied per turn (tools.rs only has is_error). Coach: '3 auto-mode denials for Bash this turn — Claude keeps retrying variants; add permissions.allow for `<cmd prefix>` or answer the prompt', and 'you rejected Edit twice — tell Claude the constraint instead of rejecting again'. Also fixes A09, which only sees PermissionRequest hooks (rare in auto mode).

[RW-03] Keyword 'correction prompt' detection is unreliable: 186/673 prompts hit broad markers, only 6 are short genuine corrections, and corrections do not follow visible tool errors | lever=better_outcomes | used=no | conf=medium
  DETAIL: Over 673 real prompts (760 minus 56 slash/skill invocations and 31 local-command echoes): broad markers hit 'not' 103, 'no' 80, 'fix' 54, 'instead' 38, 'actually' 30, 'don't' 29, 'again' 22, 'why' 21, 'wrong' 19, 'stop' 18, German 'nicht' 11, 'still broken' 5, 'revert' 2. A strict set (wrong/instead/actually/stop/undo/revert/still broken/doesn't work/not what/nope + German equivalents) matches 74 prompts, but 68 of them are >=200 chars (long instructions that merely contain 'instead'); only 6 prompts (<120 chars, hard negation) in 6 sessions are unambiguous terse corrections (0.9%). What preceded the 74: previous turn's last tool Bash 28, no tool (pure text turn) 25, SendMessage 6, Agent 5, Skill 4; last tool result was an error in 1/74 (baseline 17/673); assistant ended with '?' in 6/74 (baseline 135/673); preceding assistant text >=1500 chars in 20/53. Prompt-length distribution overall: <40 ch 228, <120 192, <500 145, <2000 45, >=2000 63. Conclusion: a live keyword rule would be ~90% false positive; corrections follow long autonomous Bash turns or long prose answers, not tool failures. Claude Code's facets classifier (LLM, offline) labels the same thing as wrong_approach (13), misunderstood_request (3), repeated_clarification (1) across 40 sessions.
  SRC: user text lines (non-meta, no tool_result) message.content; jq: cat *.jsonl | jq -r 'select(.type=="user" and .isMeta!=true and (.message.content|type)=="array" and (.message.content|all(.type!="tool_result"))) | (.message.content|map(select(.type=="text")|.text)|join(" ")) | select(length<120 and test("^\\s*(no|nope|wrong|stop|nein|nicht|falsch)\\b";"i")) | length' | wc -l  (never print the text)
  EVID: marker counts {not:103,no:80,fix:54,instead:38,actually:30,dont:29,again:22,why:21,wrong:19,stop:18,de_nicht:11,still_broken:5,revert:2}; strict=74 of which len>=200: 68, <200: 4, <40: 2; short(<120)+hard negation: 6 in 6 sessions; lastResErr before strict prompt: True 1 / False 48 / none 25; facets friction_counts wrong_approach=13, misunderstood_request=3
  ACT: Do not ship a keyword-based 'correction detected' nudge. Use structural proxies instead: (1) prompt arrives <60 s after end_turn AND is <120 chars AND starts with a negation -> 'correction' (6 true cases here); (2) 'user re-prompted within 30 s after a >1500-char answer' as an 'answer too long?' hint; (3) optionally run a Haiku classifier at turn end with the facets taxonomy if the owner accepts the cost.

[RW-04] Edit/Write failures are nearly extinct (9 of 461 calls, 0 'String to replace not found'); stale-file recovery flags are the live signal instead | lever=less_rework | used=no | conf=high
  DETAIL: Main transcripts: 461 Edit/Write/MultiEdit calls, 9 is_error: user_rejected 4, settings_denied 2, auto_mode_denied 1, Write 'has not been read yet' 1, Write 'File has been modified since read' 1. Subagent transcripts (77 files: 58 Edit, 40 Write) also show no old_string failures; their errors are 'Exit code 1' 26, user rejections 20, worktree-isolation errors 8. session-meta agrees: 'Edit Failed' 2 and 'File Not Found' 7 over 45 sessions. 4 of 317 Edits had no prior Read/Write of the path in the main transcript (Claude Code 2.1.269 enforces read-before-edit, so these were read via Bash/cat). What does show up: `toolUseResult.staleRecovered` on 7 Edit results (harness auto-recovered a stale read), `toolUseResult.staleReadFileStateHint` on 24 Bash results (file changed after a Bash command), `userModified` true on 0/461, and `edited_text_file` attachments (115, see RW-18). Reliability: high; volume too low for a rule unless combined with stale flags.
  SRC: user lines .toolUseResult (string for errors; object keys filePath,structuredPatch,userModified,staleRecovered for Edit; staleReadFileStateHint for Bash); jq: cat *.jsonl | jq -r 'select(.type=="user") | .toolUseResult | if type=="string" then select(test("not been read|String to replace|modified since read|Found [0-9]+ matches"))|.[0:60] elif type=="object" then (select(.staleRecovered or .staleReadFileStateHint)|"stale:"+(keys|join(","))) else empty end' | sort | uniq -c
  EVID: Edit/Write error classes: {user_rejected:4, settings_denied:2, auto_mode_denied:1, not_read_yet:1 (Write), modified_since_read:1 (Write)}; toolUseResult key-shapes: 'filePath,newString,oldString,originalFile,replaceAll,staleRecovered,structuredPatch,userModified' x7; 'interrupted,isImage,noOutputExpected,staleReadFileStateHint,stderr,stdout' x16 (+8 with other keys); userModified true: 0; subagent Edit errors: none with 'String to replace'
  ACT: Drop the assumption that Edit-string failures are a major rework source in 2.1.269. Surface staleRecovered / staleReadFileStateHint as 'file changed under Claude — a re-read is coming (N tokens)' and suppress A04 re-read advice for those files.

[RW-05] Bash failure loops: 5.1% of Bash calls fail, but same-command consecutive loops are rare (8) and are git/gh plumbing, not test/build loops | lever=less_rework | used=partial | conf=high
  DETAIL: Definition: Bash result whose `toolUseResult` is the string 'Error: Exit code N' (143: code 1 x94, 8 x21 [gh/jq], 128 x13 [git], 2 x5, 127 x3, 143 x3, 7 x2) or is_error with other text (35: MCP timeouts, ssh/clawctl guards, rate limits) or denials (42). Of 4,333 Bash results 222 failed. Loop = consecutive failing Bash results whose command shares the first 40 normalised chars: 8 loops in 4 sessions (lengths 2 x6, 3 x1, 6 x1). Any two consecutive failing Bash calls (different commands): 40 pairs. A failing command re-run immediately with the same prefix: 29 times — retry succeeded 16, failed again 13 (gh: 10 of 12 retries failed again; git 1/2). Failing commands by class: git 58, gh 54, other 77, grep 18, clawctl 6, ssh 4, curl 3, cargo 1, make 1, npm/pytest/tsc 0. So in this corpus 'test failed -> edit -> test failed' loops essentially do not occur in the main transcript (tests run inside Ralph subagents); the real loop is CLI/API plumbing (gh auth/repo errors, git state). Reliability: high for the exit-code marker; command-prefix matching is coarse (30/40 chars) and is the same weakness the audit found in A15. Claude Code's session-meta 'Command Failed' (85/45 sessions) is the same count.
  SRC: tool_use (assistant .message.content[].input.command) joined to user .toolUseResult by tool_use_id; jq: cat <session>.jsonl | jq -r 'select(.type=="user" and (.toolUseResult|type)=="string" and (.toolUseResult|startswith("Error: Exit code"))) | .toolUseResult[0:20]' | sort | uniq -c; join via: jq -r 'select(.type=="assistant")|.message.content[]?|select(.type=="tool_use" and .name=="Bash")|[.id,(.input.command[0:40])]|@tsv'
  EVID: bash results=4333 failed=222 (5.1%); exit codes {1:94, 8:21, 128:13, 2:5, 127:3, 143:3, 7:2, 3:1}; same-prefix consecutive loops=8 in 4 sessions, len dist {2:6, 3:1, 6:1}; consecutive failing pairs (any cmd)=40; immediate same-prefix retry=29 (succeeded 16 / failed 13; gh retry_failed 10); failing cmd classes {other:77, git:58, gh:54, grep:18, clawctl:6, ssh:4, curl:3, cargo:1, make:1}
  ACT: Replace A15's session-lifetime '>=3 errors for same 30-char input' with a windowed rule: 'same command failed twice in a row (exit N) with no change in between — Claude is retrying blind; give it the missing fact or run the command yourself'. Special-case gh/git: 'gh failed 10/12 retries in this corpus — an auth/repo problem, not code'. Show the exit code and retry count live in the Turn panel.

[RW-06] Same-file churn is the dominant edit-side waste: 15 files edited >=8x per session (max 76), 37 turn/file cases with >=4 edits, 67 edit->re-read->edit sequences, 126 redundant re-reads | lever=token_savings | used=partial | conf=high
  DETAIL: Per session: 15 file paths with >=8 Edit/Write calls (76, 42, 33, 32, 27, 17, 13, 12, 10, 10, 9, 8, 8, 8, 8); extensions of the >=8 files: md 4, yaml 1, rs 1 — churn concentrates in PRD/docs/config files edited incrementally. Per turn: 37 (session,turn,file) cases with >=4 edits of one file in a single turn (max 21 edits/turn on one YAML, 17 on one file in another session, 9, 8, 8, 8). Edit->Read->Edit on the same file (Claude re-reads after its own edit): 67 sequences in 21 sessions (15 in e370b5c1, 12 in a6d60f35, 6 in c97b53ad). Redundant Reads (same path+offset read again later in the session): 126; same file read twice within one turn: 128 (top sessions 13, 13, 12, 11, 10). Read/Edit ratio median 0.4 in the 19 sessions with >=5 edits. Only 94 of 258 Read calls use offset/limit; 55 Read results exceed 10k chars. Reliability: high (tool_use inputs are exact); the <path> sanitiser hides basenames but not counts.
  SRC: assistant .message.content[] tool_use with name Edit/Write/MultiEdit and .input.file_path (Read: .input.file_path,.input.offset,.input.limit); jq: cat <session>.jsonl | jq -r 'select(.type=="assistant")|.message.content[]?|select(.type=="tool_use" and (.name|IN("Edit","Write","MultiEdit")))|.input.file_path' | sort | uniq -c | sort -rn | head; per-turn variant: bucket by count of preceding non-meta user text lines
  EVID: files >=8 edits/session: 15 (counts 76,42,33,32,27,17,13,12,10,10,9,8,8,8,8); files >=4 edits/turn: 37 (max 21); E->R->E sequences: 67 in 21 sessions; redundant reads: 126 (session) / 128 (turn); Read with offset/limit: 94/258; Read results >10k chars: 55; extensions of >=8-edit files: {md:4, yaml:1, rs:1}
  ACT: Add per-turn edit count per file to files.rs and two coach rules: (1) 'file X edited N times this turn — ask for one consolidated Write/rewrite; each Edit returns a structuredPatch and often triggers a re-read'; (2) 'Claude re-read X right after editing it N times — say "no need to re-read after Edit"'. Make A04 ignore ranged reads (offset/limit) and reset on edited_text_file/stale hints.

[RW-07] Git undo by Claude is rare (38 destructive ops) but Bash results carry a structured `gitOperation` record (222) that gives free commit/push/PR milestones | lever=less_rework | used=no | conf=high
  DETAIL: Bash commands matching git checkout 144, switch 22, stash 32, reset 18, restore 5, revert 0 — most checkout/switch are branch navigation; destructive forms (`reset --hard`, `checkout -- `, `checkout .`, `restore `, `clean -f`, `stash`) total 38. Context: 60 of 200 git ops sat in a turn whose prompt carried a correction marker, 18 followed a failing tool result. More useful: 222 Bash results have `toolUseResult.gitOperation`, a harness-written summary of what the command did: {commit:{branch,kind:'committed'|'cherry-picked',sha}}, {push:{branch}}, {pr:{action:'created'|'edited'|'merged'|'commented'|'ready'|'closed',number,url}}, {branch:{action:'merged'|'rebased',ref}} — e.g. 14 pushes to main, dozens of 'pr created'. Reliability: high (structured). session-meta also counts git_commits/git_pushes per session.
  SRC: user .toolUseResult.gitOperation (Bash results); jq: cat *.jsonl | jq -c 'select(.type=="user" and (.toolUseResult|type)=="object" and .toolUseResult.gitOperation) | .toolUseResult.gitOperation | {kind:(keys[0]), action:(.pr.action // .branch.action // .commit.kind // "push")}' | sort | uniq -c; destructive: assistant tool_use .input.command | test("git (reset --hard|checkout -- |checkout \\.|restore |clean -f|stash)")
  EVID: git ops in Bash commands {checkout:144, switch:22, stash:32, reset:18, restore:5, revert:0}; destructive forms: 38; gitOperation results: 222 with key sets like '{"push":{"branch":"main"}}' x14, commit{kind:committed|cherry-picked,sha}, pr{action:created|edited|merged|commented|ready|closed,number,url}, branch{action:merged|rebased,ref}
  ACT: Parse gitOperation in the transcript collector: it is the missing 'natural boundary' input for A18/hand-off ('last commit 42 min / 18 edits ago'), a 'since last commit: N files changed' counter without shelling out, and a guard: 'Claude ran git stash / reset --hard — check `git stash list` before continuing'.

[RW-08] File-history checkpoints are written on every prompt (587 snapshots, 250 deltas) but no rewind/restore ever happened | lever=less_rework | used=no | conf=high
  DETAIL: `file-history-snapshot` lines: 587 (one per user prompt; keys isSnapshotUpdate,messageId,snapshot{messageId,timestamp,trackedFileBackups}); trackedFileBackups size 0 in 270, 1–9 in 182, >=10 in 135. `file-history-delta` lines: 250 in 35 sessions ({messageId, snapshotMessageId, trackingPath, backup{backupFileName,backupTime,realParentDir,version}}) — one per file version backed up before Claude modifies it. ~/.claude/file-history holds 33 session dirs, 4.8 MB. Rewind evidence: none — no /rewind local_command, isSnapshotUpdate false in 587/587, `preCheckpoint` present in only 2 snapshots. So the checkpoint system is pure overhead in this corpus, but the delta count per prompt is an exact 'files Claude will change/changed this turn' counter that also covers edits made through Bash (trackingPath is any written file).
  SRC: lines .type=="file-history-delta" (.trackingPath, .backup.version) and .type=="file-history-snapshot" (.snapshot.trackedFileBackups|length); jq: cat *.jsonl | jq -r 'select(.type=="file-history-delta") | .trackingPath' | sort | uniq -c | sort -rn | head
  EVID: snapshots=587 (isUpdate false 587); nTracked dist {0:270, 1:37, 2:29, 3:62, 4:17, 5:15, 6:7, 7:7, 8:8, >=10:135}; deltas=250 in 35 sessions, backup keys 'backupFileName,backupTime,realParentDir,version'; preCheckpoint key in 2 snapshots; ls ~/.claude/file-history: 33 dirs, 4.8M; no '/rewind' in local_command content
  ACT: After an interrupt or short correction (RW-01/RW-03 proxies), coach: 'N files checkpointed since prompt #k — Esc Esc (rewind) restores them instead of asking Claude to undo'. Use delta count per turn as the Files panel's 'touched this turn' figure (works for Bash-made writes).

[RW-09] User response latency: median 63 s, 22% of prompts arrive after >5 min, and every gap >2 h misses the prompt cache (observed TTL ~1 h; 49 misses re-wrote 10.0M cache-creation tokens) | lever=token_savings | used=partial | conf=high
  DETAIL: Latency = time from the last transcript event (assistant/result/prompt) to the next non-meta user prompt: n=666, median 63 s, p75 197 s, p90 1,589 s, p95 9,434 s; buckets <30 s 222, <2 min 208, <5 min 92, <1 h 101, <8 h 26, >=8 h 17. After an assistant question the median reply is 62 s vs 78 s otherwise. Cache effect (first API call after the prompt has cache_read < 50% of the previous call's context): gap <5 min 43/464 (9%, other causes), 5–30 min 4/95 (4%), 30 min–2 h 7/29 (24%), >2 h 38/38 (100%) — the effective TTL on these opus-5/sonnet-5 sessions behaves like 1 h, not 5 min. Cost of a miss: cache_creation on the first call after a >=5-min-gap miss median 183,377 tokens, p90 387,086, sum 10,033,555 over 49 misses. Claude Code itself already computes this: session-meta `user_response_times[]` (412 samples over 45 sessions: median 92 s, p90 724 s) and emits a `system.away_summary` line after an idle gap of >=3 min (149 lines; gap before them p10 3.0 min, median 3.1, p90 8.3) — i.e. the harness's own 'user is away' detector fires at 3 minutes. Reliability: high (timestamps on every line).
  SRC: line .timestamp deltas between the last assistant/user line and the next non-meta user text line; system.away_summary; session-meta user_response_times; jq: cat <s>.jsonl | jq -r 'select(.type=="system" and .subtype=="away_summary") | .timestamp'; cache check: assistant .message.usage.cache_read_input_tokens of the first call after the prompt vs cache_read+cache_creation+input of the previous call
  EVID: n=666 median=63s p75=197s p90=1589s p95=9434s; >5min gaps 144/666; cache-miss by gap {<5m: 43/464, 5-30m: 4/95, 30m-2h: 7/29, >2h: 38/38}; miss cache_creation median 183,377 p90 387,086 sum 10,033,555 (n=49); away_summary n=149, preceding gap median 3.1 min (p10 3.0); session-meta user_response_times n=412 median 91.7s p90 724s, >300s 104
  ACT: Turn A02 from post-mortem into a countdown: 'cache warm for mm:ss (TTL 1 h observed); a prompt before then avoids re-writing ~183k tokens'. Treat away_summary as the harness's idle signal (3 min) and show 'idle 4 min — cache expires in 56 min' in the coach view; after a >2 h gap, tell the user the next turn will pay the full prefix so batch their asks.

[RW-10] 19% of turns end with the assistant asking a question; 43% of the replies are <40 chars — question turns are clean hand-off boundaries | lever=better_outcomes | used=no | conf=medium
  DETAIL: Definition: the last text block of the last assistant message in a turn, trimmed, ends with '?'. 145 of 760 turns. Next prompt after a question turn: brief (<40 chars) 63 (43%), yes/no-ish 43 (30%), long (>=200 chars) 23 (16%); after non-question turns brief replies are 149/506 (29%). AskUserQuestion tool was used in 53 turns (66 calls, 64 answered — toolUseResult keys 'annotations,answers,questions' — 2 rejected). Strict-correction prompts followed a question turn in only 6/74 cases, i.e. asking does not provoke corrections. Reliability: high for the '?' test; semantic value medium (some '?' are rhetorical).
  SRC: assistant .message.content[] text blocks; take the last text block per turn; jq: cat <s>.jsonl | jq -r 'select(.type=="assistant") | .message.content[]? | select(.type=="text") | .text | rtrimstr("\n") | rtrimstr(" ") | test("\\?$")' (keep the last value before each user text line); AskUserQuestion: select(.toolUseResult.answers)
  EVID: turns=760, ending with '?': 145; next prompt brief 63, yes/no 43, long 23; non-question turns 506 with brief next 149; AskUserQuestion calls 66 (answered 64, error 2) in 53 turns
  ACT: Use 'ended with a question / AskUserQuestion pending' as the coach's WAITING state text ('Claude is waiting for an answer — a one-word reply is enough and keeps the cache warm') and as a natural-boundary input for A18 (question + no background tasks = safe hand-off point).

[RW-11] Turn shape: 31% of turns use zero tools, 5% use >30 tools; long autonomous runs are not more likely to be interrupted or corrected | lever=none | used=yes | conf=high
  DETAIL: 760 turns: 0 tools 233 (median wall 0.2 min), 1–5 tools 247 (1.8 min), 6–30 tools 240 (4.5 min, p90 20.6), 31–50 tools 23 (14.4 min, p90 97.6), >50 tools 17 (35 min, p90 551 min — Ralph/background loops). Outcomes: interruptions fell on 9 turns of size 1–5, 1 of 6–30, 1 of >50; a strict-correction prompt followed 6/233 zero-tool turns (2.6%), 15/247 (6%), 28/240 (11.7%), 4/23 (17%), 2/17 (12%). stop_reason over all assistant lines: tool_use 11,414 vs end_turn 939 (92% of API calls end in a tool call). Claude Code's own `system.turn_duration` lines (457: durationMs p50 0.9 min, p90 5.75 min; messageCount p50 244, p90 692) close each turn. Effort: medium 11,845 lines, high 188, xhigh 83. Reliability: high.
  SRC: count assistant tool_use blocks between consecutive non-meta user text lines; system.turn_duration {durationMs,messageCount}; jq: cat <s>.jsonl | jq -r 'select(.type=="system" and .subtype=="turn_duration") | [.durationMs,.messageCount]|@tsv'
  EVID: size buckets {0:233, 1-5:247, 6-30:240, 31-50:23, >50:17}; outcome (bucket,kind): (1-5,interrupted) 9, (6-30,interrupted) 1, (>50,interrupted) 1, corr_next: 6/15/28/4/2; turn_duration lines 457 p50 0.89 min p90 5.75 min; stop_reason tool_use 11414 / end_turn 939
  ACT: No new rule; it justifies the coaching stance 'steer in the first 5 tool calls' (RW-01) and shows that the pure-chat share (31%) is where A12/A08 (chatty turns, thinking share) apply.

[RW-12] Harness batching/silent-turn reminders: 53 batching_reminder_sent and 6 silent_turn_reminder, triggered by runs of serial single-tool calls; 78 such runs (>=5) corpus-wide | lever=token_savings | used=no | conf=high
  DETAIL: `batching_reminder_sent` attachment ({model, text:'First privately list what you need next; then request every item that doesn't depend on another's result in this one response.'}): 53, all in sessions on claude-fable-5-1; injected after the assistant issued 0–35 tool calls since its last text (median 245 s), with a preceding pattern of one-tool-per-message ('.1.1.1.11'). `silent_turn_reminder` ({text:'The user hasn't heard from you in a while — say in a few words what you're doing, then continue.'}): 6, fired 6–21 tool calls and 82–405 s (median 229 s) after the last assistant text. Corpus-wide runs of >=5 consecutive assistant messages that each contain exactly one tool_use and no text: 78 (68 of length 5–9, 9 of 10–14, 1 >=15) — each message in such a run is a full API round-trip re-sending the whole context. Also present: `bash_output_audience_note` 52, `total_tokens_reminder` 5,748 (one per API call in Fable sessions, ~90 bytes each). Reliability: high (exact attachment subtypes); model-gated (only seen with fable-5-1), so treat as version/model dependent.
  SRC: lines .type=="attachment" with .attachment.type in (batching_reminder_sent, silent_turn_reminder); jq: cat *.jsonl | jq -r 'select(.type=="attachment" and (.attachment.type|IN("batching_reminder_sent","silent_turn_reminder"))) | [.timestamp,.attachment.type,(.attachment.model//"-")]|@tsv'; serial runs: consecutive assistant lines with exactly one tool_use block and no text block
  EVID: batching_reminder_sent n=53 (model claude-fable-5-1 in all), toolsSinceLastText dist {0:13, 5:9, 10:7, 15:13, 20:5, 25:2, 30:3, 35:1}; silent_turn_reminder n=6, toolsSinceLastText 6–21, secsSinceLastText 82–405 (median 228.5); runs of >=5 single-tool assistant msgs: 78 {5-9:68, 10-14:9, >=15:1}; total_tokens_reminder lines 5748; bash_output_audience_note 52
  ACT: Coach: 'N serial single-tool calls in a row (harness sent a batching reminder) — each is a full context round-trip; ask Claude to batch independent reads/greps or delegate to an Explore agent'. Count reminder attachments per turn as 'harness overhead' bytes (audit GAP-05) and show silent_turn_reminder as a liveness indicator ('no text for 4 min').

[RW-13] TodoWrite is never used; TaskCreate/TaskUpdate appear in 2 sessions (28 creates, 32 completions); task_reminder attachments are mostly 'list is empty' | lever=better_outcomes | used=no | conf=high
  DETAIL: TodoWrite 0 calls in 81 sessions; TaskCreate 28 and TaskUpdate 57 (status completed 32, in_progress 11, pending 5, no status 9) in 2 sessions. Harness reminders: `task_reminder` 149 (itemCount 0 in 72, else 5/10/11/13/15/17/18 — the reminder re-injects the full task list JSON) and `todo_reminder` 18 (all itemCount 0). ~/.claude/plans has 3 files. So a task-list-based boundary detector would work in ~2% of sessions here; TaskUpdate status=completed is a clean milestone when present. Reliability: high (tool_use inputs).
  SRC: assistant tool_use name TodoWrite/TaskCreate/TaskUpdate (.input.status, .input.taskId); attachment task_reminder/todo_reminder .attachment.itemCount; jq: cat *.jsonl | jq -r 'select(.type=="assistant")|.message.content[]?|select(.type=="tool_use" and (.name|IN("TodoWrite","TaskCreate","TaskUpdate")))|[.name,(.input.status//"-")]|@tsv' | sort | uniq -c
  EVID: calls {TaskCreate:28, TaskUpdate:57}; TaskUpdate statuses {completed:32, in_progress:11, pending:5, none:9}; sessions using either: 2/81; task_reminder 149 (itemCount {0:72, 17:21, 10:20, 5:14, 15:14, 18:4, 11:2, 13:2}); todo_reminder 18 (itemCount 0)
  ACT: Do not build A18/hand-off detection on TodoWrite; use TaskUpdate completed + gitOperation commits (RW-07) + end_turn-without-question as the boundary trio. Count task_reminder bytes as injected context (each carries the full list).

[RW-14] Plan mode is rare (5 ExitPlanMode, 2 rejected; 90% of permission-mode lines are auto) and does not precede most large edit batches | lever=less_rework | used=partial | conf=medium
  DETAIL: ExitPlanMode 5 calls (3 accepted, 2 'user doesn't want to proceed'), EnterPlanMode 0; `plan_mode` attachments 11 ({isSubAgent, planExists, planFilePath, reminderType}) and `plan_mode_exit` 5; permission-mode lines: auto 1,273, default 54, plan 53, acceptEdits 30; mode lines all 'normal' (1,941). Turns with >=10 Edit/Write calls: 10, of which 3 in a session that had exited plan mode earlier. Slash commands run: /clear 21, /model 21, /admit 5, /review 3, /effort 3, /cctop:cctop 3 (user lines wrapped in <command-name>). Reliability: high; sample too small to claim plan mode reduces churn.
  SRC: assistant tool_use name ExitPlanMode; attachment plan_mode/plan_mode_exit; permission-mode lines .mode; jq: cat *.jsonl | jq -r 'select(.type=="permission-mode") | .mode' | sort | uniq -c; cat *.jsonl | jq -r 'select(.type=="attachment" and (.attachment.type|startswith("plan_mode"))) | .attachment.type'
  EVID: ExitPlanMode 5 (err user_rejected 2), EnterPlanMode 0; plan_mode 11, plan_mode_exit 5; permission-mode {auto:1273, default:54, plan:53, acceptEdits:30}; turns >=10 edits: 10 (3 after plan exit); ~/.claude/plans: 3 files; /clear 21, /model 21
  ACT: A '10+ edits without a plan' nudge would fire ~7 times in a month here — low value; better to show the current permission mode + 'plan file exists' from plan_mode attachments in the coach header and flag ExitPlanMode rejections as an explicit 'plan rejected, re-planning' event.

[RW-15] Resumed vs fresh sessions cannot be told apart from the transcript; /clear ran 21 times and 24 files contain >1 h internal gaps | lever=token_savings | used=partial | conf=medium
  DETAIL: No `summary` line type, no resume marker: `session_context` attachments (15) contain only {context,type}; 36/81 files have zero user lines with parentUuid==null (the first user line points at a preceding attachment/system line) and 45 have exactly one; `version` is constant within each file. Internal gaps >1 h: 24 files (1 gap: 10 files, 2: 8, 3–6: 6) — resume and idle are indistinguishable. /clear appears 21 times as user lines wrapped in <command-name>/clear</command-name> (plus /model 21), yet no >=30% context drop follows in the same file, which suggests /clear starts a new transcript file. `remote_session_change` attachments (57) carry {url:'https://claude.ai/code/session_…', commit:'Co-Authored-By…'} for bridged sessions; ~/.claude/sessions/<pid>.json has {sessionId, kind, status, bridgeSessionId, startedAt, version…}. The only authoritative source is the SessionStart hook payload `source` (startup|resume|clear|compact) (audit UNS-11). Reliability of transcript-only detection: low.
  SRC: user lines .parentUuid==null count per file; line .timestamp gaps; user text matching <command-name>/clear</command-name>; attachment remote_session_change; ~/.claude/sessions/*.json; jq: jq -rs '[.[]|select(.type=="user" and .parentUuid==null and .isMeta!=true)]|length' <s>.jsonl; jq -r 'select(.type=="user")|.message.content|tostring|capture("<command-name>(?<c>[^<]*)</command-name>").c' *.jsonl | sort | uniq -c
  EVID: roots==0: 36 files, ==1: 45 files; gaps>1h per file {0:57, 1:10, 2:8, 3:1, 4:2, 5:2, 6:1}; session_context keys 'context,type' x15; remote_session_change 57; /clear 21, /model 21; ~/.claude/sessions: 11 files, keys bridgeSessionId,cwd,entrypoint,kind,…,sessionId,startedAt,status,version
  ACT: Read SessionStart.source from the hook spool (already spooled by `cctop hook`) and the /clear command-name user line to mark hard context boundaries; on resume/clear show 'context rebuilt — previously read files are gone, expect N re-reads' and reset A04/A05 counters.

[RW-16] Compaction is effectively absent on 1M-context models: 1 compact_boundary (567k->230k, 80.7 s) and zero >=30% context drops; the cost is unbounded context growth instead | lever=token_savings | used=partial | conf=high
  DETAIL: Exactly one `system.compact_boundary` line in 81 sessions: compactMetadata {trigger:'auto', preTokens 567,672, postTokens 230,014, cumulativeDroppedTokens 337,658, durationMs 80,690, preCompactDiscoveredTools:[…], preservedSegment{headUuid,anchorUuid,tailUuid}, preservedMessages{…}}; the session ended 26 events later (1 assistant line, 0 tools, 2 prompts), so no post-compaction re-read behaviour could be measured. No consecutive main-chain assistant pair shows a >=30% context drop from >100k (cctop's A06 heuristic would never fire). Context growth: max context per model opus-5 953k, sonnet-5 502k, fable-5-1 380k, haiku-4.5 156k; sessions whose context exceeded 150k: 34, 200k: 25, 400k: 6 (top: 953k, 921k, 740k, 566k, 517k, 439k). Reliability: high.
  SRC: line .type=="system" .subtype=="compact_boundary" .compactMetadata{trigger,preTokens,postTokens,cumulativeDroppedTokens,durationMs}; assistant .message.usage (cache_read+cache_creation+input) per call; jq: cat *.jsonl | jq -c 'select(.subtype=="compact_boundary") | .compactMetadata | {trigger,preTokens,postTokens,durationMs}'
  EVID: compact_boundary count=1 {trigger:auto, preTokens:567672, postTokens:230014, durationMs:80690}; drops>=30% from >100k (main chain): 0; sessions max ctx >150k 34, >200k 25, >400k 6; per-model max ctx {opus-5:953k, sonnet-5:502k, fable-5-1:380k, haiku:156k}
  ACT: Parse compact_boundary (exact pre/post/duration) instead of inferring from drops, and re-target A06/A18 at growth: 'context 420k — every API call now re-reads 420k cached tokens; N serial calls this turn cost X; commit and /clear or hand off'. Show 'compaction would take ~80 s and drop ~60%' as the alternative.

[RW-17] Mid-turn steering is 6x more common than interrupting: 63 prompts absorbed mid-turn (queue-operation reason) and 35 human prompts queued while Claude worked | lever=less_rework | used=partial | conf=high
  DETAIL: `queue-operation` lines: enqueue 228, dequeue 125, remove 83 (reason absorbed_mid_turn 63 in 20 sessions, delivered_to_agent 18, none 20), popAll 1. `queued_command` attachments 72 in 26 sessions: commandMode 'prompt' with origin {kind:'human'} 35 (18 of them <40 chars, 15 <200, 2 >=200) = the user typed while Claude was running; commandMode 'task-notification' 37 = background/subagent completion notices. Queued human prompts occur in turns with 6–30 tools (40) and >30 tools (20), never in zero-tool turns — users steer during long runs rather than pressing Esc (11 interrupts, RW-01). Reliability: high (structured lines).
  SRC: lines .type=="queue-operation" {operation, reason, content}; attachment queued_command {commandMode, origin, prompt, timestamp}; jq: cat *.jsonl | jq -r 'select(.type=="queue-operation") | "\(.operation)\t\(.reason//"-")"' | sort | uniq -c; cat *.jsonl | jq -r 'select(.type=="attachment" and .attachment.type=="queued_command") | "\(.attachment.commandMode)\t\(.attachment.origin.kind//"-")"' | sort | uniq -c
  EVID: queue-operation {enqueue:228, dequeue:125, remove/absorbed_mid_turn:63, remove/delivered_to_agent:18, remove/-:20, popAll:1}; queued_command 72 (prompt+human 35 [len <40:18, <200:15, >=200:2]; task-notification 37); queued human prompts by turn size {6-30:40, >30:20, 1-5:12}
  ACT: Distinguish human steers from task notifications in the Turn panel's 'queued' count and coach: '1 steer queued — Claude reads it at its next API call, not immediately; Esc if it must stop now'. absorbed_mid_turn is the cleanest 'user corrected course without aborting' counter for a session rework score.

[RW-18] Files edited outside Claude are announced by the harness: 115 edited_text_file attachments in 25 sessions (mostly .md), the true source of stale-context re-reads | lever=token_savings | used=no | conf=high
  DETAIL: `edited_text_file` attachment ({filename, snippet}) is injected when the user changes a file in the IDE/editor while the session runs: 115 lines in 25 sessions; extensions md 82, ts 9, sh 4, json 4, py 3, yml 2. Related harness flags: `staleReadFileStateHint` on 24 Bash results, `staleRecovered` on 7 Edit results (RW-04). Claude Code's facets taxonomy labels this 'stale_context' (1 session). Reliability: high (structured).
  SRC: attachment .attachment.type=="edited_text_file" {filename, snippet}; jq: cat *.jsonl | jq -r 'select(.type=="attachment" and .attachment.type=="edited_text_file") | .attachment.filename | split(".")|last' | sort | uniq -c
  EVID: edited_text_file n=115 in 25 sessions; extensions {md:82, ts:9, sh:4, json:4, py:3, yml:2}; staleReadFileStateHint 24; staleRecovered 7; facets stale_context 1
  ACT: Coach: 'you edited <file> in the IDE — Claude's copy is stale; it will re-read it (~N tokens)'; exempt those files from A04 re-read advice; if the same .md is edited by both sides in one turn, warn about edit collisions.

[RW-19] Stop-hook blocking (Ralph loop) is visible as hook_blocking_error attachments (44) while stop_hook_summary.preventedContinuation is never true | lever=better_outcomes | used=partial | conf=high
  DETAIL: 683 `system.stop_hook_summary` lines (keys hasOutput, hookAdditionalContext, hookCount, hookErrors, hookInfos[].command, level, preventedContinuation, stopReason, toolUseID): hookErrors non-empty in 44, hookCount 2 in 32, preventedContinuation false in all 683. The Ralph loop's Stop hook instead surfaces as 44 `hook_blocking_error` attachments ({blockingError, hookEvent:'Stop', hookName:'Stop', toolUseID}) in one session, each re-injecting the loop prompt. Also `hook_system_message` 49 (e.g. '/ultrareview' tips) and `hook_success` 29. Reliability: high.
  SRC: system.stop_hook_summary {hookErrors, preventedContinuation, hookInfos[].command}; attachment hook_blocking_error {hookEvent, hookName, blockingError}; jq: cat *.jsonl | jq -r 'select(.type=="attachment" and .attachment.type=="hook_blocking_error") | "\(.attachment.hookEvent)\t\(.attachment.hookName)"' | sort | uniq -c
  EVID: stop_hook_summary 683: prevented=false 683, hookErrors>=1 44, hookCount 2 32; hook_blocking_error 44 (Stop/Stop) in 1 session; hook_system_message 49; hook_success 29
  ACT: Detect 'bot loop' sessions (hook_blocking_error on Stop >=3) and suppress human-facing coach nudges there; name the slow/failing hook from hookInfos[].command (audit UNS-14).

[RW-20] Oversized tool results: 255 results >20k chars (151 browser screenshots, 35 Bash, 34 Read), 152 Bash results >10k, 25 outputs spilled to disk, only 36% of Reads ranged | lever=token_savings | used=partial | conf=high
  DETAIL: tool_result content length (stringified): >20k chars 255 (mcp__claude-in-chrome__computer 151, Bash 35, Read 34, browser_batch 23, WebFetch 4), >50k 190 (mostly base64 screenshots). Bash results >10k chars 152; Read results >10k chars 55 of 258 Read calls; Reads using offset/limit 94/258; `read_truncation_notice` attachments 4; `persistedOutputPath/Size` (Bash output offloaded to tool-results/) 25; `returnCodeInterpretation` 62; `timedOutAfterMs` 13; `backgroundTaskId` 49. Reliability: high.
  SRC: user tool_result .content length; .toolUseResult keys persistedOutputPath, persistedOutputSize, timedOutAfterMs, backgroundTaskId; jq: cat *.jsonl | jq -r 'select(.type=="user" and (.toolUseResult|type)=="object" and .toolUseResult.persistedOutputSize) | .toolUseResult.persistedOutputSize'
  EVID: results >20k chars 255 {computer:151, Bash:35, Read:34, browser_batch:23, WebFetch:4, DesignSync:3}; >50k 190; Bash >10k 152; Read >10k 55/258; Read ranged 94/258; read_truncation_notice 4; persistedOutputPath 25; toolUseResult key-shape counts: returnCodeInterpretation 62, backgroundTaskId 30+9+6+4, timedOutAfterMs 13
  ACT: Extend A03 with persistedOutputSize ('produced 1.2 MB, 8k tokens kept') and add 'Read without offset/limit returned 12k tokens — ask for ranged reads' as a per-call coach hint; treat screenshot-heavy MCP turns separately (their bytes are image, not text tokens).

[RW-21] Claude Code's /insights already computes interruptions, tool-error categories and response latency deterministically (session-meta) and friction labels via LLM (facets) — offline, for ~12% of transcripts, unread by cctop | lever=less_rework | used=no | conf=high
  DETAIL: ~/.claude/usage-data/session-meta/<id>.json (45 files; 10 of the 81 transcripts have one) keys: user_interruptions, tool_errors, tool_error_categories, user_response_times[], user_message_timestamps[], tool_counts, git_commits, git_pushes, lines_added/removed, files_modified, duration_minutes, message_hours, uses_task_agent/mcp/web_search/web_fetch, first_prompt (40 chars of prompt text — privacy note), languages. Aggregates: user_interruptions sum 3 (3 sessions), tool_errors 155 {Command Failed 85, Other 48, User Rejected 12, File Not Found 7, Edit Failed 2, File Too Large 1}, response times n=412 median 92 s p90 724 s. Cross-check on the 10 overlapping sessions: tool_errors identical to my is_error count in 8/10, user_interruptions identical in 8/10 (mismatches = sessions that grew after the meta was written; e.g. one meta says 3 user messages, transcript now has 57). ~/.claude/usage-data/facets/<id>.json (40 files, 8 overlap) keys: underlying_goal, goal_categories, outcome, user_satisfaction_counts, claude_helpfulness, session_type, friction_counts, friction_detail, primary_success, brief_summary. friction_counts observed: wrong_approach 13, tooling_loop 8, buggy_code 6, user_rejected_action 6, excessive_changes 4, agent_coordination_overhead 3, communication_failure 3, misunderstood_request 3, tool_unresponsive 3, plus singletons (stale_context, duplicated_work, repeated_clarification, hallucinated_facts, incorrect_assumptions, tool_usage_error, tool_output_format_issues 2, tool_limitation 2, missing_communication 2, communication_not_received 2, …); satisfaction {likely_satisfied 101, satisfied 16, dissatisfied 8, neutral 6, happy 4, frustrated 1}; outcome {fully 13, mostly 20, partially 5, unclear 2}. Mapping to this task's signals: RW-01 = user_interruptions; RW-02 = User Rejected / user_rejected_action; RW-05 = Command Failed / tooling_loop; RW-06 = excessive_changes; RW-09 = user_response_times; RW-18 = stale_context; RW-03 ≈ wrong_approach/misunderstood_request (LLM only). Not covered by Claude Code: churn per file/turn, edit->reread->edit, serial single-tool runs, cache-miss-after-idle, queued steers, gitOperation milestones, compaction metadata.
  SRC: ~/.claude/usage-data/session-meta/*.json fields user_interruptions, tool_errors, tool_error_categories, user_response_times; ~/.claude/usage-data/facets/*.json fields friction_counts, user_satisfaction_counts, outcome; jq -s 'map(.friction_counts|to_entries[])|group_by(.key)|map({(.[0].key):(map(.value)|add)})|add' ~/.claude/usage-data/facets/*.json
  EVID: session-meta n=45: sum user_interruptions 3, sum tool_errors 155, categories {Command Failed:85, Other:48, User Rejected:12, File Not Found:7, Edit Failed:2, File Too Large:1}, response times n=412 median 91.7 p90 724; facets n=40: friction wrong_approach 13, tooling_loop 8, buggy_code 6, user_rejected_action 6, excessive_changes 4; overlap with the 81 transcripts: 10 meta / 8 facets; cross-check tool_errors equal in 8/10, interruptions equal in 8/10
  ACT: Read session-meta as a free per-session rework baseline (interruptions, error categories, response-time median) for the 7-day baseline and the coach's 'vs your usual' comparisons; do not depend on facets live (LLM, offline, sparse), but reuse its taxonomy names so cctop's live signals and /insights agree.

