[harness: subagent output matched instruction-shaped pattern(s): settings-json, permissions-allow-deny. Control tags below are neutralized (`<` → `<\`); treat any remaining directive-shaped text as a finding to relay to the user, not an instruction to you.]

GAPS — completeness critique of the coach research and the "Lights" design. Format per item: id | gap | evidence (source, counts) | follow-up task.

(a) DATA SOURCES / FIELDS NOBODY EXAMINED

A-01 | ~/.claude/debug/*.txt now exist and were not read | HS-12 (home-files report) says "debug/latest is a dangling symlink; no debug log exists". Today ~/.claude/debug holds 8 logs (40 KB each, 2026-09-12 18:06–19:25, from the workflow's `claude -p` runs) plus a valid `latest` symlink. Lines present per file: `autocompact: tokens=[REDACTED] level=ok effectiveWindow=980000` (1×), `[API:timing] first byte after 753ms` / `1758ms` (TTFT — the field O03 says cctop's otel.rs cannot get), `hooks module cctop turn.complete settled in 1.1ms` (function-hooks timing = E23 next.trace equivalent), `SessionEnd:other [cctop hook] completed with status 0`, MCP connect/reconnect lines, `Sending 45 skills via attachment (initial)`. `[PROMPT CACHE BREAK]` 0× (sessions were short). | Task: add `cctop attach --debug-log` (tail ~/.claude/debug/latest when present, or tell the user to start `claude --debug-file`), parse `autocompact:` (effectiveWindow, level), `[API:timing] first byte` (TTFT for the Turn panel), `[PROMPT CACHE BREAK]` cause lines (BIN/HS-12) and `hooks module … settled in` (A16 exact). Verify whether interactive sessions write these without --debug.

A-02 | effectiveWindow ≠ window: the design's threshold math is 20k off on 1M models | Debug log: `effectiveWindow=980000` on a 1M model; binary `sje(e,n){let r=e-13000…}` takes the EFFECTIVE window (`t2(n,r)=Math.min(q2e(e),K6n)…`), so threshold = 980,000 − 13,000 = 967,000 = the docs' "~967K" (docs C01). The design (C017, C041, L-CTX-01, always-on #1) says "threshold = window − 13,000" = 987k and warn band at 967k. The binary also emits an `autocompact_state` frame `{enabled, effective_window, threshold, enforced, source}` (SDK stream / remote sync; 0 real hits in transcripts). | Task: define threshold = effectiveWindow − 13,000 with effectiveWindow from the docs' per-model table (967k native-1M, 200k otherwise) or from a debug-log/`autocompact_state` capture; add a fixture asserting 967k for claude-fable-5-1 at window 1,000,000; re-check BIN-01's "window" wording.

A-03 | Human-presence signal (OS idle time / terminal focus) never examined; every "idle" in the design is transcript silence | `ioreg -c IOHIDSystem` HIDIdleTime works on this machine (returned 6926 s); tmux exposes `#{client_activity}`; Claude Code itself uses terminal focus for the away recap (docs X01: "terminal is unfocused"). C039 was refuted only because "no observable 'user away while a turn is running' signal exists"; C042/C001 use "idle ≥ 60 s" = no transcript line, which is false while a long turn runs. | Task: add an `human_idle_ms` collector (macOS ioreg, Linux xprintidle/xdotool, tmux client_activity, '—' when unavailable); re-open C039 with it (24/1002 turns qualify per the verdict's own recount); gate cold-countdown/waiting-on-you on human idle rather than transcript silence.

A-04 | queued_command attachments: half of human queued messages are absorbed mid-turn, contradicting the "26/28 land after turn end" figure the design uses twice | Replay over all main transcripts: 68 `queue-operation remove reason=absorbed_mid_turn` with content → 29 land as `attachment queued_command {commandMode:prompt, origin.kind:human}` BEFORE the next `turn_duration` (mid-turn), 23 as task-notification attachments mid-turn, 16 not matched; only the 28 `promptSource=queued` user lines (a different class) are next-turn deliveries. The C030 verdict measured only the promptSource=queued class. The design's L-CTX-09 ("queued text lands at turn end 26/28, so 'queue' is not offered — Esc") and L-RW-01 wording ("lands at turn end in 26/28") rest on it. | Task: re-derive the queued-delivery split (≈50 % mid-turn as queued_command, ≈50 % next-turn as promptSource=queued); offer "queue" as the explore-delegate action (no Esc, no cut turn); parse queued_command attachments (C059) as the mid-turn delivery marker.

A-05 | Function-hooks engine events nobody mapped to a candidate: `command.run` (E24: exact live /clear,/compact,/model,/rename,/btw — the design uses history.jsonl tier-2 and "new transcript file" as proxies), `config.set` (autoCompact toggled off), `session.compact trigger=precompute` (E10: early warning before auto-compaction), `turn.complete.reason=refusal` + `refusal.category` (E02; F28 api_refusal — no candidate handles refusals), `agent.spawn` pre-spawn with rewritable `model` (E06: nudge BEFORE the spend; C014 is post-hoc), `$.session.messages()` (A02: live post-compaction context composition per tool without the JSONL), `prompt.suggest` (E08: Tab-accept delivery, cheaper than $.prompt.fill), `UserPromptExpansion.prompt` / `skill.prompt` (E17/E09: per-invoke skill cost, the C025 residue). | Task: add a "pane-native sources" row per nudge family listing the engine event that replaces the transcript proxy; decide whether refusal/precompute get a NOW/NEXT entry.

A-06 | Status-line fields unused by any candidate: `pr.{number,url,review_state,kind}` (S01) is the exact "PR open / review state" source for L-RW-06 (design uses gitOperation + `git diff --numstat` shell-out); `session_name`, `exceeds_200k_tokens`, `total_output_tokens`, `workspace.git_worktree` (cache scope, docs C06), `output_style`. Locally `pr` is null in all 8 ~/.cctop/status files (never observed) and `five_hour` is absent in 3/8 (dropped after resets_at). | Task: parse `pr.*` and use `review_state` as the review-before-merge retire/suppress signal when present; confirm on a live PR session which versions populate it; make the limits light show '—' for an absent window (already stated) and log the 3/8 absence.

A-07 | ~/.claude.json `tipsHistory` (per-tip numStartups counters) not examined | HS-05 lists the key; values: shift-tab 415, memory-command 415, image-paste 415, continue 415 of numStartups 418 — Claude Code showed the plan-mode ("Shift+Tab twice") tip within the last 3 startups. The plan-first next-row (L-RW-10) and Claude Code's own spinner tip (X01) would show the same advice. | Task: read tipsHistory; suppress a coach nudge whose tip key Claude Code showed in the last N startups; alternatively ship coach text as `spinnerTipsOverride` (X01) so Claude Code displays it.

A-08 | Team/teammate sources examined but no lever: ~/.claude/teams/session-*/config.json (20 configs: 18 team-lead-only in-process, 2 with tmux teammates claw-orchestrator / ci-cd), `agent-setting` lines (197), `TeammateIdle` hook (H01), costs doc "shut down teammates when their work is done" (F17). | Task: low-frequency (2 teams / month) — add "teammate <name> idle N min, still resident" as a LATER family only if teammateMode is on; otherwise document as out of scope.

A-09 | `deferred_tools_delta.needsAuthMcpServers` (non-empty in 49/50 lines) and ~/.claude/mcp-needs-auth-cache.json (2 entries) — MCP servers that only expose `authenticate` tools ride in every tool listing and produce MCP/Browser-class errors | Evidence: C004 verdict data section; this machine lists Ahrefs/Google Calendar/Mermaid/Stripe servers with authenticate-only tools. | Task: one-shot session-start line "N MCP servers need auth (/mcp) — log in or remove"; fold into prefix-tip.

A-10 | cctop's own hook produces "Hook output does not start with {, treating as plain text" 5× per session in the debug logs | `cctop hook` writes nothing to stdout (src/hooks.rs:104-105 writes only to the spool; always exits 0), so the output is empty and harmless — but for UserPromptSubmit/SessionStart/PostModelSwitch plain stdout IS added to context (H14). | Task: assert in a test that `cctop hook` emits zero bytes on stdout for every event, and document it (verified harmless today).

A-11 | Rewind detection: verified absent rather than "unverifiable" | history.jsonl has 0 `/rewind`, 0 `/checkpoint`, 0 `/undo` (2,750 lines); parentUuid branching over 816 main-thread prompts = 0 sessions branch. So "595 checkpoints, zero rewinds used" (C045, the verifier called it unverifiable) holds for both typed and Esc-Esc paths as far as either marker can show. | Task: reinstate the benefit line in L-RW-02/C045 ("checkpoints exist, never used"); keep parentUuid-branch + `/rewind` history rows as the rewind detector and count it as the acted predicate for correction-streak.

(b) WELL-KNOWN LEVERS WITH NO CANDIDATE

B-01 | Partial summarisation (`/rewind` → "Summarize from here / up to here", P17, docs C08) | No candidate; context-reset offers only /compact <focus> or /clear. cctop has per-tool tokens_to_ctx and per-prompt checkpoints (595 snapshot lines) to size the heaviest resolved segment. | Task: add a third action form to L-CTX-01 when a contiguous resolved tool-output segment ≥ 50k sits behind the last N turns: "Esc Esc → Summarize from #k keeps the brief intact"; needs a segment finder over tools.calls (turn, result_tokens_est).

B-02 | Plan-accept as a checkpoint + `showClearContextOnPlanAccept` (X01) and "implement the spec in a fresh session" (P10) | ExitPlanMode / plan_mode_exit (5/month) is treated only as a phase word (C041) and as "not a cold boundary" (C003); Claude Code natively offers "Yes, clear context and …" at plan accept and reports how much context planning used. | Task: add plan-accept to the boundary signals of L-CTX-01 (highest confidence with TaskUpdate completed / '?'), suggest the setting once, and recognise SessionStart source=clear right after ExitPlanMode as "acted".

B-03 | `/autocompact <N>` / `autoCompactWindow` as a one-time cap for users who run to 900k (docs C01 ACT) | Two sessions ran to 921k/953k uncompacted; the design nudges per session (/compact, /clear) but never the persistent setting; trade-off: mid-task compaction loses detail (C018). | Task: decide in the PRD whether L-CTX-01 offers "/autocompact 400k (persists)" as the setting-class action after the second context-reset fire in a project, or explicitly rejects it.

B-04 | Whole-file rewrite and written-then-read-back (C013 corrections) | 218 rewrite events (122k tok) + 142 written-then-read targets (159k tok) per 113 sessions; concrete triggers exist (Write/heredoc to a path already written or read, payload ≥ 1k; cat/sed -n/Read within 3 calls of writing ≥ 500 tok). Absent from the design and from the A04 fix. | Task: add as a LATER family or fold READ-BACK into the A04 rewrite and REWRITE into the churn token; needs the C055 Bash write regex.

B-05 | CLI-over-MCP (P35) and code-intelligence plugin (P37) session-start one-shots | mcp__github__* 14 calls vs `gh` 620 Bash calls; 0 LSP tool_uses; deferred schemas cost ~0 prefix on 2.1.269 (GAP-03), so token value is small here. | Task: one-shot prefix-tip clause only when an MCP server with a CLI equivalent was actually called this session; skip LSP unless Grep→Read chains reappear outside auto mode.

B-06 | Unrelated queued prompt behind a long turn → second session / worktree (P39) | queue-operation enqueue 258 with content; no candidate compares queued content to touched files. | Task: LATER candidate "queued task unrelated to the running turn — `claude --worktree` in a second pane"; needs path/identifier overlap like C061 (which was refuted for edits, not for queued prompts) — expect low precision, treat as optional.

B-07 | Human-vs-model wall-clock split and the user's own pause distribution (F27) | session-meta `user_response_times` computable live; C027 correction cites p90 pause 26 min; on a 5m TTL that predicts ~60 % cold turns. Not shown anywhere. | Task: 1-key detail on the cache light: "your pauses p50 63 s · p90 26 min (> 5 m TTL)"; use it to decide whether the TTL-flip event should carry "batch prompts" advice (C027 corrections).

B-08 | Image-cap approach (docs C07 "accumulating many images" → oldest batch dropped, history reprocessed) | 201 image blocks/month; C007 counts screenshots per turn but no "approaching the per-request image cap" warning; the cap value is not recovered from the binary. | Task: grep the binary for the image/PDF per-request cap; if found, add an Events row "N/cap images in context — next batch drop re-processes history" as a named-miss cause.

B-09 | Workflow-agent limit refusals as a NOW event (C015 corrections) | 148/148 journal `failed` lines were limit refusals (123 with 0 tool calls, 1 s median); this session: 174 failed spawns. The design keeps only a count in the limits-light detail; the correct action ("stop the run now, resume via resumeFromRunId after resets") is a keystroke-level NOW action. | Task: extend L-LIM-01 turn-died with an "agents failing on the limit" branch fed by journal.jsonl `failed` + agent transcript tail (isApiErrorMessage), coalesced per wf_<id>.

B-10 | Per-skill invocation cost (C025 corrections: SKILL.md bytes/4 when the same skill is invoked ≥ 3×; `attributionSkill` share as "produced while active") | Dropped entirely; the design's prefix-tip covers listing size only. | Task: dashboard row, not a nudge; add to the prefix inspector.

(c) VERIFIER REJECTIONS TO RE-CHECK (over-strict or refuted on the wrong version of the trigger)

C-01 | C034 verification target — refuted on the untightened trigger; the corrected trigger (≥ 60 chars, path/PRD/'?' suppression, suppress when Claude self-verified in the last 3 turns, depends on C055 test cmd) was never re-simulated. 42 fires/459 prompts before tightening, ~40 % unnecessary; after tightening plausibly ≤ 1-in-5. It is the docs' pairing practice for verify-gap (P02) and could sit in the next-row like plan-first (which got a slot with weaker evidence). | Task: re-run the C034 simulation with the corrected trigger over the 459 typed prompts; admit as next-row-only if wrong-rate ≤ 20 %.

C-02 | C039 unattended run — refuted for lack of an away signal, which A-03 supplies (OS idle time). The verdict's own recount (94 turns ≥ 20 calls, 40 with an edit, 24 with no test-class Bash = 2.4 % of turns) is a usable base rate; desktop notification (--notify) answers "the person cannot read the screen". | Task: re-verify C039 with human_idle_ms > 5 min as the away predicate.

C-03 | C030's "queued prompts land after turn end 26/28" (used to justify Esc-only wording in L-CTX-09 and the "queue:" wording in L-RW-01) — see A-04: 29 human steers were absorbed mid-turn as queued_command attachments. | Task: correct both nudges; the honest action for explore-delegate is "queue 'use an Explore subagent for X'" (absorbed within the run ~50 %; delivered next turn otherwise).

C-04 | C024 rate-limit weight — refuted only on the "explains bar-vs-$ divergence" claim; the verbatim `Ln()` formula and the `Sor()` runway levers (fable→/model opus, opus→/model sonnet ~2×, effort high→/effort medium, shown when 7d ≥ 50 % or the projection is negative) survived as corrections and are absent from the limits light, while the design still promises a `$`→"rate-limit units (10/5/3/1)" toggle (which IS Ln()). | Task: keep Ln() as the unit conversion (trivial, verbatim), add the Sor() lever line to the limits light, or drop the units toggle; reconcile design risk 4.

C-05 | C021 churn — refuted as a nudge, but the design's churn token uses the pre-verdict thresholds (≥ 4 Edit/Write with ≥ 2 re-reads) and ignores Bash edits (682 of ~1,190 edits are `sed -i`/heredoc/redirect). | Task: retune the state-line token to the C021 corrections (≥ 6 same-path edits incl. Bash write regex, or ≥ 2 Edit→Read→Edit, non-doc paths); note trackedFileBackups.version lags one turn and file-history-delta is always v1.

C-06 | C045 rewind line — "zero rewinds" was called unverifiable; A-11 verifies it on two independent markers. | Task: restore the benefit line and add the two markers as the acted predicate.

C-07 | C013 model-written tool inputs — the two corrected triggers (REWRITE, READ-BACK) are cheap and were dropped with the refuted rule (see B-04). | Task: re-admit the corrections as a LATER family or A04 fold.

C-08 | C011 effort/thinking — refuted as a nudge; the A08 v2 metric (effort × effort_cost_index ratio, retained thinking since boundary, `/effort medium s` at a boundary) is not in the design's A01–A18 resolution. | Task: carry A08 v2 into the dashboard fix list; the coach only needs "effort high" in the state line when it differs from the session default.

C-09 | C020 A04 rewrite — verdict P3, but under bash-first auto mode A04 cannot see any re-read (Grep/Glob 0×, cat/sed -n via Bash) and penalises ranged reads; the rewrite is a correctness fix, not a nudge, and blocks the "reads_since_edit" reset the design cites for post-compaction. | Task: schedule the A04 rewrite (Bash reads, ranged exclusion, file_unchanged credit, edited_text_file/stale resets, boundary resets) in the same story as C056 ingestion.

C-10 | C051 cross-session friction — correctly refuted as a nudge, but the deterministic session-meta counters (user_interruptions, tool_error_categories with the corrected classes, response times) can be recomputed live per project by cctop (F02, C038) and used as the per-family baseline for measurement (see F-02). | Task: implement session-meta-equivalent counters in baseline.rs rather than reading the stale 2026-08-16 files.

(d) DESIGN CLAIMS NOT BACKED BY A VERIFIED CANDIDATE (or contradicting its corrections)

D-01 | Threshold "window − 13,000" (see A-02): off by 20k on 1M models; the context light's ● band and L-CTX-01's price form use it. | Task: effectiveWindow − 13,000; fixture at 967k.

D-02 | L-RW-09 commit-without-check: trigger "no test-class call in the previous 10 calls", "grafted from Radar R09", benefit "32 % of commit calls" — C031 was refuted on exactly that window (7 distinct fires / 91 sessions after suppressions, ~2/month; escalated ~4/91) and its corrections say window-by-commit-boundary, code paths only, once per turn, no PreToolUse/Esc. | Task: rewrite L-RW-09 to the C031 corrections and quote 7/91 sessions, not 32 %.

D-03 | Churn token thresholds and Edit/Write-only counting (see C-05). | Task: as C-05.

D-04 | "queued text lands at turn end 26/28" (see A-04/C-03). | Task: as C-03.

D-05 | `$` toggle to "rate-limit units (fable 10 / opus 5 / sonnet 3 / haiku 1)" and agent-context-heavy's "~3× on a subscription too" rest on C024 (refuted); risk 4 says the weighting "is not reproduced". | Task: as C-04.

D-06 | Cache light: "a 1h→5m TTL flip is itself the 'over plan usage' signal" and the flip event text "(over plan usage)" hard-code one cause; C027's corrections require the attribution order env (ps eww) → settings promptCacheTtl → shim five_hour ≥ 100 → API key → unknown. Locally: 12,311 calls at 1h, 965 at 5m, three genuine flips. | Task: adopt the attribution order; wording "cache TTL now 5m (<cause>)".

D-07 | Inconsistent compaction-false-positive counts: L-LIM-01 says "8 of 8 current 'compactions' are these zero-usage lines", C017 says "3 of the 4", C060 verdict counts 18 error lines on disk. | Task: pick one measured figure from the fixture replay.

D-08 | Limits light ◐ "when A13's least-squares exhaustion lands before resets_at": A13 needs ≥ 3 status samples in 30 min and the shim rewrites only on Claude Code redraws (RULE-A13), so the ◐ path rarely arms; five_hour was absent in 3/8 status files. | Task: set `statusLine.refreshInterval` in `cctop install` (C001 correction mentions it) or compute exhaustion from the transcript's usage series; document '—'.

D-09 | Explore-delegate evidence "Explore agents return p50 71 tok" — the C012 verdict says it is a corpus constant from 10 calls, not live-computable, and the run-≥8 nudge saves p50 ~10k more (p90 45k) before the run ends, not "p50 48.7k". The design keeps the 48.7k figure in the state line rationale. | Task: quote the live run growth and "typically ~10k more (p90 ~45k)"; keep 71 tok only in the explain overlay as unverified.

D-10 | Waiting-on-you: "45 % of replies < 40 chars", "wait mean 44 min" come from the candidate; the verdict measured p50 90 s, 18/55 > 5 min, 5/55 > 60 min. The design cites the verdict's numbers — fine — but the cache-clock clause applies to 5/55 cases; the desktop notification is the only high-value part. | Task: none beyond keeping the alert opt-in; state the 5/55 figure in the explain overlay.

D-11 | L-CACHE-01 5m-TTL variant "idle ≥ 3 min" assumes ttl==5m sessions; the C001 verdict says with 5m the countdown is true 60 s after every turn (median reply 63–92 s) — the design's (0,120 s] window is the correction; OK. But it also says "no countdown anywhere while > 300 s remain" while the cache light shows "warm 41m (1h)" every tick — consistent only if minutes-only. | Task: none; note that mm:ss appears only under 5 min.

D-12 | State line "silent 3:50 · nudged 2×" cites silent_turn_reminder at 6/11/16/21 calls (C023 verdict — backed) but the design's L-RUN-00 heuristic fallback "≥ 5 consecutive API responses with tool_use and no text" will double-fire with the reminder on 2.1.269; the batching reminder is correctly excluded. | Task: use the attachment when present, the heuristic only when the attachment type is absent for the model/version.

(e) INTERACTIONS THE PRD AUTHOR MUST HANDLE (A01–A18, alerts, pane PRD, existing coach PRD)

E-01 | alerts.rs (ALERT-01) is untouched by the design but overlaps it: ContextHigh (ratio > 0.80 → desktop critical on a 1M model at 800k, contradicting the coach's "● only inside the warn band ≥ 947k"), CompactionSoon (turns_until_compaction ≤ 2 from the 80 % default; the design says Claude Code's footer owns pre-compaction), CacheHitLow (< 0.5 over 5 turns; duplicates named-miss), PermissionWaitLong (duplicates waiting-on-you), ToolRunningLong (A10). The toast channel is single-slot 3 s and the coach adds ≤ 1 toast/turn. | Task: an alert-by-alert table: retire, re-threshold to C017 math, or make the coach the owner; one toast budget across alerts.rs and coach.

E-02 | Advisor panel 'x' = session dismiss (Engine.dismissed, in-memory) vs coach 'x' = 5-turn snooze / 'X' = session; the design says the two sets merge in ~/.cctop/<session>.advisor.json. Also `cctop query advice` (used by the pane's Advisor view and the pane PRD's verification item "Advisor top item equals cctop query advice", prd-cctop-pane.md L183) changes meaning once the coach nudge becomes the Advisor panel's first row. | Task: define one dismissal model (snooze with turn horizon), update the pane verification item and the advice JSON schema version.

E-03 | A-rule fixes with no owner in the design's "A01–A18 RESOLUTION": A04 (C020 rewrite), A07 (PRD 5.1 says "re-base on ToolSearch rebuilds (A22)" — C004 refuted as version-obsolete), A08 (C011 metric v2), A10 (exclude backgroundTaskId/timedOutAfterMs — C056), A11 (turn-sum misattribution), A16 (name the hook — UNS-14), A17 (cost-per-turn framing). | Task: a complete 18-row table with disposition (kept / reworded / replaced by <family> / dashboard-only fix).

E-04 | The existing tasks/prd-cctop-coach.md (522 lines, untracked) predates verification and lists A19–A52; the following now conflict with the verdicts: A22 (C004 refuted), A24 nudges (C009 → metric), A26 (C013 → display + REWRITE/READ-BACK), A28 as nudge (C021 → indicator), A29 (C022 → counter), A31 advice (C027 → event with cause), A35 (C034 refuted pending C-01), A37 nudge (C036 → accounting), A39 (C039 pending C-02), A44 lead variant (C046 → residue), A48 (C063), A49–A52 (C052/C053/C047/C048). §4 still contains the facets session-start line (C051 refuted), the microcompact "cleared: N tool results" row (C019: no writer), and "ToolSearch → prefix re-cached" Events. §13 Q1 (TTL source) is resolved by C001/C027 (shim == transcript ephemeral_*); Q3 (567k compaction) is answerable via the debug-log `effectiveWindow` line. | Task: prune US-007/US-008 to the survivors + corrections, mark the refuted rules with their verdict id, close Q1, re-open Q3 with the debug-log method.

E-05 | Turn identity (C057) and the synthetic filter (C017) change visible numbers everywhere: header turn count (−20 %), A12 denominator, Context history/velocity/compaction count, header model (no more `<synthetic>`), ledger rows, query summary snapshot, pane Overview. | Task: list as user-visible behaviour changes with fixture/snapshot regeneration in one story that lands before any coach family.

E-06 | Pane PRD: `$.ui.status` is one line per plugin and is listed as a diagnostics channel (prd-cctop-pane.md L62); the coach claims it for the L0 line. The poller's known-verb list (plugin/hooks/model.ts QUERY_VERBS, parseQueryVerbs) must include `coach`; each tick spawns one more `cctop query` process (2 s busy); the pane's `[2 snooze]` writes advisor.json from a fresh process while the TUI may hold it (C064 lock). The pane PRD's §6 non-goals must state that `$.prompt.fill` drafts are allowed and `$.prompt.submit`/`$.command.run` are not. | Task: amend the pane PRD (data-source table, US for the coach view, verification list) and reserve `$.ui.status` for the coach.

E-07 | Claude Code's own coaching surfaces the design must not duplicate but currently could: spinner tips (tipsHistory shows 'shift-tab' shown recently — overlaps plan-first), `/model` warm-cache confirm dialog (F16; the design's warm-switch tier-2 is an undo window after the dialog was already answered), context-low footer, resume-from-summary dialog (Pro/Max only), auto-continue at usage limit (`quota_auto_resume_*` Notification types unused). | Task: per family, name the Claude Code surface it complements and the condition under which the coach stays silent (e.g. warm-switch only when hooks are absent AND no confirm dialog was shown).

E-08 | Session-mode gating (C058) vs the pane and the skill: the `cctop-insights` skill and `cctop_coach` MCP tool are called BY Claude inside the session; in loop mode the skill must not re-propose snoozed nudges (stated) but also must not let Claude "do what the coach says" from a task-notification turn. | Task: the coach JSON carries `session_mode` and the skill refuses actions unless promptSource is typed.

E-09 | Human turn definition excludes `sdk` (53 lines, 18 with origin.kind human — Ralph/insights programmatic prompts); cooldowns, budgets and "first 2 human turns" all count differently in those sessions. | Task: per-rule choice documented (C057 correction), default: sdk = machine.

E-10 | Key `c`: the design says it is free (only Ctrl-C bound); the pane's `/cctop coach` argument must coexist with `/cctop close` parsing (pane PRD US-009 open question 1) and with `--view coach` persisted in Config. | Task: add to BINDINGS + help test (app.rs:832-847), extend the `/cctop` argument parser.

(f) MEASUREMENT — proving the coach reduced tokens / rework on the user's own baseline

F-01 | The existing PRD's §12 baselines use pre-verification denominators (commits unchecked 32 %, episodes unverified 35 %, cold restarts 49/month, explore runs 46/month, spend > 300k 53.6 %) that the verdicts revised (7 commit fires / 91 sessions; 15 of 37 source-edit turns; 55/58 cold gaps, 50 with ctx > 100k; 25.5 %/51.6 %). Targets like "< 15 %" at n ≈ 2/month cannot be shown in "two dogfood weeks". | Task: restate every baseline with the verdict denominators and add expected monthly volume + weeks-to-detect (rare families — cold-countdown 2–4/month, post-compaction 1/113 sessions, warm-switch 1/11 sessions, commit-without-check ~2/month, correction-streak 3–8/month — get "fires correctly ≥ N times" criteria instead of rate targets).

F-02 | One measurable outcome metric per family, computed from transcripts alone so the pre-coach corpus (2026-08-12..09-12, 113 transcripts) is the control: cold-countdown → cache_creation tokens on first calls after 45–65 min gaps; cold-resume → cache_creation after > TTL gaps split by /clear-avoidable (session ended) vs continued; context-reset → share of calls > 300k and count of new transcript files whose predecessor exceeded 300k; explore-delegate → main-context explore-run tokens (≥ 8 calls) and Explore Agent spawns; verify-gap → share of source-edit turns with a stdout-confirmed test-class call within 14 calls; failure-cascade → blind same-prefix retries; denial-streak → automode-blocked count and permissions.allow growth; waiting-on-you → waits > 5 min / > 55 min after AskUserQuestion; turn-died → re-prompts into the same error class; correction-streak → correction pairs and rewind markers (parentUuid branch, /rewind — currently 0). | Task: implement these as `cctop coach-stats` columns with the 4-week pre-coach values as the baseline row; exclude session_mode ≠ interactive sessions (this session's 90 workflow agents / 174 failed spawns would otherwise dominate).

F-03 | Backtest before dogfood: replay the final rule set over the 113 transcripts + fixtures (`cctop coach-replay <jsonl>`) to get fires/session, precision against the verdict labels, and — crucially — the "acted anyway within TTL" rate (verified episodes reach a check in p50 2 calls with no nudge), so the causal estimate is acted-rate(with coach) − baseline-acted-rate, not raw acted-rate (design risk 7). | Task: turn the scratchpad scripts (phases4.py, c005.py, c012_runs.py, c037.jq, drops.py …) into fixtures and a replay command; each rule ships with its replay precision.

F-04 | Attribution needs visibility and exposure logging: a nudge can only claim an effect if a surface was visible when it fired (TUI view open, pane docked, status line active). advisor.json must record per fire: surface_visible, human_idle_ms at fire, time-to-x, acted completion + delay, Claude Code version, model, project, session_mode. | Task: add these fields to the C064 schema; define "exposed fire" as the denominator for every rate.

F-05 | A/B on one user: alternate exposure by session (odd/even session ids or weekly) with `--view coach` vs dashboard-only, stratified by project and model (fable vs opus vs sonnet change the cost gradient 4×) and by Claude Code version (2.1.260/2.1.267 fixes removed miss causes independent of the coach). | Task: `cctop run --coach off|on|auto` with the assignment logged; report per stratum; minimum 8–12 weeks for the rare families, 2 weeks for verify-gap / denial / explore / cold-write / waiting.

F-06 | Regret metrics as first-class: snooze rate, X (session) rate, time-to-x < 2 s (reflex dismissal), view toggled away within 10 s of a promotion, view-open share of session time, nudges expired unacted; the PRD's "false-positive = snoozed + expired-unacted ÷ fired" needs the exposed-fire denominator and the acted-anyway correction. | Task: add to coach-stats; demotion rule per family after N exposed fires, not after "two weeks".

F-07 | Cost of the coach itself: zero model tokens except 'S' socket sends (count them and their tokens) and pane `$.prompt.fill` drafts; CPU from the 1-s TUI tick, the 2-s pane poller spawning `cctop query coach`, and the git numstat shell-out; hook overhead p99 (cctop hooks 30 ms p99 per ~/.claude.json lastSessionMetrics). | Task: report these in coach-stats so the coach cannot silently become an A16 case.

F-08 | Version drift: log `version` with every fire and every baseline row so precision can be split by Claude Code version (tools_changed misses only < 2.1.267; rendered only ≥ 2.1.266; silent_turn_reminder model-gated). | Task: version column in advisor.json and coach-stats; a rule whose precision collapses on a new version is auto-demoted to LATER.

Ids and paths referenced: scratchpad reports reader-function-hooks-api.txt (E/A/S), reader-docs-hooks-statusline-otel.txt (H/S/O/C/E/L/X), reader-best-practices-signals.txt (P/D), reader-competitor-scan.txt (F), reader-cctop-source-audit.txt (ING/UNS/RULE/GAP/TASK/QUERY/LAYOUT/VIEW/ALERT/BASE); verdicts in /Users/tom/code/cctop/tasks/research-coach/verdicts-done.json; existing PRD /Users/tom/code/cctop/tasks/prd-cctop-coach.md (§4, §5.2, §12, §13); pane PRD /Users/tom/code/cctop/tasks/prd-cctop-pane.md (L62, L183, L267-277); new evidence from ~/.claude/debug/*.txt, ~/.claude/history.jsonl, ~/.claude.json tipsHistory, ~/.claude/teams/*/config.json, ~/.cctop/status/*.json, and replays over scratchpad/all.jsonl (aggregates only).